#pragma once

#include <QtCore/QtGlobal>

#if defined(SKRIBISTO_APPLICATION_SCENE_LIBRARY)
#define SKRIBISTO_APPLICATION_SCENE_EXPORT Q_DECL_EXPORT
#else // if defined(SKRIBISTO_APPLICATION_SCENE_LIBRARY)
#define SKRIBISTO_APPLICATION_SCENE_EXPORT Q_DECL_IMPORT
#endif // if defined(SKRIBISTO_APPLICATION_SCENE_LIBRARY)