// Generated by Qleany v1.4.2 from feature_events_h.tera
#pragma once

#include <QList>
#include <QMetaObject>
#include <QObject>

namespace Skribisto::Common::Features
{
class WorkManagementEvents : public QObject
{
    Q_OBJECT
  public:
    explicit WorkManagementEvents(QObject *parent = nullptr) : QObject(parent)
    {
        // Register metatypes for cross-thread signal delivery
        qRegisterMetaType<QList<int>>("QList<int>");
    }

  public Q_SLOTS:
    // These methods can be invoked from any thread; they will emit signals in this object's thread

    void publishLoadWorkSignal()
    {
        Q_EMIT loadWorkSignal();
    }

    void publishSaveWorkSignal()
    {
        Q_EMIT saveWorkSignal();
    }

  Q_SIGNALS:
    // Do not hesitate to give arguments if needed. If you do, make sure they are registered metatypes
    // for cross-thread signal deliver. Also, modify the corresponding "unit of work" file. And, if existing,
    // the QML mock file.

    void loadWorkSignal();

    void saveWorkSignal();
};

} // namespace Skribisto::Common::Features
