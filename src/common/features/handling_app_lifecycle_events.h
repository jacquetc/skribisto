// Generated by Qleany v1.4.5 from feature_events_h.tera
#pragma once

#include <QList>
#include <QMetaObject>
#include <QObject>

namespace Skribisto::Common::Features
{
class HandlingAppLifecycleEvents : public QObject
{
    Q_OBJECT
  public:
    explicit HandlingAppLifecycleEvents(QObject *parent = nullptr) : QObject(parent)
    {
        // Register metatypes for cross-thread signal delivery
        qRegisterMetaType<QList<int>>("QList<int>");
    }

  public Q_SLOTS:
    // These methods can be invoked from any thread; they will emit signals in this object's thread

    void publishInitializeAppSignal()
    {
        Q_EMIT initializeAppSignal();
    }

    void publishCleanUpBeforeExitSignal()
    {
        Q_EMIT cleanUpBeforeExitSignal();
    }

  Q_SIGNALS:
    // Do not hesitate to give arguments if needed. If you do, make sure they are registered metatypes
    // for cross-thread signal deliver. Also, modify the corresponding "unit of work" file. And, if existing,
    // the QML mock file.

    void initializeAppSignal();

    void cleanUpBeforeExitSignal();
};

} // namespace Skribisto::Common::Features
