// Generated by Qleany v1.6.1 from entity_repository_h.tera
#pragma once

#include "database/db_context.h"
#include "database/snapshot_types.h"
#include "direct_access/event_registry.h"
#include "direct_access/work/i_work_repository.h"
#include "direct_access/work/work_events.h"
#include "entities/work.h"
#include "signal_buffer.h"
#include <QPointer>

namespace Skribisto::Common::DirectAccess::Work
{
namespace SCE = Skribisto::Common::Entities;

class IWorkTable
{
  public:
    virtual ~IWorkTable() = default;

    // Creation assigns new ids
    virtual QList<SCE::Work> createMany(const QList<SCE::Work> &works) = 0;

    // Update
    virtual QList<SCE::Work> updateMany(const QList<SCE::Work> &works) = 0;
    virtual QList<SCE::Work> updateWithRelationshipsMany(const QList<SCE::Work> &works) = 0;

    // Query/Delete
    [[nodiscard]] virtual QList<SCE::Work> findMany(const QList<int> &ids) const = 0;
    [[nodiscard]] virtual QList<SCE::Work> findAll() const = 0;
    virtual QList<int> removeMany(const QList<int> &ids) = 0;
    // Relationship setters/getters
    // Set the relationship value for a given Work id
    virtual void setRelationshipIds(int workId, WorkRelationshipField relationship, QList<int> relatedId) = 0;

    // Get the relationship value for a given Work id
    [[nodiscard]] virtual QHash<int, QList<int>> getRelationshipIdsMany(const QList<int> &workIds,
                                                                        WorkRelationshipField relationship) const = 0;
    virtual int getRelationshipIdsCount(int rootId, WorkRelationshipField relationship) const = 0;
    virtual QList<int> getRelationshipIdsInRange(int rootId, WorkRelationshipField relationship, int offset,
                                                 int limit) const = 0;

    // Move specific related IDs to a new position (only meaningful for ordered relationships)
    virtual QList<int> moveRelationshipIds(int workId, WorkRelationshipField relationship, const QList<int> &idsToMove,
                                           int newIndex) = 0;

    // Snapshot / Restore (table level)
    virtual Database::TableLevelSnapshot snapshotRows(const QList<int> &ids) = 0;
    virtual void restoreRows(const Database::TableLevelSnapshot &snap) = 0;
    // Reverse lookup: find owner IDs that reference the given child IDs via the owner junction
    [[nodiscard]] virtual QList<int> getOwnerIds(const QList<int> &childIds) const = 0;
};

class WorkRepository : public IWorkRepository
{
  public:
    WorkRepository(std::unique_ptr<IWorkTable> table, Database::DbSubContext &dbSubContext,
                   QPointer<EventRegistry> eventRegistry, QPointer<Common::SignalBuffer> signalBuffer);

    ~WorkRepository() override = default;

    // CRUD
    QList<SCE::Work> createOrphans(const QList<SCE::Work> &works) override;
    QList<SCE::Work> create(const QList<SCE::Work> &works, int ownerId, int index) override;
    QList<int> getRelationshipsFromOwner(int ownerId) const override;
    void setRelationshipsInOwner(const QList<int> &itemIds, int ownerId) override;
    QList<SCE::Work> get(const QList<int> &workIds) const override;
    QList<SCE::Work> getAll() const override;
    QList<SCE::Work> update(const QList<SCE::Work> &works) override;
    QList<SCE::Work> updateWithRelationships(const QList<SCE::Work> &works) override;
    QList<int> remove(const QList<int> &workIds) override;

    // Snapshot / Restore
    Database::EntityTreeSnapshot snapshot(const QList<int> &ids) override;
    void restore(const Database::EntityTreeSnapshot &snap) override;

    // Relationships
    void setRelationshipIds(int workId, WorkRelationshipField relationship, QList<int> relatedId) override;
    QList<int> getRelationshipIds(int workId, WorkRelationshipField relationship) const override;
    QHash<int, QList<int>> getRelationshipIdsMany(const QList<int> &workIds,
                                                  WorkRelationshipField relationship) const override;
    int getRelationshipIdsCount(int rootId, WorkRelationshipField relationship) const override;
    QList<int> getRelationshipIdsInRange(int rootId, WorkRelationshipField relationship, int offset,
                                         int limit) const override;
    QList<int> moveRelationshipIds(int workId, WorkRelationshipField relationship, const QList<int> &idsToMove,
                                   int newIndex) override;

  private:
    std::unique_ptr<IWorkTable> m_table;
    QPointer<WorkEvents> m_events;                 // not owned
    QPointer<EventRegistry> m_eventRegistry;       // not owned
    QPointer<Common::SignalBuffer> m_signalBuffer; // not owned

    // For cascade operations
    Database::DbSubContext &m_dbSubContext;

    void emitCreated(const QList<int> &ids) const;
    void emitUpdated(const QList<int> &ids) const;
    void emitRemoved(const QList<int> &ids) const;
    void emitRelationshipChanged(int workId, WorkRelationshipField rel, const QList<int> &relatedIds) const;
};

} // namespace Skribisto::Common::DirectAccess::Work
