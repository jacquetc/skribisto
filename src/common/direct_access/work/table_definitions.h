// Generated by Qleany v1.4.2 from table_definitions_h.tera
#pragma once
#include <QList>
#include <QString>
#include <QStringLiteral>

namespace Skribisto::Common::DirectAccess::Work
{

inline QString getSqlTableDefinition()
{
    return QStringLiteral(
        "CREATE TABLE IF NOT EXISTS work ("
        "    id INTEGER PRIMARY KEY ON CONFLICT ROLLBACK AUTOINCREMENT UNIQUE ON CONFLICT ROLLBACK NOT NULL,"
        "    created_at TEXT NOT NULL,"
        "    updated_at TEXT NOT NULL,"
        "    title TEXT NOT NULL DEFAULT '',"
        "    author_name TEXT NOT NULL DEFAULT '',"
        "    dict_language TEXT NOT NULL DEFAULT '',"
        "    binders TEXT NOT NULL DEFAULT '[]',"
        "    tags TEXT NOT NULL DEFAULT '[]',"
        "    dict_words TEXT NOT NULL DEFAULT '[]'"
        ");");
}

inline QList<QString> getSqlJunctionTableDefinitions()
{
    QList<QString> definitions;

    // ordered one-to-many junction table for binders
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS work_binders_to_binder_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    order_ INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    // unordered one-to-many junction table for dict_words
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS work_dict_words_to_dict_word_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    // unordered one-to-many junction table for tags
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS work_tags_to_binder_tag_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    return definitions;
}

} // namespace Skribisto::Common::DirectAccess::Work
