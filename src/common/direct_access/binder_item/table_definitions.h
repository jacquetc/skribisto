// Generated by Qleany v1.4.2 from table_definitions_h.tera
#pragma once
#include <QList>
#include <QString>
#include <QStringLiteral>

namespace Skribisto::Common::DirectAccess::BinderItem
{

inline QString getSqlTableDefinition()
{
    return QStringLiteral(
        "CREATE TABLE IF NOT EXISTS binder_item ("
        "    id INTEGER PRIMARY KEY ON CONFLICT ROLLBACK AUTOINCREMENT UNIQUE ON CONFLICT ROLLBACK NOT NULL,"
        "    created_at TEXT NOT NULL,"
        "    updated_at TEXT NOT NULL,"
        "    title TEXT NOT NULL DEFAULT '',"
        "    sub_title TEXT NOT NULL DEFAULT '',"
        "    role TEXT NOT NULL DEFAULT '',"
        "    sub_role TEXT NOT NULL DEFAULT '',"
        "    label TEXT NOT NULL DEFAULT '',"
        "    activated INTEGER NOT NULL DEFAULT 0,"
        "    is_favorite INTEGER NOT NULL DEFAULT 0,"
        "    is_printable INTEGER NOT NULL DEFAULT 0,"
        "    indent INTEGER NOT NULL DEFAULT 0,"
        "    word_count_goal INTEGER NOT NULL DEFAULT 0,"
        "    char_count_goal INTEGER NOT NULL DEFAULT 0,"
        "    dict_language TEXT NOT NULL DEFAULT '',"
        "    contents TEXT NOT NULL DEFAULT '[]',"
        "    _references TEXT NOT NULL DEFAULT '[]',"
        "    tags TEXT NOT NULL DEFAULT '[]'"
        ");");
}

inline QList<QString> getSqlJunctionTableDefinitions()
{
    QList<QString> definitions;

    // unordered one-to-many junction table for contents
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS binder_item_contents_to_content_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    // many-to-many junction table for references
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS binder_item_references_to_binder_item_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    // many-to-many junction table for tags
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS binder_item_tags_to_binder_tag_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    return definitions;
}

} // namespace Skribisto::Common::DirectAccess::BinderItem
