// Generated by Qleany v1.6.1 from entity_table_cpp.tera
#include "dict_word_table.h"
#include "database/db_context.h"
#include "database/junction_table_ops/junction_cache.h"
#include "database/junction_table_ops/unordered_one_to_many.h"
#include "database/snapshot_types.h"
#include "database/table_cache.h"
#include "entities/dict_word.h"

#include <QDateTime>
#include <QList>
#include <QtSql/QSqlDatabase>
#include <QtSql/QSqlError>
#include <QtSql/QSqlQuery>
#include <QtSql/QSqlRecord>
#include <algorithm>
#include <iterator>

namespace SCDDictWord = Skribisto::Common::DirectAccess::DictWord;
using namespace Skribisto::Common::Database;
namespace SCE = Skribisto::Common::Entities;

// Cache type alias (used across all methods)
using DictWordCache = TableCache<SCE::DictWord, SCDDictWord::DictWordRelationshipField>;

// Forward relationship junction tables

// Backward relationship junction tables

const QString WORK_DICT_WORDS_JUNCTION_TABLE = "work_dict_words_to_dict_word_junction"_L1;

// Pre-built SQL column lists (compile-time constants, reused across create/update/find)
static const QString COLUMNS_NO_ID = QStringLiteral("created_at,updated_at,word");
static const QString COLUMNS_WITH_ID = QStringLiteral("id,") + COLUMNS_NO_ID;

static const QString PLACEHOLDERS_NO_ID = QStringLiteral(":created_at,:updated_at,:word");
static const QString PLACEHOLDERS_WITH_ID = QStringLiteral(":id,") + PLACEHOLDERS_NO_ID;

// Helper: extract IDs from a list of entities
static QList<int> extractIds(const QList<SCE::DictWord> &items)
{
    QList<int> ids;
    ids.reserve(items.size());
    std::ranges::transform(items, std::back_inserter(ids), [](const SCE::DictWord &item) { return item.id; });
    return ids;
}

SCDDictWord::DictWordTable::DictWordTable(DbSubContext &dbSubContext) : m_dbSubContext(dbSubContext)
{
}

QList<SCE::DictWord> SCDDictWord::DictWordTable::createMany(const QList<SCE::DictWord> &dictWords)
{
    QList<SCE::DictWord> created;
    created.reserve(dictWords.size());

    QSqlDatabase db = m_dbSubContext.getConnection();

    // Pre-build both SQL variants (with/without explicit id)
    const QString sqlWithId = "INSERT INTO dict_word (%1) VALUES (%2)"_L1.arg(COLUMNS_WITH_ID, PLACEHOLDERS_WITH_ID);
    const QString sqlWithoutId = "INSERT INTO dict_word (%1) VALUES (%2)"_L1.arg(COLUMNS_NO_ID, PLACEHOLDERS_NO_ID);

    QSqlQuery q(db);

    for (SCE::DictWord r : dictWords)
    {
        const bool hasExplicitId = r.id > 0;
        q.prepare(hasExplicitId ? sqlWithId : sqlWithoutId);

        // Set timestamps if not provided
        if (r.createdAt.isNull())
            r.createdAt = QDateTime::currentDateTimeUtc();
        if (r.updatedAt.isNull())
            r.updatedAt = r.createdAt;

        if (hasExplicitId)
            q.bindValue(":id"_L1, r.id);
        q.bindValue(":created_at"_L1, r.createdAt.toString(Qt::ISODate));
        q.bindValue(":updated_at"_L1, r.updatedAt.toString(Qt::ISODate));
        q.bindValue(":word"_L1, r.word);

        if (!q.exec())
        {
            qCritical() << "Failed to insert DictWord:" << q.lastError().text();
            return {};
        }

        // Retrieve the auto-generated id
        if (!hasExplicitId)
            r.id = q.lastInsertId().toInt();

        // Handle junction table relationships

        created.append(std::move(r));
    }

    // Invalidate cache for created entities
    if (!created.isEmpty())
        DictWordCache::instance().invalidateEntities(extractIds(created));

    return created;
}

QList<SCE::DictWord> SCDDictWord::DictWordTable::updateMany(const QList<SCE::DictWord> &dictWords)
{
    QList<int> updatedIds;
    updatedIds.reserve(dictWords.size());

    QSqlDatabase db = m_dbSubContext.getConnection();

    // Build SET clause once
    const QString sqlString = QStringLiteral("UPDATE dict_word SET "
                                             "id = :id"
                                             ",created_at = :created_at"
                                             ",updated_at = :updated_at"
                                             ",word = :word"
                                             " WHERE id = :id");

    QSqlQuery q(db);

    for (const SCE::DictWord &r : dictWords)
    {
        q.prepare(sqlString);
        q.bindValue(":id"_L1, r.id);
        q.bindValue(":created_at"_L1, r.createdAt.toString(Qt::ISODate));
        q.bindValue(":updated_at"_L1, r.updatedAt.toString(Qt::ISODate));
        q.bindValue(":word"_L1, r.word);

        if (q.exec() && q.numRowsAffected() > 0)
        {
            updatedIds.append(r.id);
        }
    }

    // Invalidate cache for updated entities
    if (!updatedIds.isEmpty())
    {
        DictWordCache::instance().invalidateEntities(updatedIds);
        DictWordCache::instance().invalidateRelationships(updatedIds);
    }

    // Re-read entities to populate relationship fields from junction tables
    return findMany(updatedIds);
}

QList<SCE::DictWord> SCDDictWord::DictWordTable::updateWithRelationshipsMany(const QList<SCE::DictWord> &dictWords)
{
    QList<int> updatedIds;
    updatedIds.reserve(dictWords.size());

    QSqlDatabase db = m_dbSubContext.getConnection();

    // Build SET clause once
    const QString sqlString = QStringLiteral("UPDATE dict_word SET "
                                             "id = :id"
                                             ",created_at = :created_at"
                                             ",updated_at = :updated_at"
                                             ",word = :word"
                                             " WHERE id = :id");

    QSqlQuery q(db);

    for (const SCE::DictWord &r : dictWords)
    {
        q.prepare(sqlString);
        q.bindValue(":id"_L1, r.id);
        q.bindValue(":created_at"_L1, r.createdAt.toString(Qt::ISODate));
        q.bindValue(":updated_at"_L1, r.updatedAt.toString(Qt::ISODate));
        q.bindValue(":word"_L1, r.word);

        if (q.exec() && q.numRowsAffected() > 0)
        {
            // Handle junction table relationships

            updatedIds.append(r.id);
        }
    }

    // Invalidate cache for updated entities
    if (!updatedIds.isEmpty())
    {
        DictWordCache::instance().invalidateEntities(updatedIds);
        DictWordCache::instance().invalidateRelationships(updatedIds);
    }

    // Re-read entities to populate relationship fields from junction tables
    return findMany(updatedIds);
}

QList<SCE::DictWord> SCDDictWord::DictWordTable::findMany(const QList<int> &ids) const
{
    QList<SCE::DictWord> result;
    result.reserve(ids.size());

    if (ids.isEmpty())
        return result;

    // Try cache first
    if (DictWordCache::instance().getCachedEntities(ids, result))
        return result;

    QSqlDatabase db = m_dbSubContext.getConnection();

    // Build a dynamic IN clause
    QStringList inPlaceholders;
    inPlaceholders.fill("?"_L1, ids.size());
    const QString sql =
        QStringLiteral("SELECT id,%1 FROM dict_word WHERE id IN (%2)").arg(COLUMNS_NO_ID, inPlaceholders.join(","_L1));

    QSqlQuery q(db);
    q.prepare(sql);
    for (int id : ids)
        q.addBindValue(id);

    if (q.exec())
    {
        QList<int> foundIds;
        while (q.next())
        {
            const int rowId = q.value(0).toInt();
            foundIds.append(rowId);

            SCE::DictWord dictWord;
            dictWord.id = rowId;
            dictWord.createdAt = QDateTime::fromString(q.value(1).toString(), Qt::ISODate);
            dictWord.updatedAt = QDateTime::fromString(q.value(2).toString(), Qt::ISODate);
            dictWord.word = q.value(3).toString();

            result.append(std::move(dictWord));
        }

        // Get relationship data for all found IDs in batch

        // Populate relationships into results
        for (auto &dictWord : result)
        {
        }

        // Reorder results to match input ID order (SQL IN does not guarantee order)
        {
            QHash<int, int> idToIndex;
            idToIndex.reserve(ids.size());
            for (int i = 0; i < ids.size(); ++i)
                idToIndex[ids[i]] = i;
            std::sort(result.begin(), result.end(), [&idToIndex](const auto &a, const auto &b) {
                return idToIndex.value(a.id) < idToIndex.value(b.id);
            });
        }

        // Cache the result
        DictWordCache::instance().setCachedEntities(ids, result);
    }
    return result;
}

QList<SCE::DictWord> SCDDictWord::DictWordTable::findAll() const
{
    QList<SCE::DictWord> result;

    QSqlDatabase db = m_dbSubContext.getConnection();

    const QString sql = QStringLiteral("SELECT id,%1 FROM dict_word").arg(COLUMNS_NO_ID);

    QSqlQuery q(db);
    q.prepare(sql);

    if (q.exec())
    {
        QList<int> foundIds;
        while (q.next())
        {
            const int rowId = q.value(0).toInt();
            foundIds.append(rowId);

            SCE::DictWord dictWord;
            dictWord.id = rowId;
            dictWord.createdAt = QDateTime::fromString(q.value(1).toString(), Qt::ISODate);
            dictWord.updatedAt = QDateTime::fromString(q.value(2).toString(), Qt::ISODate);
            dictWord.word = q.value(3).toString();

            result.append(std::move(dictWord));
        }

        // Get relationship data for all found IDs in batch

        // Populate relationships into results
        for (auto &dictWord : result)
        {
        }
    }
    return result;
}

QList<int> SCDDictWord::DictWordTable::removeMany(const QList<int> &ids)
{
    if (ids.isEmpty())
        return {};

    QSqlDatabase db = m_dbSubContext.getConnection();

    // Clean up forward junction table relationships first

    // Clean up backward junction table relationships (including owner junctions)

    JunctionTableOps::UnorderedOneToMany::removeWithRightIdsMany(db, ids, WORK_DICT_WORDS_JUNCTION_TABLE);

    // Batch DELETE with IN clause instead of per-row
    QStringList inPlaceholders;
    inPlaceholders.fill("?"_L1, ids.size());
    const QString sql = "DELETE FROM dict_word WHERE id IN (%1)"_L1.arg(inPlaceholders.join(","_L1));

    QSqlQuery q(db);
    q.prepare(sql);
    for (int id : ids)
        q.addBindValue(id);

    QList<int> removed;
    if (q.exec() && q.numRowsAffected() > 0)
    {
        // If all rows were deleted, return all ids; otherwise fall back to per-id verification
        if (q.numRowsAffected() == ids.size())
        {
            removed = ids;
        }
        else
        {
            // Some ids may not have existed — verify which ones were actually removed.
            // Use a direct SQL SELECT instead of findMany() to avoid populating the
            // global cache with uncommitted transaction state (the DELETE above has not
            // been committed yet; if the caller later rolls back, cached data would be stale).
            QStringList selectPlaceholders;
            selectPlaceholders.fill("?"_L1, ids.size());
            const QString selectSql =
                "SELECT id FROM dict_word WHERE id IN (%1)"_L1.arg(selectPlaceholders.join(","_L1));

            QSqlQuery selectQ(db);
            selectQ.prepare(selectSql);
            for (int id : ids)
                selectQ.addBindValue(id);

            QSet<int> remainingIds;
            if (selectQ.exec())
            {
                while (selectQ.next())
                    remainingIds.insert(selectQ.value(0).toInt());
            }

            for (int id : ids)
            {
                if (!remainingIds.contains(id))
                    removed.append(id);
            }
        }
    }

    // Invalidate cache for removed entities
    if (!removed.isEmpty())
    {
        DictWordCache::instance().invalidateEntities(removed);
        DictWordCache::instance().invalidateRelationships(removed);
    }

    return removed;
}

void SCDDictWord::DictWordTable::setRelationshipIds(int dictWordId, DictWordRelationshipField relationship,
                                                    QList<int> relatedIds)
{
    QSqlDatabase db = m_dbSubContext.getConnection();

    switch (relationship)
    {
    }

    // Invalidate cache for relationship changes
    DictWordCache::instance().invalidateEntity(dictWordId);
    DictWordCache::instance().invalidateRelationships(dictWordId);
}

QHash<int, QList<int>> SCDDictWord::DictWordTable::getRelationshipIdsMany(const QList<int> &dictWordIds,
                                                                          DictWordRelationshipField relationship) const
{
    // Try cache first
    QHash<int, QList<int>> result;
    if (DictWordCache::instance().getCachedRelationshipData(dictWordIds, relationship, result))
        return result;

    QSqlDatabase db = m_dbSubContext.getConnection();

    switch (relationship)
    {

    default:
        throw std::invalid_argument("Unhandled relationship type");
    }

    // Cache the result
    DictWordCache::instance().setCachedRelationshipData(dictWordIds, relationship, result);

    return result;
}

int SCDDictWord::DictWordTable::getRelationshipIdsCount(int dictWordId, DictWordRelationshipField relationship) const
{
    QSqlDatabase db = m_dbSubContext.getConnection();

    switch (relationship)
    {

    default:
        throw std::invalid_argument("Unhandled relationship type");
    }
}

QList<int> SCDDictWord::DictWordTable::getRelationshipIdsInRange(int dictWordId, DictWordRelationshipField relationship,
                                                                 int offset, int limit) const
{
    QSqlDatabase db = m_dbSubContext.getConnection();

    switch (relationship)
    {

    default:
        throw std::invalid_argument("Unhandled relationship type");
    }
}

QList<int> SCDDictWord::DictWordTable::moveRelationshipIds(int dictWordId, DictWordRelationshipField relationship,
                                                           const QList<int> &idsToMove, int newIndex)
{
    QSqlDatabase db = m_dbSubContext.getConnection();

    switch (relationship)
    {

    default:
        throw std::invalid_argument("Unhandled relationship type");
    }
}

TableLevelSnapshot SCDDictWord::DictWordTable::snapshotRows(const QList<int> &ids)
{
    TableLevelSnapshot snap;
    QSqlDatabase db = m_dbSubContext.getConnection();

    // Capture entity rows
    snap.entityRows = SnapshotUtil::captureRowsByIds(db, "dict_word"_L1, ids);

    // Capture forward junction rows

    // Capture backward junction rows (including owner junctions)

    snap.backwardJunctions.append(SnapshotUtil::captureJunctionByRight(db, WORK_DICT_WORDS_JUNCTION_TABLE, ids));

    return snap;
}

void SCDDictWord::DictWordTable::restoreRows(const TableLevelSnapshot &snap)
{
    QSqlDatabase db = m_dbSubContext.getConnection();

    // Restore entity rows
    SnapshotUtil::restoreRows(db, snap.entityRows);

    // Restore forward junction rows
    for (const auto &jSnap : snap.forwardJunctions)
        SnapshotUtil::restoreJunctionRowsByLeft(db, jSnap);

    // Restore backward junction rows
    for (const auto &jSnap : snap.backwardJunctions)
        SnapshotUtil::restoreJunctionRowsByRight(db, jSnap);

    // Invalidate caches for restored IDs
    if (const auto restoredIds = SnapshotUtil::extractIds(snap.entityRows); !restoredIds.isEmpty())
    {
        DictWordCache::instance().invalidateEntities(restoredIds);
        DictWordCache::instance().invalidateRelationships(restoredIds);
    }

    // Invalidate JunctionCache for affected junction tables

    JunctionTableOps::JunctionCache::instance().invalidateTable(WORK_DICT_WORDS_JUNCTION_TABLE);
}

QList<int> SCDDictWord::DictWordTable::getOwnerIds(const QList<int> &childIds) const
{
    if (childIds.isEmpty())
        return {};

    QSqlDatabase db = m_dbSubContext.getConnection();
    QSet<int> ownerIdSet;

    {
        const auto map =
            JunctionTableOps::UnorderedOneToMany::getLeftIdMany(db, WORK_DICT_WORDS_JUNCTION_TABLE, childIds);
        for (auto it = map.cbegin(); it != map.cend(); ++it)
            ownerIdSet.insert(it.value());
    }

    return ownerIdSet.values();
}
