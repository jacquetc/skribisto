// Generated by Qleany v1.4.2 from table_definitions_h.tera
#pragma once
#include <QList>
#include <QString>
#include <QStringLiteral>

namespace Skribisto::Common::DirectAccess::TrashInfo
{

inline QString getSqlTableDefinition()
{
    return QStringLiteral(
        "CREATE TABLE IF NOT EXISTS trash_info ("
        "    id INTEGER PRIMARY KEY ON CONFLICT ROLLBACK AUTOINCREMENT UNIQUE ON CONFLICT ROLLBACK NOT NULL,"
        "    created_at TEXT NOT NULL,"
        "    updated_at TEXT NOT NULL,"
        "    trashed_at TEXT NOT NULL,"
        "    origin_binder_id INTEGER NOT NULL DEFAULT 0,"
        "    trashed_binder INTEGER NOT NULL DEFAULT 0,"
        "    trashed_binder_item INTEGER NOT NULL DEFAULT 0"
        ");");
}

inline QList<QString> getSqlJunctionTableDefinitions()
{
    QList<QString> definitions;

    // many-to-one junction table for trashed_binder
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS trash_info_trashed_binder_to_binder_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    // many-to-one junction table for trashed_binder_item
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS trash_info_trashed_binder_item_to_binder_item_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    return definitions;
}

} // namespace Skribisto::Common::DirectAccess::TrashInfo
