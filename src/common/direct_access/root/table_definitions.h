// Generated by Qleany v1.4.2 from table_definitions_h.tera
#pragma once
#include <QList>
#include <QString>
#include <QStringLiteral>

namespace Skribisto::Common::DirectAccess::Root
{

inline QString getSqlTableDefinition()
{
    return QStringLiteral(
        "CREATE TABLE IF NOT EXISTS root ("
        "    id INTEGER PRIMARY KEY ON CONFLICT ROLLBACK AUTOINCREMENT UNIQUE ON CONFLICT ROLLBACK NOT NULL,"
        "    created_at TEXT NOT NULL,"
        "    updated_at TEXT NOT NULL,"
        "    system INTEGER NOT NULL DEFAULT 0,"
        "    works TEXT NOT NULL DEFAULT '[]'"
        ");");
}

inline QList<QString> getSqlJunctionTableDefinitions()
{
    QList<QString> definitions;

    // one-to-one junction table for system
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS root_system_to_system_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    // unordered one-to-many junction table for works
    definitions << QStringLiteral("CREATE TABLE IF NOT EXISTS root_works_to_work_junction ("
                                  "    left_id INTEGER NOT NULL,"
                                  "    right_id INTEGER NOT NULL,"
                                  "    PRIMARY KEY (left_id, right_id)"
                                  ");");

    return definitions;
}

} // namespace Skribisto::Common::DirectAccess::Root
