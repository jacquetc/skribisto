// Generated by Qleany v1.4.2 from i_entity_repository_h.tera
#pragma once

#include "database/snapshot_types.h"
#include "entities/root.h"

#include <QList>
#include <optional>

namespace Skribisto::Common::DirectAccess::Root
{
Q_NAMESPACE

// Relationships for Root entity derived from its relational fields
enum class RootRelationshipField
{

    System,
    Works
};
Q_ENUM_NS(RootRelationshipField)

class IRootRepository
{
  public:
    virtual ~IRootRepository() = default;

    // CRUD
    virtual QList<Entities::Root> createOrphans(const QList<Entities::Root> &roots) = 0;
    virtual QList<Entities::Root> get(const QList<int> &rootIds) const = 0;
    virtual QList<Entities::Root> getAll() const = 0;
    virtual QList<Entities::Root> update(const QList<Entities::Root> &roots) = 0;
    virtual QList<int> remove(const QList<int> &rootIds) = 0;

    // Relationship setters/getters
    // Set the relationship value for a given Root id (e.g., set root id)
    virtual void setRelationshipIds(int rootId, RootRelationshipField relationship, QList<int> relatedId) = 0;

    // Get the relationship value for a given Root id (e.g., get root id)
    virtual QList<int> getRelationshipIds(int rootId, RootRelationshipField relationship) const = 0;
    virtual QHash<int, QList<int>> getRelationshipIdsMany(const QList<int> &rootIds,
                                                          RootRelationshipField relationship) const = 0;
    virtual int getRelationshipIdsCount(int rootId, RootRelationshipField relationship) const = 0;
    virtual QList<int> getRelationshipIdsInRange(int rootId, RootRelationshipField relationship, int offset,
                                                 int limit) const = 0;

    // Move specific related IDs to a new position (only meaningful for ordered relationships)
    virtual QList<int> moveRelationshipIds(int rootId, RootRelationshipField relationship, const QList<int> &idsToMove,
                                           int newIndex) = 0;

    // Snapshot / Restore
    virtual Database::EntityTreeSnapshot snapshot(const QList<int> &ids) = 0;
    virtual void restore(const Database::EntityTreeSnapshot &snap) = 0;
};

} // namespace Skribisto::Common::DirectAccess::Root
