// Generated by Qleany v1.4.2 from entity_repository_h.tera
#pragma once

#include "database/db_context.h"
#include "database/snapshot_types.h"
#include "direct_access/event_registry.h"
#include "direct_access/root/i_root_repository.h"
#include "direct_access/root/root_events.h"
#include "entities/root.h"
#include "signal_buffer.h"
#include <QPointer>

namespace Skribisto::Common::DirectAccess::Root
{
namespace SCE = Skribisto::Common::Entities;

class IRootTable
{
  public:
    virtual ~IRootTable() = default;

    // Creation assigns new ids
    virtual QList<SCE::Root> createMany(const QList<SCE::Root> &roots) = 0;

    // Update
    virtual QList<SCE::Root> updateMany(const QList<SCE::Root> &roots) = 0;

    // Query/Delete
    [[nodiscard]] virtual QList<SCE::Root> findMany(const QList<int> &ids) const = 0;
    [[nodiscard]] virtual QList<SCE::Root> findAll() const = 0;
    virtual QList<int> removeMany(const QList<int> &ids) = 0;
    // Relationship setters/getters
    // Set the relationship value for a given Root id
    virtual void setRelationshipIds(int rootId, RootRelationshipField relationship, QList<int> relatedId) = 0;

    // Get the relationship value for a given Root id
    [[nodiscard]] virtual QHash<int, QList<int>> getRelationshipIdsMany(const QList<int> &rootIds,
                                                                        RootRelationshipField relationship) const = 0;
    virtual int getRelationshipIdsCount(int rootId, RootRelationshipField relationship) const = 0;
    virtual QList<int> getRelationshipIdsInRange(int rootId, RootRelationshipField relationship, int offset,
                                                 int limit) const = 0;

    // Move specific related IDs to a new position (only meaningful for ordered relationships)
    virtual QList<int> moveRelationshipIds(int rootId, RootRelationshipField relationship, const QList<int> &idsToMove,
                                           int newIndex) = 0;

    // Snapshot / Restore (table level)
    virtual Database::TableLevelSnapshot snapshotRows(const QList<int> &ids) = 0;
    virtual void restoreRows(const Database::TableLevelSnapshot &snap) = 0;
};

class RootRepository : public IRootRepository
{
  public:
    RootRepository(std::unique_ptr<IRootTable> table, Database::DbSubContext &dbSubContext,
                   QPointer<EventRegistry> eventRegistry, QPointer<Common::SignalBuffer> signalBuffer);

    ~RootRepository() override = default;

    // CRUD
    QList<SCE::Root> createOrphans(const QList<SCE::Root> &roots) override;
    QList<SCE::Root> get(const QList<int> &rootIds) const override;
    QList<SCE::Root> getAll() const override;
    QList<SCE::Root> update(const QList<SCE::Root> &roots) override;
    QList<int> remove(const QList<int> &rootIds) override;

    // Snapshot / Restore
    Database::EntityTreeSnapshot snapshot(const QList<int> &ids) override;
    void restore(const Database::EntityTreeSnapshot &snap) override;

    // Relationships
    void setRelationshipIds(int rootId, RootRelationshipField relationship, QList<int> relatedId) override;
    QList<int> getRelationshipIds(int rootId, RootRelationshipField relationship) const override;
    QHash<int, QList<int>> getRelationshipIdsMany(const QList<int> &rootIds,
                                                  RootRelationshipField relationship) const override;
    int getRelationshipIdsCount(int rootId, RootRelationshipField relationship) const override;
    QList<int> getRelationshipIdsInRange(int rootId, RootRelationshipField relationship, int offset,
                                         int limit) const override;
    QList<int> moveRelationshipIds(int rootId, RootRelationshipField relationship, const QList<int> &idsToMove,
                                   int newIndex) override;

  private:
    std::unique_ptr<IRootTable> m_table;
    QPointer<RootEvents> m_events;                 // not owned
    QPointer<EventRegistry> m_eventRegistry;       // not owned
    QPointer<Common::SignalBuffer> m_signalBuffer; // not owned

    // For cascade operations
    Database::DbSubContext &m_dbSubContext;

    void emitCreated(const QList<int> &ids) const;
    void emitUpdated(const QList<int> &ids) const;
    void emitRemoved(const QList<int> &ids) const;
    void emitRelationshipChanged(int rootId, RootRelationshipField rel, const QList<int> &relatedIds) const;
};

} // namespace Skribisto::Common::DirectAccess::Root
