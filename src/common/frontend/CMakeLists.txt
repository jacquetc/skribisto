# Generated by Qleany v1.4.2 from common_frontend_cmake.tera
set(LIBRARY_NAME skribisto-common-frontend-bootstrap)

add_library(${LIBRARY_NAME} STATIC
    app_bootstrap.cpp
    app_bootstrap.h
)

target_link_libraries(${LIBRARY_NAME}
    PRIVATE project_options
    PUBLIC Qt6::Core Qt6::Qml
)

if (NOT SKR_BUILD_WITH_MOCKS)
    target_link_libraries(${LIBRARY_NAME}
        PRIVATE skribisto-common QCoro::Core QCoro::Qml
    )
endif()

if (SKR_BUILD_WITH_MOCKS)
    target_compile_definitions(${LIBRARY_NAME} PRIVATE SKR_BUILD_WITH_MOCKS)
    target_compile_definitions(${LIBRARY_NAME} PRIVATE QML_IMPORT_PATH="${CMAKE_SOURCE_DIR}/src/presentation/mock_imports")
endif()

target_include_directories(${LIBRARY_NAME}
    PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}
)