// Generated by Qleany v1.4.2 from entity_h.tera
#pragma once

#include <QDateTime>
#include <QString>
#include <compare>
namespace Skribisto::Common::Entities
{

/// @brief DictWord entity (C++20 aggregate).
/// Supports designated initializers: DictWord{.id = 1, .name = "..."};
struct DictWord
{
    int id = 0;
    QDateTime createdAt{};
    QDateTime updatedAt{};
    QString word{};

    // Defaulted comparison operators (C++20 three-way comparison)
    [[nodiscard]] auto operator<=>(const DictWord &) const = default;
    [[nodiscard]] bool operator==(const DictWord &) const = default;
};

// Verify DictWord remains an aggregate
static_assert(std::is_aggregate_v<DictWord>);

} // namespace Skribisto::Common::Entities
