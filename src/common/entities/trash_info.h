// Generated by Qleany v1.4.2 from entity_h.tera
#pragma once

#include <QDateTime>
#include <compare>
#include <optional>
namespace Skribisto::Common::Entities
{

/// @brief TrashInfo entity (C++20 aggregate).
/// Supports designated initializers: TrashInfo{.id = 1, .name = "..."};
struct TrashInfo
{
    int id = 0;
    QDateTime createdAt{};
    QDateTime updatedAt{};
    QDateTime trashedAt{};
    int originBinderId = 0;
    std::optional<int> trashedBinder = std::nullopt;
    std::optional<int> trashedBinderItem = std::nullopt;

    // Defaulted comparison operators (C++20 three-way comparison)
    [[nodiscard]] auto operator<=>(const TrashInfo &) const = default;
    [[nodiscard]] bool operator==(const TrashInfo &) const = default;
};

// Verify TrashInfo remains an aggregate
static_assert(std::is_aggregate_v<TrashInfo>);

} // namespace Skribisto::Common::Entities
