// Generated by Qleany v1.4.2 from entity_h.tera
#pragma once

#include <QDateTime>
#include <QList>
#include <compare>
namespace Skribisto::Common::Entities
{

/// @brief System entity (C++20 aggregate).
/// Supports designated initializers: System{.id = 1, .name = "..."};
struct System
{
    int id = 0;
    QDateTime createdAt{};
    QDateTime updatedAt{};
    QList<int> recentWorks{};
    QList<int> trashInfos{};

    // Defaulted comparison operators (C++20 three-way comparison)
    [[nodiscard]] auto operator<=>(const System &) const = default;
    [[nodiscard]] bool operator==(const System &) const = default;
};

// Verify System remains an aggregate
static_assert(std::is_aggregate_v<System>);

} // namespace Skribisto::Common::Entities
