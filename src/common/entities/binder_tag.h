// Generated by Qleany v1.4.2 from entity_h.tera
#pragma once

#include <QDateTime>
#include <QString>
#include <compare>
namespace Skribisto::Common::Entities
{

/// @brief BinderTag entity (C++20 aggregate).
/// Supports designated initializers: BinderTag{.id = 1, .name = "..."};
struct BinderTag
{
    int id = 0;
    QDateTime createdAt{};
    QDateTime updatedAt{};
    QString name{};
    QString color{};
    QString textColor{};

    // Defaulted comparison operators (C++20 three-way comparison)
    [[nodiscard]] auto operator<=>(const BinderTag &) const = default;
    [[nodiscard]] bool operator==(const BinderTag &) const = default;
};

// Verify BinderTag remains an aggregate
static_assert(std::is_aggregate_v<BinderTag>);

} // namespace Skribisto::Common::Entities
