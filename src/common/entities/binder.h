// Generated by Qleany v1.6.1 from entity_h.tera
#pragma once

#include <QDateTime>
#include <QList>
#include <QString>
#include <compare>
namespace Skribisto::Common::Entities
{

/// @brief Binder entity (C++20 aggregate).
/// Supports designated initializers: Binder{.id = 1, .name = "..."};
struct Binder
{
    int id = 0;
    QDateTime createdAt{};
    QDateTime updatedAt{};
    QString name = QString(QLatin1StringView(""));
    bool activated = false;
    QList<int> binderItems{};

    // Defaulted comparison operators (C++20 three-way comparison)
    [[nodiscard]] auto operator<=>(const Binder &) const = default;
    [[nodiscard]] bool operator==(const Binder &) const = default;
};

// Verify Binder remains an aggregate
static_assert(std::is_aggregate_v<Binder>);

} // namespace Skribisto::Common::Entities
