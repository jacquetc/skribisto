// Generated by Qleany v1.4.2 from entity_h.tera
#pragma once

#include <QDateTime>
#include <QList>
#include <compare>
namespace Skribisto::Common::Entities
{

/// @brief Root entity (C++20 aggregate).
/// Supports designated initializers: Root{.id = 1, .name = "..."};
struct Root
{
    int id = 0;
    QDateTime createdAt{};
    QDateTime updatedAt{};
    int system = 0;
    QList<int> works{};

    // Defaulted comparison operators (C++20 three-way comparison)
    [[nodiscard]] auto operator<=>(const Root &) const = default;
    [[nodiscard]] bool operator==(const Root &) const = default;
};

// Verify Root remains an aggregate
static_assert(std::is_aggregate_v<Root>);

} // namespace Skribisto::Common::Entities
