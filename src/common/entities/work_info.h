// Generated by Qleany v1.4.5 from entity_h.tera
#pragma once

#include <QDateTime>
#include <QString>
#include <compare>
#include <optional>
namespace Skribisto::Common::Entities
{

/// @brief WorkInfo entity (C++20 aggregate).
/// Supports designated initializers: WorkInfo{.id = 1, .name = "..."};
struct WorkInfo
{
    int id = 0;
    QDateTime createdAt{};
    QDateTime updatedAt{};
    std::optional<QString> fileName = std::nullopt;

    // Defaulted comparison operators (C++20 three-way comparison)
    [[nodiscard]] auto operator<=>(const WorkInfo &) const = default;
    [[nodiscard]] bool operator==(const WorkInfo &) const = default;
};

// Verify WorkInfo remains an aggregate
static_assert(std::is_aggregate_v<WorkInfo>);

} // namespace Skribisto::Common::Entities
