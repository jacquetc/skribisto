// Generated by Qleany v1.6.1 from entity_controller_qml.tera

import QtQuick

QtObject {
    id: controller

    property int undoRedoStackId: 0
    function createOrphans(dtos) {
        for (var i = 0; i < dtos.length; i++) {
            const dto = dtos[i];
            // create random id
            dto["id"] = Math.floor(Math.random() * 1000000);
        }

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(dtos);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.rootEvents().created(dtos);
            });
        }

        return task;
    }
    function get(ids) {
        let dtos = [];
        for (var i = 0; i < ids.length; i++) {
            const id = ids[i];
            let dto = {};
            dto["id"] = id;
            dto["createdAt"] = "2026-01-01T00:00:00Z";
            dto["updatedAt"] = "2026-01-01T00:00:00Z";
            dto["system"] = 0;
            dto["works"] = [];
            dtos.push(dto);
        }
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(dtos);
            task.setDelay(50);
        }

        return task;
    }
    function getAll() {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue([]);
            task.setDelay(50);
        }

        return task;
    }
    function getCreateDto() {
        return {
            "createdAt": "2026-01-01T00:00:00Z",
            "updatedAt": "2026-01-01T00:00:00Z",
            "system": 0,
            "works": []
        };
    }
    function getUpdateDto() {
        return {
            "id": 0,
            "createdAt": "2026-01-01T00:00:00Z",
            "updatedAt": "2026-01-01T00:00:00Z"
        };
    }
    function remove(ids) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(ids);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.rootEvents().removed(ids);
            });
        }

        return task;
    }
    function update(dtos) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(dtos);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.rootEvents().updated(dtos);
            });
        }

        return task;
    }
    function updateWithRelationships(dtos) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(dtos);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.rootEvents().updated(dtos);
            });
        }

        return task;
    }

    function getRelationshipIds(entityId, relationship) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue([]);
            task.setDelay(50);
        }
        return task;
    }
    function setRelationshipIds(entityId, relationship, relatedIds) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(undefined);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.rootEvents().relationshipChanged(entityId, relationship, relatedIds);
            });
        }
        return task;
    }
    function getRelationshipIdsMany(entityIds, relationship) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue({});
            task.setDelay(50);
        }
        return task;
    }
    function getRelationshipIdsCount(entityId, relationship) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(0);
            task.setDelay(50);
        }
        return task;
    }
    function getRelationshipIdsInRange(entityId, relationship, offset, limit) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue([]);
            task.setDelay(50);
        }
        return task;
    }
    function moveRelationshipIds(entityId, relationship, idsToMove, newIndex) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue([]);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.rootEvents().relationshipChanged(entityId, relationship, idsToMove);
            });
        }
        return task;
    }
}
