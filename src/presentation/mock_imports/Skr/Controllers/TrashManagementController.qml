// Generated by Qleany v1.4.2 from feature_controller_qml.tera

import QtQuick

QtObject {
    id: controller

    property int undoRedoStackId: 0

    function getTrashBinderItemsDto() {
        return {
            "binderItemIds": [],
            "originBinderId": 0
        };
    }

    function trashBinderItems(trashBinderItemsDto) {
        let return_dto = true;

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(return_dto);
            task.setDelay(50);
        }

        return task;
    }

    function getTrashBinderDto() {
        return {
            "binderId": 0
        };
    }

    function trashBinder(trashBinderDto) {
        let return_dto = true;

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(return_dto);
            task.setDelay(50);
        }

        return task;
    }

    function getRestoreItemsDto() {
        return {
            "trashInfoIds": []
        };
    }

    function restoreItems(restoreItemsDto) {
        let return_dto = {};

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(return_dto);
            task.setDelay(50);
        }

        return task;
    }

    function emptyTrash() {
        let return_dto = true;

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(return_dto);
            task.setDelay(50);
        }

        return task;
    }
}
