// Generated by Qleany v1.4.6 from feature_controller_qml.tera

import QtQuick

QtObject {
    id: controller

    property int undoRedoStackId: 0

    function getLoadWorkDto() {
        return {
            "fileName": ""
        };
    }

    function loadWork(loadWorkDto) {
        let return_dto = true;

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(return_dto);
            task.setDelay(50);
        }

        return task;
    }

    function getSaveWorkDto() {
        return {
            "fileName": "",
            "overwrite": false
        };
    }

    function saveWork(saveWorkDto) {
        let return_dto = true;

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(return_dto);
            task.setDelay(50);
        }

        return task;
    }

    function closeWork() {
        let return_dto = true;

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(return_dto);
            task.setDelay(50);
        }

        return task;
    }

    function getNewWorkDto() {
        return {
            "fileName": ""
        };
    }

    function newWork(newWorkDto) {
        let return_dto = true;

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(return_dto);
            task.setDelay(50);
        }

        return task;
    }
}
