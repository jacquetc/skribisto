// Generated by Qleany v1.4.2 from entity_controller_qml.tera

import QtQuick

QtObject {
    id: controller

    property int undoRedoStackId: 0
    function create(dtos, ownerId, index) {
        for (var i = 0; i < dtos.length; i++) {
            const dto = dtos[i];
            // create random id
            dto["id"] = Math.floor(Math.random() * 1000000);
        }

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(dtos);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.binderItemEvents().created(dtos);
            });
        }

        return task;
    }
    function createOrphans(dtos) {
        for (var i = 0; i < dtos.length; i++) {
            const dto = dtos[i];
            // create random id
            dto["id"] = Math.floor(Math.random() * 1000000);
        }

        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(dtos);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.binderItemEvents().created(dtos);
            });
        }

        return task;
    }
    function get(ids) {
        let dtos = [];
        for (var i = 0; i < ids.length; i++) {
            const id = ids[i];
            let dto = {};
            dto["id"] = id;
            dto["createdAt"] = "2026-01-01T00:00:00Z";
            dto["updatedAt"] = "2026-01-01T00:00:00Z";
            dto["title"] = "";
            dto["subTitle"] = "";
            dto["role"] = "";
            dto["subRole"] = "";
            dto["label"] = "";
            dto["activated"] = false;
            dto["isFavorite"] = false;
            dto["isPrintable"] = false;
            dto["indent"] = 0;
            dto["wordCountGoal"] = 0;
            dto["charCountGoal"] = 0;
            dto["dictLanguage"] = "";
            dto["contents"] = [];
            dto["references"] = [];
            dto["tags"] = [];
            dtos.push(dto);
        }
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(dtos);
            task.setDelay(50);
        }

        return task;
    }
    function getAll() {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue([]);
            task.setDelay(50);
        }

        return task;
    }
    function getCreateDto() {
        return {
            "createdAt": "2026-01-01T00:00:00Z",
            "updatedAt": "2026-01-01T00:00:00Z",
            "title": "",
            "subTitle": "",
            "role": "",
            "subRole": "",
            "label": "",
            "activated": false,
            "isFavorite": false,
            "isPrintable": false,
            "indent": 0,
            "wordCountGoal": 0,
            "charCountGoal": 0,
            "dictLanguage": "",
            "contents": [],
            "references": [],
            "tags": []
        };
    }
    function remove(ids) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(ids);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.binderItemEvents().removed(ids);
            });
        }

        return task;
    }
    function update(dtos) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(dtos);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.binderItemEvents().updated(dtos);
            });
        }

        return task;
    }

    function getRelationshipIds(entityId, relationship) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue([]);
            task.setDelay(50);
        }
        return task;
    }
    function setRelationshipIds(entityId, relationship, relatedIds) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(undefined);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.binderItemEvents().relationshipChanged(entityId, relationship, relatedIds);
            });
        }
        return task;
    }
    function getRelationshipIdsMany(entityIds, relationship) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue({});
            task.setDelay(50);
        }
        return task;
    }
    function getRelationshipIdsCount(entityId, relationship) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue(0);
            task.setDelay(50);
        }
        return task;
    }
    function getRelationshipIdsInRange(entityId, relationship, offset, limit) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue([]);
            task.setDelay(50);
        }
        return task;
    }
    function moveRelationshipIds(entityId, relationship, idsToMove, newIndex) {
        // mocking QCoro::Task
        let task;
        var component = Qt.createComponent("QCoroQmlTask.qml");
        if (component.status === Component.Ready) {
            task = component.createObject(controller);
            task.setValue([]);
            task.setDelay(50);
            task.setSignalFn(function () {
                EventRegistry.binderItemEvents().relationshipChanged(entityId, relationship, idsToMove);
            });
        }
        return task;
    }
}
