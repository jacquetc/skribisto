// Generated by Qleany v1.4.2 from foreign_event_registry_h.tera
#pragma once

#include "direct_access/event_registry.h"
#include "service_locator.h"
#include <QQmlEngine>

struct ForeignEventRegistry : public QObject
{
    Q_OBJECT
    QML_SINGLETON
    QML_NAMED_ELEMENT(EventRegistry)

  public:
    explicit ForeignEventRegistry(QObject *parent = nullptr)
        : QObject(parent), m_registry(Skribisto::Common::ServiceLocator::instance()->eventRegistry())
    {
        connect(m_registry, &Skribisto::Common::DirectAccess::EventRegistry::reset, this, &ForeignEventRegistry::reset);
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::Root::RootEvents *rootEvents() const
    {
        return m_registry->rootEvents();
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::System::SystemEvents *systemEvents() const
    {
        return m_registry->systemEvents();
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::RecentWork::RecentWorkEvents *recentWorkEvents() const
    {
        return m_registry->recentWorkEvents();
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::Work::WorkEvents *workEvents() const
    {
        return m_registry->workEvents();
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::TrashInfo::TrashInfoEvents *trashInfoEvents() const
    {
        return m_registry->trashInfoEvents();
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::Binder::BinderEvents *binderEvents() const
    {
        return m_registry->binderEvents();
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::BinderItem::BinderItemEvents *binderItemEvents() const
    {
        return m_registry->binderItemEvents();
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::BinderTag::BinderTagEvents *binderTagEvents() const
    {
        return m_registry->binderTagEvents();
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::Content::ContentEvents *contentEvents() const
    {
        return m_registry->contentEvents();
    }
    Q_INVOKABLE Skribisto::Common::DirectAccess::DictWord::DictWordEvents *dictWordEvents() const
    {
        return m_registry->dictWordEvents();
    }

  Q_SIGNALS:
    void reset();

  private:
    Skribisto::Common::DirectAccess::EventRegistry *m_registry;
};