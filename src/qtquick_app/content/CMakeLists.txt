# Generated by Qleany v1.4.2 from qt_quick_content_cmake.tera
if (NOT SKR_BUILD_WITH_MOCKS)
    find_package(QCoro6 REQUIRED COMPONENTS Core Qml)
endif ()

qt_add_library(content STATIC)
qt6_add_qml_module(content
        URI "content"
        VERSION 1.0
        RESOURCE_PREFIX "/qt/qml"
        QML_FILES
        App.qml
)

target_link_libraries(content PRIVATE skr skr-desktop skr-mobile)
if (NOT SKR_BUILD_WITH_MOCKS)
    target_link_libraries(content PRIVATE QCoro::Core QCoro::Qml)
endif ()
