// Generated by Qleany v1.4.2 from feature_controller_h.tera
#pragma once

#include "database/db_context.h"
#include "direct_access/event_registry.h"
#include "features/feature_event_registry.h"
#include "trash_management_dtos.h"
#include "undo_redo/undo_redo_system.h"

#include <QCoro/QCoroTask>

#include <QPointer>

#include <optional>

namespace Skribisto::TrashManagement
{
namespace SCDatabase = Skribisto::Common::Database;

class TrashManagementController : public QObject
{
    Q_OBJECT
  public:
    TrashManagementController(const TrashManagementController &) = delete;
    TrashManagementController &operator=(const TrashManagementController &) = delete;
    TrashManagementController(TrashManagementController &&) = delete;
    TrashManagementController &operator=(TrashManagementController &&) = delete;
    explicit TrashManagementController(QObject *parent, int undoRedoStackId = 0);

    void setUndoRedoStackId(int undoRedoStackId);
    int undoRedoStackId() const;

    static TrashBinderItemsDto getTrashBinderItemsDto()
    {
        return {};
    }
    QCoro::Task<bool> trashBinderItems(const TrashBinderItemsDto &trashBinderItemsDto);

    static TrashBinderDto getTrashBinderDto()
    {
        return {};
    }
    QCoro::Task<bool> trashBinder(const TrashBinderDto &trashBinderDto);

    static RestoreItemsDto getRestoreItemsDto()
    {
        return {};
    }
    QCoro::Task<RestoreResultDto> restoreItems(const RestoreItemsDto &restoreItemsDto);

    QCoro::Task<bool> emptyTrash();

  private:
    void resolveDependencies();
    QPointer<SCDatabase::DbContext> m_dbContext;
    QPointer<Common::DirectAccess::EventRegistry> m_eventRegistry;
    QPointer<Common::Features::FeatureEventRegistry> m_featureEventRegistry;
    QPointer<Common::UndoRedo::UndoRedoSystem> m_undoRedoSystem;
    int m_undoRedoStackId = 0;
};
} // namespace Skribisto::TrashManagement
