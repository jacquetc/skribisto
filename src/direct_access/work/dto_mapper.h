// Generated by Qleany v1.4.2 from dto_mapper_h.tera
#pragma once

#include "direct_access/mapper_tools.h"
#include "direct_access/work/i_work_repository.h"
#include "entities/work.h"
#include "work/dtos.h"

#include <QList>
#include <algorithm>
#include <iterator>

namespace Skribisto::DirectAccess::Work
{
namespace SCE = Skribisto::Common::Entities;
namespace SCDWork = Skribisto::Common::DirectAccess::Work;
namespace CD = Common::DirectAccess;

/// @brief Static-only mapper between Work entities and DTOs.
class DtoMapper final
{
  public:
    DtoMapper() = delete;

    static SCE::Work toEntity(const CreateWorkDto &dto)
    {
        return SCE::Work{
            .id = 0,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .title = dto.title,
            .authorName = dto.authorName,
            .dictLanguage = dto.dictLanguage,
            .binders = dto.binders,
            .tags = dto.tags,
            .dictWords = dto.dictWords,
        };
    }

    static SCE::Work toEntity(const WorkDto &dto)
    {
        return SCE::Work{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .title = dto.title,
            .authorName = dto.authorName,
            .dictLanguage = dto.dictLanguage,
            .binders = dto.binders,
            .tags = dto.tags,
            .dictWords = dto.dictWords,
        };
    }

    static WorkDto toDto(const SCE::Work &entity)
    {
        return WorkDto{
            .id = entity.id,
            .createdAt = entity.createdAt,
            .updatedAt = entity.updatedAt,
            .title = entity.title,
            .authorName = entity.authorName,
            .dictLanguage = entity.dictLanguage,
            .binders = entity.binders,
            .tags = entity.tags,
            .dictWords = entity.dictWords,
        };
    }

    static QList<SCE::Work> toEntityList(const QList<CreateWorkDto> &dtos)
    {
        QList<SCE::Work> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const CreateWorkDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::Work> toEntityList(const QList<WorkDto> &dtos)
    {
        QList<SCE::Work> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities), [](const WorkDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<WorkDto> toDtoList(const QList<SCE::Work> &entities)
    {
        QList<WorkDto> dtos;
        dtos.reserve(entities.size());
        std::ranges::transform(entities, std::back_inserter(dtos),
                               [](const SCE::Work &entity) { return toDto(entity); });
        return dtos;
    }

    static SCDWork::WorkRelationshipField toCommonRelationshipField(WorkRelationshipField field)
    {
        switch (field)
        {

        case WorkRelationshipField::Binders:
            return SCDWork::WorkRelationshipField::Binders;
        case WorkRelationshipField::DictWords:
            return SCDWork::WorkRelationshipField::DictWords;
        case WorkRelationshipField::Tags:
            return SCDWork::WorkRelationshipField::Tags;
        }

        return SCDWork::WorkRelationshipField::Binders; // fallback
    }
};
} // namespace Skribisto::DirectAccess::Work
