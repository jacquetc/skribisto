// Generated by Qleany v1.4.2 from dto_mapper_h.tera
#pragma once

#include "dict_word/dtos.h"
#include "direct_access/dict_word/i_dict_word_repository.h"
#include "direct_access/mapper_tools.h"
#include "entities/dict_word.h"

#include <QList>
#include <algorithm>
#include <iterator>

namespace Skribisto::DirectAccess::DictWord
{
namespace SCE = Skribisto::Common::Entities;
namespace SCDDictWord = Skribisto::Common::DirectAccess::DictWord;
namespace CD = Common::DirectAccess;

/// @brief Static-only mapper between DictWord entities and DTOs.
class DtoMapper final
{
  public:
    DtoMapper() = delete;

    static SCE::DictWord toEntity(const CreateDictWordDto &dto)
    {
        return SCE::DictWord{
            .id = 0,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .word = dto.word,
        };
    }

    static SCE::DictWord toEntity(const DictWordDto &dto)
    {
        return SCE::DictWord{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .word = dto.word,
        };
    }

    static DictWordDto toDto(const SCE::DictWord &entity)
    {
        return DictWordDto{
            .id = entity.id,
            .createdAt = entity.createdAt,
            .updatedAt = entity.updatedAt,
            .word = entity.word,
        };
    }

    static QList<SCE::DictWord> toEntityList(const QList<CreateDictWordDto> &dtos)
    {
        QList<SCE::DictWord> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const CreateDictWordDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::DictWord> toEntityList(const QList<DictWordDto> &dtos)
    {
        QList<SCE::DictWord> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const DictWordDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<DictWordDto> toDtoList(const QList<SCE::DictWord> &entities)
    {
        QList<DictWordDto> dtos;
        dtos.reserve(entities.size());
        std::ranges::transform(entities, std::back_inserter(dtos),
                               [](const SCE::DictWord &entity) { return toDto(entity); });
        return dtos;
    }
};
} // namespace Skribisto::DirectAccess::DictWord
