// Generated by Qleany v1.6.1 from dto_mapper_h.tera
#pragma once

#include "direct_access/mapper_tools.h"
#include "direct_access/trash_info/i_trash_info_repository.h"
#include "entities/trash_info.h"
#include "trash_info/dtos.h"

#include <QList>
#include <algorithm>
#include <iterator>

namespace Skribisto::DirectAccess::TrashInfo
{
namespace SCE = Skribisto::Common::Entities;
namespace SCDTrashInfo = Skribisto::Common::DirectAccess::TrashInfo;
namespace CD = Common::DirectAccess;

/// @brief Static-only mapper between TrashInfo entities and DTOs.
class DtoMapper final
{
  public:
    DtoMapper() = delete;

    static SCE::TrashInfo toEntity(const CreateTrashInfoDto &dto)
    {
        return SCE::TrashInfo{
            .id = 0,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .trashedAt = dto.trashedAt,
            .originBinderId = dto.originBinderId,
            .trashedBinder = dto.trashedBinder,
            .trashedBinderItem = dto.trashedBinderItem,
        };
    }

    static SCE::TrashInfo toEntity(const TrashInfoDto &dto)
    {
        return SCE::TrashInfo{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .trashedAt = dto.trashedAt,
            .originBinderId = dto.originBinderId,
            .trashedBinder = dto.trashedBinder,
            .trashedBinderItem = dto.trashedBinderItem,
        };
    }

    static TrashInfoDto toDto(const SCE::TrashInfo &entity)
    {
        return TrashInfoDto{
            .id = entity.id,
            .createdAt = entity.createdAt,
            .updatedAt = entity.updatedAt,
            .trashedAt = entity.trashedAt,
            .originBinderId = entity.originBinderId,
            .trashedBinder = entity.trashedBinder,
            .trashedBinderItem = entity.trashedBinderItem,
        };
    }

    static SCE::TrashInfo toEntity(const UpdateTrashInfoDto &dto)
    {
        return SCE::TrashInfo{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .trashedAt = dto.trashedAt,
            .originBinderId = dto.originBinderId,
        };
    }

    static QList<SCE::TrashInfo> toEntityList(const QList<UpdateTrashInfoDto> &dtos)
    {
        QList<SCE::TrashInfo> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const UpdateTrashInfoDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::TrashInfo> toEntityList(const QList<CreateTrashInfoDto> &dtos)
    {
        QList<SCE::TrashInfo> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const CreateTrashInfoDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::TrashInfo> toEntityList(const QList<TrashInfoDto> &dtos)
    {
        QList<SCE::TrashInfo> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const TrashInfoDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<TrashInfoDto> toDtoList(const QList<SCE::TrashInfo> &entities)
    {
        QList<TrashInfoDto> dtos;
        dtos.reserve(entities.size());
        std::ranges::transform(entities, std::back_inserter(dtos),
                               [](const SCE::TrashInfo &entity) { return toDto(entity); });
        return dtos;
    }

    static SCDTrashInfo::TrashInfoRelationshipField toCommonRelationshipField(TrashInfoRelationshipField field)
    {
        switch (field)
        {

        case TrashInfoRelationshipField::TrashedBinder:
            return SCDTrashInfo::TrashInfoRelationshipField::TrashedBinder;
        case TrashInfoRelationshipField::TrashedBinderItem:
            return SCDTrashInfo::TrashInfoRelationshipField::TrashedBinderItem;
        }

        return SCDTrashInfo::TrashInfoRelationshipField::TrashedBinder; // fallback
    }
};
} // namespace Skribisto::DirectAccess::TrashInfo
