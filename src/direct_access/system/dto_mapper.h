// Generated by Qleany v1.4.6 from dto_mapper_h.tera
#pragma once

#include "direct_access/mapper_tools.h"
#include "direct_access/system/i_system_repository.h"
#include "entities/system.h"
#include "system/dtos.h"

#include <QList>
#include <algorithm>
#include <iterator>

namespace Skribisto::DirectAccess::System
{
namespace SCE = Skribisto::Common::Entities;
namespace SCDSystem = Skribisto::Common::DirectAccess::System;
namespace CD = Common::DirectAccess;

/// @brief Static-only mapper between System entities and DTOs.
class DtoMapper final
{
  public:
    DtoMapper() = delete;

    static SCE::System toEntity(const CreateSystemDto &dto)
    {
        return SCE::System{
            .id = 0,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .recentWorks = dto.recentWorks,
            .trashInfos = dto.trashInfos,
            .workInfo = dto.workInfo,
        };
    }

    static SCE::System toEntity(const SystemDto &dto)
    {
        return SCE::System{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .recentWorks = dto.recentWorks,
            .trashInfos = dto.trashInfos,
            .workInfo = dto.workInfo,
        };
    }

    static SystemDto toDto(const SCE::System &entity)
    {
        return SystemDto{
            .id = entity.id,
            .createdAt = entity.createdAt,
            .updatedAt = entity.updatedAt,
            .recentWorks = entity.recentWorks,
            .trashInfos = entity.trashInfos,
            .workInfo = entity.workInfo,
        };
    }

    static QList<SCE::System> toEntityList(const QList<CreateSystemDto> &dtos)
    {
        QList<SCE::System> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const CreateSystemDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::System> toEntityList(const QList<SystemDto> &dtos)
    {
        QList<SCE::System> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities), [](const SystemDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SystemDto> toDtoList(const QList<SCE::System> &entities)
    {
        QList<SystemDto> dtos;
        dtos.reserve(entities.size());
        std::ranges::transform(entities, std::back_inserter(dtos),
                               [](const SCE::System &entity) { return toDto(entity); });
        return dtos;
    }

    static SCDSystem::SystemRelationshipField toCommonRelationshipField(SystemRelationshipField field)
    {
        switch (field)
        {

        case SystemRelationshipField::RecentWorks:
            return SCDSystem::SystemRelationshipField::RecentWorks;
        case SystemRelationshipField::TrashInfos:
            return SCDSystem::SystemRelationshipField::TrashInfos;
        case SystemRelationshipField::WorkInfo:
            return SCDSystem::SystemRelationshipField::WorkInfo;
        }

        return SCDSystem::SystemRelationshipField::RecentWorks; // fallback
    }
};
} // namespace Skribisto::DirectAccess::System
