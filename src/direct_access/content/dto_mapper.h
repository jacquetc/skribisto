// Generated by Qleany v1.6.1 from dto_mapper_h.tera
#pragma once

#include "content/dtos.h"
#include "direct_access/content/i_content_repository.h"
#include "direct_access/mapper_tools.h"
#include "entities/content.h"

#include <QList>
#include <algorithm>
#include <iterator>

namespace Skribisto::DirectAccess::Content
{
namespace SCE = Skribisto::Common::Entities;
namespace SCDContent = Skribisto::Common::DirectAccess::Content;
namespace CD = Common::DirectAccess;

/// @brief Static-only mapper between Content entities and DTOs.
class DtoMapper final
{
  public:
    DtoMapper() = delete;

    static SCE::Content toEntity(const CreateContentDto &dto)
    {
        return SCE::Content{
            .id = 0,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .activated = dto.activated,
            .role = dto.role,
            .data = dto.data,
        };
    }

    static SCE::Content toEntity(const ContentDto &dto)
    {
        return SCE::Content{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .activated = dto.activated,
            .role = dto.role,
            .data = dto.data,
        };
    }

    static ContentDto toDto(const SCE::Content &entity)
    {
        return ContentDto{
            .id = entity.id,
            .createdAt = entity.createdAt,
            .updatedAt = entity.updatedAt,
            .activated = entity.activated,
            .role = entity.role,
            .data = entity.data,
        };
    }

    static SCE::Content toEntity(const UpdateContentDto &dto)
    {
        return SCE::Content{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .activated = dto.activated,
            .role = dto.role,
            .data = dto.data,
        };
    }

    static QList<SCE::Content> toEntityList(const QList<UpdateContentDto> &dtos)
    {
        QList<SCE::Content> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const UpdateContentDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::Content> toEntityList(const QList<CreateContentDto> &dtos)
    {
        QList<SCE::Content> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const CreateContentDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::Content> toEntityList(const QList<ContentDto> &dtos)
    {
        QList<SCE::Content> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities), [](const ContentDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<ContentDto> toDtoList(const QList<SCE::Content> &entities)
    {
        QList<ContentDto> dtos;
        dtos.reserve(entities.size());
        std::ranges::transform(entities, std::back_inserter(dtos),
                               [](const SCE::Content &entity) { return toDto(entity); });
        return dtos;
    }
};
} // namespace Skribisto::DirectAccess::Content
