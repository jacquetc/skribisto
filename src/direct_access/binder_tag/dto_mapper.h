// Generated by Qleany v1.4.2 from dto_mapper_h.tera
#pragma once

#include "binder_tag/dtos.h"
#include "direct_access/binder_tag/i_binder_tag_repository.h"
#include "direct_access/mapper_tools.h"
#include "entities/binder_tag.h"

#include <QList>
#include <algorithm>
#include <iterator>

namespace Skribisto::DirectAccess::BinderTag
{
namespace SCE = Skribisto::Common::Entities;
namespace SCDBinderTag = Skribisto::Common::DirectAccess::BinderTag;
namespace CD = Common::DirectAccess;

/// @brief Static-only mapper between BinderTag entities and DTOs.
class DtoMapper final
{
  public:
    DtoMapper() = delete;

    static SCE::BinderTag toEntity(const CreateBinderTagDto &dto)
    {
        return SCE::BinderTag{
            .id = 0,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .name = dto.name,
            .color = dto.color,
            .textColor = dto.textColor,
        };
    }

    static SCE::BinderTag toEntity(const BinderTagDto &dto)
    {
        return SCE::BinderTag{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .name = dto.name,
            .color = dto.color,
            .textColor = dto.textColor,
        };
    }

    static BinderTagDto toDto(const SCE::BinderTag &entity)
    {
        return BinderTagDto{
            .id = entity.id,
            .createdAt = entity.createdAt,
            .updatedAt = entity.updatedAt,
            .name = entity.name,
            .color = entity.color,
            .textColor = entity.textColor,
        };
    }

    static QList<SCE::BinderTag> toEntityList(const QList<CreateBinderTagDto> &dtos)
    {
        QList<SCE::BinderTag> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const CreateBinderTagDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::BinderTag> toEntityList(const QList<BinderTagDto> &dtos)
    {
        QList<SCE::BinderTag> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const BinderTagDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<BinderTagDto> toDtoList(const QList<SCE::BinderTag> &entities)
    {
        QList<BinderTagDto> dtos;
        dtos.reserve(entities.size());
        std::ranges::transform(entities, std::back_inserter(dtos),
                               [](const SCE::BinderTag &entity) { return toDto(entity); });
        return dtos;
    }
};
} // namespace Skribisto::DirectAccess::BinderTag
