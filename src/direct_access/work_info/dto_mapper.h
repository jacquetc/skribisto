// Generated by Qleany v1.4.5 from dto_mapper_h.tera
#pragma once

#include "direct_access/mapper_tools.h"
#include "direct_access/work_info/i_work_info_repository.h"
#include "entities/work_info.h"
#include "work_info/dtos.h"

#include <QList>
#include <algorithm>
#include <iterator>

namespace Skribisto::DirectAccess::WorkInfo
{
namespace SCE = Skribisto::Common::Entities;
namespace SCDWorkInfo = Skribisto::Common::DirectAccess::WorkInfo;
namespace CD = Common::DirectAccess;

/// @brief Static-only mapper between WorkInfo entities and DTOs.
class DtoMapper final
{
  public:
    DtoMapper() = delete;

    static SCE::WorkInfo toEntity(const CreateWorkInfoDto &dto)
    {
        return SCE::WorkInfo{
            .id = 0,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .fileName = dto.fileName,
        };
    }

    static SCE::WorkInfo toEntity(const WorkInfoDto &dto)
    {
        return SCE::WorkInfo{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .fileName = dto.fileName,
        };
    }

    static WorkInfoDto toDto(const SCE::WorkInfo &entity)
    {
        return WorkInfoDto{
            .id = entity.id,
            .createdAt = entity.createdAt,
            .updatedAt = entity.updatedAt,
            .fileName = entity.fileName,
        };
    }

    static QList<SCE::WorkInfo> toEntityList(const QList<CreateWorkInfoDto> &dtos)
    {
        QList<SCE::WorkInfo> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const CreateWorkInfoDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::WorkInfo> toEntityList(const QList<WorkInfoDto> &dtos)
    {
        QList<SCE::WorkInfo> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const WorkInfoDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<WorkInfoDto> toDtoList(const QList<SCE::WorkInfo> &entities)
    {
        QList<WorkInfoDto> dtos;
        dtos.reserve(entities.size());
        std::ranges::transform(entities, std::back_inserter(dtos),
                               [](const SCE::WorkInfo &entity) { return toDto(entity); });
        return dtos;
    }
};
} // namespace Skribisto::DirectAccess::WorkInfo
