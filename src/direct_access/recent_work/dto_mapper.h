// Generated by Qleany v1.4.2 from dto_mapper_h.tera
#pragma once

#include "direct_access/mapper_tools.h"
#include "direct_access/recent_work/i_recent_work_repository.h"
#include "entities/recent_work.h"
#include "recent_work/dtos.h"

#include <QList>
#include <algorithm>
#include <iterator>

namespace Skribisto::DirectAccess::RecentWork
{
namespace SCE = Skribisto::Common::Entities;
namespace SCDRecentWork = Skribisto::Common::DirectAccess::RecentWork;
namespace CD = Common::DirectAccess;

/// @brief Static-only mapper between RecentWork entities and DTOs.
class DtoMapper final
{
  public:
    DtoMapper() = delete;

    static SCE::RecentWork toEntity(const CreateRecentWorkDto &dto)
    {
        return SCE::RecentWork{
            .id = 0,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .title = dto.title,
            .lastOpenedAt = dto.lastOpenedAt,
            .absolutePath = dto.absolutePath,
        };
    }

    static SCE::RecentWork toEntity(const RecentWorkDto &dto)
    {
        return SCE::RecentWork{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .title = dto.title,
            .lastOpenedAt = dto.lastOpenedAt,
            .absolutePath = dto.absolutePath,
        };
    }

    static RecentWorkDto toDto(const SCE::RecentWork &entity)
    {
        return RecentWorkDto{
            .id = entity.id,
            .createdAt = entity.createdAt,
            .updatedAt = entity.updatedAt,
            .title = entity.title,
            .lastOpenedAt = entity.lastOpenedAt,
            .absolutePath = entity.absolutePath,
        };
    }

    static QList<SCE::RecentWork> toEntityList(const QList<CreateRecentWorkDto> &dtos)
    {
        QList<SCE::RecentWork> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const CreateRecentWorkDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::RecentWork> toEntityList(const QList<RecentWorkDto> &dtos)
    {
        QList<SCE::RecentWork> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const RecentWorkDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<RecentWorkDto> toDtoList(const QList<SCE::RecentWork> &entities)
    {
        QList<RecentWorkDto> dtos;
        dtos.reserve(entities.size());
        std::ranges::transform(entities, std::back_inserter(dtos),
                               [](const SCE::RecentWork &entity) { return toDto(entity); });
        return dtos;
    }
};
} // namespace Skribisto::DirectAccess::RecentWork
