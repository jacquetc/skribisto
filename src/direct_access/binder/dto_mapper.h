// Generated by Qleany v1.6.1 from dto_mapper_h.tera
#pragma once

#include "binder/dtos.h"
#include "direct_access/binder/i_binder_repository.h"
#include "direct_access/mapper_tools.h"
#include "entities/binder.h"

#include <QList>
#include <algorithm>
#include <iterator>

namespace Skribisto::DirectAccess::Binder
{
namespace SCE = Skribisto::Common::Entities;
namespace SCDBinder = Skribisto::Common::DirectAccess::Binder;
namespace CD = Common::DirectAccess;

/// @brief Static-only mapper between Binder entities and DTOs.
class DtoMapper final
{
  public:
    DtoMapper() = delete;

    static SCE::Binder toEntity(const CreateBinderDto &dto)
    {
        return SCE::Binder{
            .id = 0,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .name = dto.name,
            .activated = dto.activated,
            .binderItems = dto.binderItems,
        };
    }

    static SCE::Binder toEntity(const BinderDto &dto)
    {
        return SCE::Binder{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .name = dto.name,
            .activated = dto.activated,
            .binderItems = dto.binderItems,
        };
    }

    static BinderDto toDto(const SCE::Binder &entity)
    {
        return BinderDto{
            .id = entity.id,
            .createdAt = entity.createdAt,
            .updatedAt = entity.updatedAt,
            .name = entity.name,
            .activated = entity.activated,
            .binderItems = entity.binderItems,
        };
    }

    static SCE::Binder toEntity(const UpdateBinderDto &dto)
    {
        return SCE::Binder{
            .id = dto.id,
            .createdAt = dto.createdAt,
            .updatedAt = dto.updatedAt,
            .name = dto.name,
            .activated = dto.activated,
        };
    }

    static QList<SCE::Binder> toEntityList(const QList<UpdateBinderDto> &dtos)
    {
        QList<SCE::Binder> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const UpdateBinderDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::Binder> toEntityList(const QList<CreateBinderDto> &dtos)
    {
        QList<SCE::Binder> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities),
                               [](const CreateBinderDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<SCE::Binder> toEntityList(const QList<BinderDto> &dtos)
    {
        QList<SCE::Binder> entities;
        entities.reserve(dtos.size());
        std::ranges::transform(dtos, std::back_inserter(entities), [](const BinderDto &dto) { return toEntity(dto); });
        return entities;
    }

    static QList<BinderDto> toDtoList(const QList<SCE::Binder> &entities)
    {
        QList<BinderDto> dtos;
        dtos.reserve(entities.size());
        std::ranges::transform(entities, std::back_inserter(dtos),
                               [](const SCE::Binder &entity) { return toDto(entity); });
        return dtos;
    }

    static SCDBinder::BinderRelationshipField toCommonRelationshipField(BinderRelationshipField field)
    {
        switch (field)
        {

        case BinderRelationshipField::BinderItems:
            return SCDBinder::BinderRelationshipField::BinderItems;
        }

        return SCDBinder::BinderRelationshipField::BinderItems; // fallback
    }
};
} // namespace Skribisto::DirectAccess::Binder
