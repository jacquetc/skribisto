# Generated by Qleany v1.4.2 from root_cmake.tera

cmake_minimum_required(VERSION 3.21)

set(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake/ ${CMAKE_SOURCE_DIR}/cmake/3rdparty/ ${CMAKE_MODULE_PATH})
# git tag for version :
include(VersionFromGit)
version_from_git(
        LOG ON
        TIMESTAMP "%Y%m%d%H%M%S"
)

project(skribisto VERSION ${VERSION} LANGUAGES CXX)

set(SKR_VERSION ${VERSION})
set(SKR_VERSION_MAJOR ${VERSION_MAJOR})
set(SKR_VERSION_MINOR ${VERSION_MINOR})
set(SKR_VERSION_PATCH ${VERSION_PATCH})

add_library(project_options INTERFACE)
target_compile_definitions(project_options INTERFACE
        SKR_VERSION="${SKR_VERSION}"
        SKR_SEMANTIC_VERSION="${SEMVER}"
        SKR_GIT_TAG="${GIT_TAG}"
        SKR_VERSION_MAJOR=${SKR_VERSION_MAJOR}
        SKR_VERSION_MINOR=${SKR_VERSION_MINOR}
        SKR_VERSION_PATCH=${SKR_VERSION_PATCH}
        QT_NO_FOREACH
)

if (CMAKE_BUILD_TYPE STREQUAL "Debug")
    set(SKR_DEBUG true)
    target_compile_definitions(project_options INTERFACE SKR_DEBUG=true)
else ()
    set(SKR_DEBUG false)
    target_compile_definitions(project_options INTERFACE SKR_DEBUG=false)
endif ()

# On Windows, build internal libraries as static to avoid DLL export issues
# (Qleany-generated code does not use __declspec(dllexport) annotations)
if(WIN32)
    set(BUILD_SHARED_LIBS OFF CACHE BOOL "" FORCE)
endif()

set(CMAKE_AUTOMOC ON)
set(CMAKE_AUTOUIC ON)
set(CMAKE_AUTORCC ON)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_STANDARD 20)

# Find includes in corresponding build directories
set(CMAKE_INCLUDE_CURRENT_DIR ON)
set(QT_DEFAULT_MAJOR_VERSION 6)
set(QT_MIN_VERSION "6.7.0")

find_package(Qt6 ${QT_MIN_VERSION} COMPONENTS Core Sql Concurrent Gui Qml Quick QuickControls2 REQUIRED)
find_package(QCoro6 REQUIRED COMPONENTS Core Qml)

option(SKR_BUILD_TESTS "Build tests" OFF)
option(SKR_BUILD_WITH_MOCKS "Build with mocks" OFF)

message("------------------CMAKE_SYSTEM_NAME---------------------")
message("${CMAKE_SYSTEM_NAME}")

if (CMAKE_SYSTEM_NAME STREQUAL "Linux")
    find_package(ECM REQUIRED NO_MODULE)
    set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} ${ECM_MODULE_PATH})
    include(ECMInstallIcons)
    include(ECMOptionalAddSubdirectory)
    include(GNUInstallDirs)
    # TODO: Add a CMakeLists.txt in resources/unix/icons (or another path) and use ecm_install_icons macro
    # see example at https://github.com/jacquetc/skribisto/tree/develop/resources/unix/icons
    # add_subdirectory(resources/unix/icons)

    message("-----------ECM_MODULE_PATH----------")
    message(${ECM_MODULE_PATH})
endif (CMAKE_SYSTEM_NAME STREQUAL "Linux")

include(CMakePackageConfigHelpers)
include(cmake/InstallHelpers.cmake)

if (NOT SKR_BUILD_WITH_MOCKS)
    message("Not building with mocks")
    add_subdirectory(src/common)
    add_subdirectory(src/direct_access)
    add_subdirectory(src/work_management)
    add_subdirectory(src/export_management)
    add_subdirectory(src/trash_management)
    add_subdirectory(src/presentation)
endif (NOT SKR_BUILD_WITH_MOCKS)

add_subdirectory(src/common/frontend)
add_subdirectory(src/qtquick_app)

enable_testing()

# Subdirectories that contain CMakeLists.txt for the tests and example
if (SKR_BUILD_TESTS)
    add_subdirectory(tests)
endif (SKR_BUILD_TESTS)

if (CMAKE_SYSTEM_NAME STREQUAL "Linux")
    # TODO: this part is empty, it is for you to adapt to your needs
    # see example at https://github.com/jacquetc/skribisto/tree/develop/resources/unix
    install(PROGRAMS resources/unix/applications/skribisto.eu.skribisto.desktop DESTINATION "${CMAKE_INSTALL_DATAROOTDIR}/applications")
    install(FILES resources/unix/mime/skribisto.eu.skribisto.xml DESTINATION "${CMAKE_INSTALL_DATAROOTDIR}/mime/packages")
    install(FILES skribisto.eu.skribisto.appdata.xml DESTINATION "${CMAKE_INSTALL_DATAROOTDIR}/metainfo")
endif (CMAKE_SYSTEM_NAME STREQUAL "Linux")

