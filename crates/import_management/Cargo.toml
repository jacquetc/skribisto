# SPDX-License-Identifier: GPL-3.0-only
# SPDX-FileCopyrightText: 2026 Cyril Jacquet

# Generated by Qleany v1.8.0 from feature_cargo.tera
[package]
name = "skribisto-import-management"
description = "import_management feature for Skribisto"
authors.workspace = true
documentation.workspace = true
keywords.workspace = true
categories.workspace = true
version.workspace = true
edition.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
readme = "../../README.md"
publish = true

[lib]
name = "import_management"

[dependencies]
anyhow = { workspace = true }
common = { workspace = true }
macros = { workspace = true }
skribisto_model = { workspace = true }
# The one authority on "where does a block land relative to an anchor, and at what
# indent" — shared with trash_management, which resolves the identical question for
# restore-to-destination through the same helpers.
binder_ordering = { workspace = true }
skrib_format = { workspace = true }
# The format-agnostic ingest pipeline: block model, scanner seam, planning.
document_ingest = { workspace = true }
serde = "1.0"
serde_json = "1.0"
chrono = { workspace = true }
uuid = { workspace = true }
# Reading .plume containers (single-file zip) and their XML.
zip = { version = "8.6.0", default-features = false, features = ["deflate"] }
roxmltree = "0.21.1"

[dev-dependencies]
# The ANALYSE->APPLY integration test builds a real store through direct_access.
direct_access = { workspace = true }
skribisto_model = { workspace = true }
# The integration test builds `.plume` fixtures (zip + loose dir) in a scratch
# dir; `skrib_format` (a normal dep) reads the produced `.skrib` back.
tempfile = "3"
# M-S7 round-trip test: builds a real `.docx`/`.odt` "returning file" via
# `text-document`'s own writer (real `DocumentComment`/`CommentReply` uids), the
# same dev-only wiring `document_ingest`'s own `odt_writer_roundtrip.rs`/
# `docx_writer_roundtrip.rs` already use — dev-only so this crate's runtime
# dependency graph never grows a writer it does not otherwise need.
text-document = { workspace = true }

[lints]
workspace = true



