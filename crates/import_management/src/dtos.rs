// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.10.0 from feature_dtos.tera
//
// NOTE: two hand-corrections, re-applied after every regeneration of this file.
// Both are the same generator bugs analysis_management/src/dtos.rs and
// mention_management/src/dtos.rs already record.
//
// 1. SPDX headers are stripped by generation; re-added.
//
// 2. The import inference emitted
//    `use common::entities::{ApplyImportRow, DocumentImportRow, ImportDiagnosticRow,
//    ImportRowKind};` — none of those are entities. They are declared further down in
//    THIS file from the use cases' own `enum_values`, so importing them collides with
//    their own declarations. Nothing outside this file is needed, so the line is
//    removed rather than corrected.
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct ImportPlumeCreatorFileDto {
    pub source_path: String,
    pub output_path: String,
    pub overwrite: bool,
    pub manuscript_binder_name: String,
    pub story_bible_binder_name: String,
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct ImportPlumeCreatorFileResultDto {
    pub output_path: String,
    pub imported_items: i64,
    pub skipped_trashed: i64,
    pub warnings: Vec<String>,
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct AnalyzeDocumentImportDto {
    pub work_id: u64,
    pub binder_id: u64,
    pub source_paths: Vec<String>,
    pub start_kind: ImportRowKind,
    pub base_indent: i64,
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum ImportRowKind {
    #[default]
    Book,
    Part,
    Chapter,
    Scene,
    Note,
    NoteFolder,
    Folder,
    Paratext,
    ParatextFolder,
    EndOfBook,
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct DocumentImportPlanDto {
    pub row: DocumentImportRow,
    pub rows: DocumentImportRows,
    pub diagnostic: ImportDiagnosticRow,
    pub diagnostics: ImportDiagnosticRows,
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum DocumentImportRow {
    #[default]
    Empty,
    Found {
        indent: i64,
        kind: ImportRowKind,
        title: String,
        stripped_ordinal: String,
        djot: String,
        scene_breaks: i64,
        word_count: i64,
        origin: String,
        included: bool,
    },
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum DocumentImportRows {
    #[default]
    Empty,
    Found(Vec<DocumentImportRow>),
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum ImportDiagnosticRow {
    #[default]
    Empty,
    Reported {
        key: String,
        severity: String,
        path: String,
        detail: String,
        count: i64,
        row_index: i64,
    },
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum ImportDiagnosticRows {
    #[default]
    Empty,
    Reported(Vec<ImportDiagnosticRow>),
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct ApplyDocumentImportDto {
    pub work_id: u64,
    pub binder_id: u64,
    pub anchor_item_id: u64,
    pub row: ApplyImportRow,
    pub rows: ApplyImportRows,
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum ApplyImportRow {
    #[default]
    Empty,
    Create {
        indent: i64,
        kind: ImportRowKind,
        title: String,
        djot: String,
    },
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum ApplyImportRows {
    #[default]
    Empty,
    Create(Vec<ApplyImportRow>),
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct ApplyDocumentImportResultDto {
    pub created_ids: Vec<u64>,
}
