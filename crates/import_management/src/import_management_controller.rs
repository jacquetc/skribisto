// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.10.0 from feature_controller.tera

use crate::AnalyzeDocumentImportDto;
use crate::ApplyDocumentImportDto;
use crate::ApplyDocumentImportResultDto;
use crate::DocumentImportPlanDto;
use crate::ImportPlumeCreatorFileDto;
use crate::ImportPlumeCreatorFileResultDto;
use crate::units_of_work::analyze_document_import_uow::AnalyzeDocumentImportUnitOfWorkFactory;
use crate::units_of_work::apply_document_import_uow::ApplyDocumentImportUnitOfWorkFactory;
use crate::units_of_work::import_plume_creator_file_uow::ImportPlumeCreatorFileUnitOfWorkFactory;
use crate::use_cases::analyze_document_import_uc::AnalyzeDocumentImportUseCase;
use crate::use_cases::apply_document_import_uc::ApplyDocumentImportUseCase;
use crate::use_cases::import_plume_creator_file_uc::ImportPlumeCreatorFileUseCase;
use anyhow::Result;

use common::long_operation::{LongOperationManager, OperationProgress};
use common::undo_redo::UndoRedoManager;
use common::{database::db_context::DbContext, event::EventHub};
use std::sync::Arc;

pub fn import_plume_creator_file(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    long_operation_manager: &mut LongOperationManager,
    dto: &ImportPlumeCreatorFileDto,
) -> Result<String> {
    let uow_context = ImportPlumeCreatorFileUnitOfWorkFactory::new(db_context, event_hub);
    let uc = ImportPlumeCreatorFileUseCase::new(Box::new(uow_context), dto);
    let operation_id = long_operation_manager.start_operation(uc);
    Ok(operation_id)
}

pub fn get_import_plume_creator_file_progress(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Option<OperationProgress> {
    long_operation_manager.get_operation_progress(operation_id)
}

pub fn get_import_plume_creator_file_result(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Result<Option<ImportPlumeCreatorFileResultDto>> {
    // Get the operation result as a JSON string
    let result_json = long_operation_manager.get_operation_result(operation_id);

    // If there's no result, return None
    if result_json.is_none() {
        return Ok(None);
    }
    // Parse the JSON string into a ImportPlumeCreatorFileResultDto
    let result_dto: ImportPlumeCreatorFileResultDto = serde_json::from_str(&result_json.unwrap())?;

    Ok(Some(result_dto))
}

pub fn analyze_document_import(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    long_operation_manager: &mut LongOperationManager,
    dto: &AnalyzeDocumentImportDto,
) -> Result<String> {
    let uow_context = AnalyzeDocumentImportUnitOfWorkFactory::new(db_context, event_hub);
    let uc = AnalyzeDocumentImportUseCase::new(Box::new(uow_context), dto);
    let operation_id = long_operation_manager.start_operation(uc);
    Ok(operation_id)
}

pub fn get_analyze_document_import_progress(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Option<OperationProgress> {
    long_operation_manager.get_operation_progress(operation_id)
}

pub fn get_analyze_document_import_result(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Result<Option<DocumentImportPlanDto>> {
    // Get the operation result as a JSON string
    let result_json = long_operation_manager.get_operation_result(operation_id);

    // If there's no result, return None
    if result_json.is_none() {
        return Ok(None);
    }
    // Parse the JSON string into a DocumentImportPlanDto
    let result_dto: DocumentImportPlanDto = serde_json::from_str(&result_json.unwrap())?;

    Ok(Some(result_dto))
}

pub fn apply_document_import(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    dto: &ApplyDocumentImportDto,
) -> Result<ApplyDocumentImportResultDto> {
    let uow_context = ApplyDocumentImportUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = ApplyDocumentImportUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(return_dto)
}
