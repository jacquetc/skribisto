// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from feature_controller.tera

use crate::AnalyzeBookDto;
use crate::BookAnalysisResultDto;
use crate::units_of_work::analyze_book_uow::AnalyzeBookUnitOfWorkFactory;
use crate::use_cases::analyze_book_uc::AnalyzeBookUseCase;
use anyhow::Result;

use common::long_operation::{LongOperationManager, OperationProgress};
use common::{database::db_context::DbContext, event::EventHub};
use std::sync::Arc;

pub fn analyze_book(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    long_operation_manager: &mut LongOperationManager,
    dto: &AnalyzeBookDto,
) -> Result<String> {
    let uow_context = AnalyzeBookUnitOfWorkFactory::new(db_context, event_hub);
    let uc = AnalyzeBookUseCase::new(Box::new(uow_context), dto);
    let operation_id = long_operation_manager.start_operation(uc);
    Ok(operation_id)
}

pub fn get_analyze_book_progress(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Option<OperationProgress> {
    long_operation_manager.get_operation_progress(operation_id)
}

pub fn get_analyze_book_result(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Result<Option<BookAnalysisResultDto>> {
    // Get the operation result as a JSON string
    let result_json = long_operation_manager.get_operation_result(operation_id);

    // If there's no result, return None
    if result_json.is_none() {
        return Ok(None);
    }
    // Parse the JSON string into a BookAnalysisResultDto
    let result_dto: BookAnalysisResultDto = serde_json::from_str(&result_json.unwrap())?;

    Ok(Some(result_dto))
}
