// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from feature_dtos.tera
//
// NOTE: three hand-corrections, re-applied after every regeneration of this file. The first
// two are the same generator bugs mention_management/src/dtos.rs already records.
//
// 1. SPDX headers are stripped by generation; re-added.
//
// 2. The import inference emits
//    `use common::entities::{DriftRow, DuplicateRow, EchoRow, SceneAnalysis};` — those are
//    not entities, they are declared further down in THIS file from the use case's own
//    `enum_values`, so importing them collides with their own declarations. The real
//    dependency is `EntityId`.
//
// 3. `Eq` is derived on the four `Vec` wrapper enums (SceneAnalyses, EchoRows,
//    DuplicateRows, DriftRows) even though each wraps a row type carrying `f64`, which is
//    not `Eq`. The generator correctly omits `Eq` from the row enums themselves and then
//    does not propagate that to their wrappers. With `Eq` they do not compile.
use common::types::EntityId;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct AnalyzeBookDto {
    pub work_id: u64,
    pub scope_item_id: u64,
}
#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct BookAnalysisResultDto {
    pub scene: SceneAnalysis,
    pub scenes: SceneAnalyses,
    pub echo: EchoRow,
    pub echoes: EchoRows,
    pub duplicate: DuplicateRow,
    pub duplicates: DuplicateRows,
    pub drift: DriftRow,
    pub drifts: DriftRows,
    pub diversity: BookDiversity,
    pub total_words: u64,
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq)]
pub enum SceneAnalysis {
    #[default]
    Empty,
    Measured {
        item_id: EntityId,
        title: String,
        chapter_title: String,
        words: i64,
        sentence_mean: Option<f64>,
        sentence_stddev: Option<f64>,
        paragraph_mean: Option<f64>,
        punctuation_per_1k: f64,
        dialogue: Option<f64>,
        synopsis_words: i64,
        synopsis_coverage: Option<f64>,
    },
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq)]
pub enum SceneAnalyses {
    #[default]
    Empty,
    Measured(Vec<SceneAnalysis>),
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq)]
pub enum EchoRow {
    #[default]
    Empty,
    Found {
        item_id: EntityId,
        word: String,
        occurrences: i64,
        closest_gap: i64,
        score: f64,
        first_at: i64,
        first_len: i64,
    },
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq)]
pub enum EchoRows {
    #[default]
    Empty,
    Found(Vec<EchoRow>),
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq)]
pub enum DuplicateRow {
    #[default]
    Empty,
    Found {
        a_item_id: EntityId,
        a_title: String,
        b_item_id: EntityId,
        b_title: String,
        containment: f64,
        jaccard: f64,
        shared_shingles: i64,
    },
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq)]
pub enum DuplicateRows {
    #[default]
    Empty,
    Found(Vec<DuplicateRow>),
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq)]
pub enum DriftRow {
    #[default]
    Empty,
    Found {
        item_id: EntityId,
        title: String,
        covered: f64,
        missing: Vec<String>,
    },
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq)]
pub enum DriftRows {
    #[default]
    Empty,
    Found(Vec<DriftRow>),
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq)]
pub enum BookDiversity {
    #[default]
    Empty,
    Measured {
        words: i64,
        distinct_words: i64,
        mattr: Option<f64>,
        hdd: Option<f64>,
        reliable: bool,
    },
}
