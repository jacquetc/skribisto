// Generated by Qleany v1.7.4 from feature_use_case.tera
use anyhow::{Result, anyhow};
use common::database::CommandUnitOfWork;
use common::entities::{TrashInfo, Work};
use common::types::EntityId;

pub trait CloseWorkUnitOfWorkFactoryTrait: Send + Sync {
    fn create(&self) -> Box<dyn CloseWorkUnitOfWorkTrait>;
}

//TODO: adapt entities and actions to real use :
// Create, CreateMulti, Get, GetMulti, Update (scalar-only), UpdateMulti (scalar-only),
// UpdateWithRelationships, UpdateWithRelationshipsMulti,
// Remove, RemoveMulti, GetRelationship, GetRelationshipsFromRightIds,
// SetRelationship, SetRelationshipMulti
//
// You have here a read-write unit of work trait.
//
// RO means Read Only.
// Do not mix read-only and write actions in the same unit of work.
//
// Exactly the same macros must be set in the use case uow trait file in ../units_of_work/close_work_uow.rs
//
#[macros::uow_action(entity = "Work", action = "Get")]
#[macros::uow_action(entity = "Work", action = "GetMulti")]
#[macros::uow_action(entity = "Work", action = "Snapshot")]
#[macros::uow_action(entity = "Work", action = "Restore")]
#[macros::uow_action(entity = "TrashInfo", action = "Get")]
#[macros::uow_action(entity = "TrashInfo", action = "GetMulti")]
#[macros::uow_action(entity = "TrashInfo", action = "Snapshot")]
#[macros::uow_action(entity = "TrashInfo", action = "Restore")]
pub trait CloseWorkUnitOfWorkTrait: CommandUnitOfWork {
    fn publish_close_work_event(&self, ids: Vec<EntityId>, data: Option<String>);
}

pub struct CloseWorkUseCase {
    uow_factory: Box<dyn CloseWorkUnitOfWorkFactoryTrait>,
}

impl CloseWorkUseCase {
    pub fn new(uow_factory: Box<dyn CloseWorkUnitOfWorkFactoryTrait>) -> Self {
        CloseWorkUseCase { uow_factory }
    }

    pub fn execute(&mut self) -> Result<()> {
        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;

        //TODO: CloseWorkUseCase to be implemented
        unimplemented!("CloseWorkUseCase unimplemented");
        uow.commit()?;

        uow.publish_close_work_event(vec![], None);
        Ok(())
    }
}
