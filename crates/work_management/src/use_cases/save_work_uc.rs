// Generated by Qleany v1.7.4 from feature_use_case.tera
use crate::SaveWorkDto;
use anyhow::Result;
use common::database::CommandUnitOfWork;
use common::types::EntityId;

pub trait SaveWorkUnitOfWorkFactoryTrait: Send + Sync {
    fn create(&self) -> Box<dyn SaveWorkUnitOfWorkTrait>;
}

//TODO: adapt entities and actions to real use :
// Create, CreateMulti, Get, GetMulti, Update (scalar-only), UpdateMulti (scalar-only),
// UpdateWithRelationships, UpdateWithRelationshipsMulti,
// Remove, RemoveMulti, GetRelationship, GetRelationshipsFromRightIds,
// SetRelationship, SetRelationshipMulti
//
// You have here a read-write unit of work trait.
//
// RO means Read Only.
// Do not mix read-only and write actions in the same unit of work.
//
// Exactly the same macros must be set in the use case uow trait file in ../units_of_work/save_work_uow.rs
//
pub trait SaveWorkUnitOfWorkTrait: CommandUnitOfWork {
    fn publish_save_work_event(&self, ids: Vec<EntityId>, data: Option<String>);
}

pub struct SaveWorkUseCase {
    uow_factory: Box<dyn SaveWorkUnitOfWorkFactoryTrait>,
}

impl SaveWorkUseCase {
    pub fn new(uow_factory: Box<dyn SaveWorkUnitOfWorkFactoryTrait>) -> Self {
        SaveWorkUseCase { uow_factory }
    }

    #[allow(unreachable_code)]
    pub fn execute(&mut self, _dto: &SaveWorkDto) -> Result<()> {
        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;

        //TODO: SaveWorkUseCase to be implemented
        unimplemented!("SaveWorkUseCase unimplemented");
        uow.commit()?;

        uow.publish_save_work_event(vec![], None);
        Ok(())
    }
}
