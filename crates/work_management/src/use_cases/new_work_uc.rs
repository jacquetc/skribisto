// Generated by Qleany v1.7.4 from feature_use_case.tera
use crate::NewWorkDto;
use anyhow::Result;
use common::database::CommandUnitOfWork;
use common::entities::{Binder, BinderItem, Content, Root, Work};
use common::types::EntityId;

pub trait NewWorkUnitOfWorkFactoryTrait: Send + Sync {
    fn create(&self) -> Box<dyn NewWorkUnitOfWorkTrait>;
}

//TODO: adapt entities and actions to real use :
// Create, CreateMulti, Get, GetMulti, Update (scalar-only), UpdateMulti (scalar-only),
// UpdateWithRelationships, UpdateWithRelationshipsMulti,
// Remove, RemoveMulti, GetRelationship, GetRelationshipsFromRightIds,
// SetRelationship, SetRelationshipMulti
//
// You have here a read-write unit of work trait.
//
// RO means Read Only.
// Do not mix read-only and write actions in the same unit of work.
//
// Exactly the same macros must be set in the use case uow trait file in ../units_of_work/new_work_uow.rs
//
#[macros::uow_action(entity = "Root", action = "Get")]
#[macros::uow_action(entity = "Root", action = "GetMulti")]
#[macros::uow_action(entity = "Root", action = "Snapshot")]
#[macros::uow_action(entity = "Root", action = "Restore")]
#[macros::uow_action(entity = "Work", action = "Get")]
#[macros::uow_action(entity = "Work", action = "GetMulti")]
#[macros::uow_action(entity = "Work", action = "Snapshot")]
#[macros::uow_action(entity = "Work", action = "Restore")]
#[macros::uow_action(entity = "Binder", action = "Get")]
#[macros::uow_action(entity = "Binder", action = "GetMulti")]
#[macros::uow_action(entity = "Binder", action = "Snapshot")]
#[macros::uow_action(entity = "Binder", action = "Restore")]
#[macros::uow_action(entity = "BinderItem", action = "Get")]
#[macros::uow_action(entity = "BinderItem", action = "GetMulti")]
#[macros::uow_action(entity = "BinderItem", action = "Snapshot")]
#[macros::uow_action(entity = "BinderItem", action = "Restore")]
#[macros::uow_action(entity = "Content", action = "Get")]
#[macros::uow_action(entity = "Content", action = "GetMulti")]
#[macros::uow_action(entity = "Content", action = "Snapshot")]
#[macros::uow_action(entity = "Content", action = "Restore")]
pub trait NewWorkUnitOfWorkTrait: CommandUnitOfWork {
    fn publish_new_work_event(&self, ids: Vec<EntityId>, data: Option<String>);
}

pub struct NewWorkUseCase {
    uow_factory: Box<dyn NewWorkUnitOfWorkFactoryTrait>,
}

impl NewWorkUseCase {
    pub fn new(uow_factory: Box<dyn NewWorkUnitOfWorkFactoryTrait>) -> Self {
        NewWorkUseCase { uow_factory }
    }

    #[allow(unreachable_code)]
    pub fn execute(&mut self, _dto: &NewWorkDto) -> Result<()> {
        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;

        //TODO: NewWorkUseCase to be implemented
        unimplemented!("NewWorkUseCase unimplemented");
        uow.commit()?;

        uow.publish_new_work_event(vec![], None);
        Ok(())
    }
}
