// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.8.0 from feature_controller.tera

use crate::DeleteTrashEntriesDto;
use crate::RestoreItemsDto;
use crate::RestoreItemsToDto;
use crate::RestoreItemsToResultDto;
use crate::RestoreResultDto;
use crate::TrashBinderDto;
use crate::TrashBinderItemsDto;
use crate::units_of_work::delete_trash_entries_uow::DeleteTrashEntriesUnitOfWorkFactory;
use crate::units_of_work::empty_trash_uow::EmptyTrashUnitOfWorkFactory;
use crate::units_of_work::restore_items_to_uow::RestoreItemsToUnitOfWorkFactory;
use crate::units_of_work::restore_items_uow::RestoreItemsUnitOfWorkFactory;
use crate::units_of_work::trash_binder_items_uow::TrashBinderItemsUnitOfWorkFactory;
use crate::units_of_work::trash_binder_uow::TrashBinderUnitOfWorkFactory;
use crate::use_cases::delete_trash_entries_uc::DeleteTrashEntriesUseCase;
use crate::use_cases::empty_trash_uc::EmptyTrashUseCase;
use crate::use_cases::restore_items_to_uc::RestoreItemsToUseCase;
use crate::use_cases::restore_items_uc::RestoreItemsUseCase;
use crate::use_cases::trash_binder_items_uc::TrashBinderItemsUseCase;
use crate::use_cases::trash_binder_uc::TrashBinderUseCase;
use anyhow::Result;

use common::undo_redo::UndoRedoManager;
use common::{database::db_context::DbContext, event::EventHub};
use std::sync::Arc;

pub fn trash_binder_items(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    dto: &TrashBinderItemsDto,
) -> Result<()> {
    let uow_context = TrashBinderItemsUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = TrashBinderItemsUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(return_dto)
}

pub fn trash_binder(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    dto: &TrashBinderDto,
) -> Result<()> {
    let uow_context = TrashBinderUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = TrashBinderUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(return_dto)
}

pub fn restore_items(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    dto: &RestoreItemsDto,
) -> Result<RestoreResultDto> {
    let uow_context = RestoreItemsUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = RestoreItemsUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(return_dto)
}

pub fn empty_trash(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
) -> Result<()> {
    let uow_context = EmptyTrashUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = EmptyTrashUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute()?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(return_dto)
}

pub fn restore_items_to(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    dto: &RestoreItemsToDto,
) -> Result<RestoreItemsToResultDto> {
    let uow_context = RestoreItemsToUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = RestoreItemsToUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(return_dto)
}

pub fn delete_trash_entries(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    dto: &DeleteTrashEntriesDto,
) -> Result<()> {
    let uow_context = DeleteTrashEntriesUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = DeleteTrashEntriesUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(return_dto)
}
