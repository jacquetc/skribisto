// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from feature_use_case_uow.tera
// Modify-and-protect scaffold: don't blanket-regenerate. The
// `#[macros::uow_action(...)]` list below is hand-trimmed to the specific
// actions this use case's business logic actually calls -- the generator's
// scaffold re-renders the full generic Get/GetMulti/Snapshot/Restore set for
// every related entity plus a TODO stub, which would silently replace this
// trimmed set and break compilation. The `write_guard` field/acquire below is
// now plain generated output (Qleany v1.9.0+), not a hand-edit; the action
// list above is the sole reason this file stays hand-maintained.

use crate::use_cases::import_tags_uc::{
    ImportTagsUnitOfWorkFactoryTrait, ImportTagsUnitOfWorkTrait,
};
use anyhow::{Ok, Result};
use common::database::CommandUnitOfWork;
use common::database::write_guard::WriteTransactionGuard;
use common::database::{db_context::DbContext, transactions::Transaction};
#[allow(unused_imports)]
use common::entities::{BinderTag, Work};
use common::event::TagManagementEvent::ImportTags;
use common::event::{AllEvent, DirectAccessEntity, Event, EventBuffer, EventHub, Origin};
#[allow(unused_imports)]
use common::types;
#[allow(unused_imports)]
use common::types::EntityId;
use std::cell::RefCell;
use std::sync::Arc;

// Unit of work for ImportTags

pub struct ImportTagsUnitOfWork {
    context: DbContext,
    transaction: Option<Transaction>,
    // RAII: acquired in `begin_transaction`, released via `commit`/`rollback`
    // falling out of scope alongside `transaction` — see
    // `common::database::write_guard`'s module doc.
    write_guard: Option<WriteTransactionGuard>,
    event_hub: Arc<EventHub>,
    event_buffer: RefCell<EventBuffer>,
}

impl ImportTagsUnitOfWork {
    pub fn new(db_context: &DbContext, event_hub: &Arc<EventHub>) -> Self {
        ImportTagsUnitOfWork {
            context: db_context.clone(),
            transaction: None,
            write_guard: None,
            event_hub: event_hub.clone(),
            event_buffer: RefCell::new(EventBuffer::new()),
        }
    }
}

impl CommandUnitOfWork for ImportTagsUnitOfWork {
    fn begin_transaction(&mut self) -> Result<()> {
        // Acquire BEFORE the write transaction itself — see
        // `common::database::write_guard`'s module doc for why this call must
        // stay UI-thread-synchronous unless given a real off-thread gate.
        self.write_guard = Some(WriteTransactionGuard::acquire(&self.context, "import_tags")?);
        self.transaction = Some(Transaction::begin_write_transaction(&self.context)?);
        self.event_buffer.get_mut().begin_buffering();
        Ok(())
    }

    fn commit(&mut self) -> Result<()> {
        self.transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .commit()?;
        self.write_guard = None;
        for event in self.event_buffer.get_mut().flush() {
            self.event_hub.send_event(event);
        }
        Ok(())
    }

    fn rollback(&mut self) -> Result<()> {
        self.transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .rollback()?;
        self.write_guard = None;
        self.event_buffer.get_mut().discard();
        Ok(())
    }

    fn create_savepoint(&self) -> Result<types::Savepoint> {
        self.transaction
            .as_ref()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .create_savepoint()
    }

    fn restore_to_savepoint(&mut self, savepoint: types::Savepoint) -> Result<()> {
        let mut transaction = self
            .transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?;
        transaction.restore_to_savepoint(savepoint)?;

        // Discard buffered events — savepoint restore invalidated them
        self.event_buffer.get_mut().discard();

        // Send Reset immediately (not buffered — UI must refresh now)
        self.event_hub.send_event(Event {
            origin: Origin::DirectAccess(DirectAccessEntity::All(AllEvent::Reset)),
            ids: vec![],
            data: None,
        });

        // Recreate the transaction after restoring to savepoint
        self.transaction = Some(transaction);

        Ok(())
    }
}

// The same macro set must appear on the trait in ../use_cases/import_tags_uc.rs.
#[macros::uow_action(entity = "Work", action = "GetAll")]
#[macros::uow_action(entity = "Work", action = "GetRelationship")]
#[macros::uow_action(entity = "Work", action = "SetRelationship")]
#[macros::uow_action(entity = "Work", action = "Snapshot")]
#[macros::uow_action(entity = "Work", action = "Restore")]
#[macros::uow_action(entity = "BinderTag", action = "GetMulti")]
#[macros::uow_action(entity = "BinderTag", action = "CreateOrphan")]
impl ImportTagsUnitOfWorkTrait for ImportTagsUnitOfWork {
    fn publish_import_tags_event(&self, ids: Vec<EntityId>, data: Option<String>) {
        self.event_hub.send_event(Event {
            origin: Origin::TagManagement(ImportTags),
            ids,
            data,
        });
    }
}

pub struct ImportTagsUnitOfWorkFactory {
    context: DbContext,
    event_hub: Arc<EventHub>,
}

impl ImportTagsUnitOfWorkFactory {
    pub fn new(db_context: &DbContext, event_hub: &Arc<EventHub>) -> Self {
        ImportTagsUnitOfWorkFactory {
            context: db_context.clone(),
            event_hub: event_hub.clone(),
        }
    }
}

impl ImportTagsUnitOfWorkFactoryTrait for ImportTagsUnitOfWorkFactory {
    fn create(&self) -> Box<dyn ImportTagsUnitOfWorkTrait> {
        Box::new(ImportTagsUnitOfWork::new(&self.context, &self.event_hub))
    }
}
