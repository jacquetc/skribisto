// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from frontend_entity_commands.tera

//! CommentReply entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{
    CommentReplyDto, CreateCommentReplyDto, UpdateCommentReplyDto, comment_reply_controller,
};

/// Create a new comment_reply entity (orphan, no parent)
pub fn create_orphan_comment_reply(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateCommentReplyDto,
) -> Result<CommentReplyDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating comment_reply")
}
/// Create a new comment_reply entity as child of owner
pub fn create_comment_reply(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateCommentReplyDto,
    owner_id: EntityId,
    index: i32,
) -> Result<CommentReplyDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating comment_reply")
}
/// Create multiple comment_reply entities (orphan, no parent)
pub fn create_orphan_comment_reply_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateCommentReplyDto],
) -> Result<Vec<CommentReplyDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating comment_reply entities")
}
/// Create multiple comment_reply entities as children of owner
pub fn create_comment_reply_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateCommentReplyDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<CommentReplyDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating comment_reply entities")
}
/// Get a comment_reply entity by ID
pub fn get_comment_reply(ctx: &AppContext, id: &EntityId) -> Result<Option<CommentReplyDto>> {
    comment_reply_controller::get(&ctx.db_context, id).context("getting comment_reply")
}

/// Get multiple comment_reply entities by IDs
pub fn get_comment_reply_multi(
    ctx: &AppContext,
    ids: &[EntityId],
) -> Result<Vec<Option<CommentReplyDto>>> {
    comment_reply_controller::get_multi(&ctx.db_context, ids)
        .context("getting comment_reply entities")
}

/// Get all comment_reply entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_comment_reply(ctx: &AppContext) -> Result<Vec<CommentReplyDto>> {
    comment_reply_controller::get_all(&ctx.db_context).context("getting all comment_reply entities")
}

/// Update a comment_reply entity
pub fn update_comment_reply(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateCommentReplyDto,
) -> Result<CommentReplyDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating comment_reply")
}

/// Update multiple comment_reply entities
pub fn update_comment_reply_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateCommentReplyDto],
) -> Result<Vec<CommentReplyDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating comment_reply entities")
}

/// Update a comment_reply entity with relationships
pub fn update_comment_reply_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CommentReplyDto,
) -> Result<CommentReplyDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating comment_reply with relationships")
}

/// Update multiple comment_reply entities with relationships
pub fn update_comment_reply_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CommentReplyDto],
) -> Result<Vec<CommentReplyDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating comment_reply entities with relationships")
}

/// Remove a comment_reply entity by ID
pub fn remove_comment_reply(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing comment_reply")
}

/// Remove multiple comment_reply entities by IDs
pub fn remove_comment_reply_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    comment_reply_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing comment_reply entities")
}
