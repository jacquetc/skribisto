// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.10.0 from frontend_entity_commands.tera

//! Asset entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{AssetDto, CreateAssetDto, UpdateAssetDto, asset_controller};

/// Create a new asset entity (orphan, no parent)
pub fn create_orphan_asset(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateAssetDto,
) -> Result<AssetDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating asset")
}
/// Create a new asset entity as child of owner
pub fn create_asset(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateAssetDto,
    owner_id: EntityId,
    index: i32,
) -> Result<AssetDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating asset")
}
/// Create multiple asset entities (orphan, no parent)
pub fn create_orphan_asset_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateAssetDto],
) -> Result<Vec<AssetDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating asset entities")
}
/// Create multiple asset entities as children of owner
pub fn create_asset_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateAssetDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<AssetDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating asset entities")
}
/// Get a asset entity by ID
pub fn get_asset(ctx: &AppContext, id: &EntityId) -> Result<Option<AssetDto>> {
    asset_controller::get(&ctx.db_context, id).context("getting asset")
}

/// Get multiple asset entities by IDs
pub fn get_asset_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<AssetDto>>> {
    asset_controller::get_multi(&ctx.db_context, ids).context("getting asset entities")
}

/// Get all asset entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_asset(ctx: &AppContext) -> Result<Vec<AssetDto>> {
    asset_controller::get_all(&ctx.db_context).context("getting all asset entities")
}

/// Update a asset entity
pub fn update_asset(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateAssetDto,
) -> Result<AssetDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating asset")
}

/// Update multiple asset entities
pub fn update_asset_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateAssetDto],
) -> Result<Vec<AssetDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating asset entities")
}

/// Update a asset entity with relationships
pub fn update_asset_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &AssetDto,
) -> Result<AssetDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating asset with relationships")
}

/// Update multiple asset entities with relationships
pub fn update_asset_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[AssetDto],
) -> Result<Vec<AssetDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating asset entities with relationships")
}

/// Remove a asset entity by ID
pub fn remove_asset(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing asset")
}

/// Remove multiple asset entities by IDs
pub fn remove_asset_multi(ctx: &AppContext, stack_id: Option<u64>, ids: &[EntityId]) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    asset_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing asset entities")
}
