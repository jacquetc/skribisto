// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.7.4 from frontend_entity_commands.tera

//! DictWord entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{CreateDictWordDto, DictWordDto, UpdateDictWordDto, dict_word_controller};

/// Create a new dict_word entity (orphan, no parent)
pub fn create_orphan_dict_word(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateDictWordDto,
) -> Result<DictWordDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating dict_word")
}
/// Create a new dict_word entity as child of owner
pub fn create_dict_word(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateDictWordDto,
    owner_id: EntityId,
    index: i32,
) -> Result<DictWordDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating dict_word")
}
/// Create multiple dict_word entities (orphan, no parent)
pub fn create_orphan_dict_word_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateDictWordDto],
) -> Result<Vec<DictWordDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating dict_word entities")
}
/// Create multiple dict_word entities as children of owner
pub fn create_dict_word_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateDictWordDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<DictWordDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating dict_word entities")
}
/// Get a dict_word entity by ID
pub fn get_dict_word(ctx: &AppContext, id: &EntityId) -> Result<Option<DictWordDto>> {
    dict_word_controller::get(&ctx.db_context, id).context("getting dict_word")
}

/// Get multiple dict_word entities by IDs
pub fn get_dict_word_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<DictWordDto>>> {
    dict_word_controller::get_multi(&ctx.db_context, ids).context("getting dict_word entities")
}

/// Get all dict_word entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_dict_word(ctx: &AppContext) -> Result<Vec<DictWordDto>> {
    dict_word_controller::get_all(&ctx.db_context).context("getting all dict_word entities")
}

/// Update a dict_word entity
pub fn update_dict_word(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateDictWordDto,
) -> Result<DictWordDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating dict_word")
}

/// Update multiple dict_word entities
pub fn update_dict_word_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateDictWordDto],
) -> Result<Vec<DictWordDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating dict_word entities")
}

/// Update a dict_word entity with relationships
pub fn update_dict_word_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &DictWordDto,
) -> Result<DictWordDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating dict_word with relationships")
}

/// Update multiple dict_word entities with relationships
pub fn update_dict_word_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[DictWordDto],
) -> Result<Vec<DictWordDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating dict_word entities with relationships")
}

/// Remove a dict_word entity by ID
pub fn remove_dict_word(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing dict_word")
}

/// Remove multiple dict_word entities by IDs
pub fn remove_dict_word_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dict_word_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing dict_word entities")
}
