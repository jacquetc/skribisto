// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from frontend_entity_commands.tera

//! RecentWork entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{
    CreateRecentWorkDto, RecentWorkDto, UpdateRecentWorkDto, recent_work_controller,
};

/// Create a new recent_work entity (orphan, no parent)
pub fn create_orphan_recent_work(
    ctx: &AppContext,

    dto: &CreateRecentWorkDto,
) -> Result<RecentWorkDto> {
    recent_work_controller::create_orphan(&ctx.db_context, &ctx.event_hub, dto)
        .context("creating recent_work")
}
/// Create a new recent_work entity as child of owner
pub fn create_recent_work(
    ctx: &AppContext,

    dto: &CreateRecentWorkDto,
    owner_id: EntityId,
    index: i32,
) -> Result<RecentWorkDto> {
    recent_work_controller::create(&ctx.db_context, &ctx.event_hub, dto, owner_id, index)
        .context("creating recent_work")
}
/// Create multiple recent_work entities (orphan, no parent)
pub fn create_orphan_recent_work_multi(
    ctx: &AppContext,

    dtos: &[CreateRecentWorkDto],
) -> Result<Vec<RecentWorkDto>> {
    recent_work_controller::create_orphan_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("creating recent_work entities")
}
/// Create multiple recent_work entities as children of owner
pub fn create_recent_work_multi(
    ctx: &AppContext,

    dtos: &[CreateRecentWorkDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<RecentWorkDto>> {
    recent_work_controller::create_multi(&ctx.db_context, &ctx.event_hub, dtos, owner_id, index)
        .context("creating recent_work entities")
}
/// Get a recent_work entity by ID
pub fn get_recent_work(ctx: &AppContext, id: &EntityId) -> Result<Option<RecentWorkDto>> {
    recent_work_controller::get(&ctx.db_context, id).context("getting recent_work")
}

/// Get multiple recent_work entities by IDs
pub fn get_recent_work_multi(
    ctx: &AppContext,
    ids: &[EntityId],
) -> Result<Vec<Option<RecentWorkDto>>> {
    recent_work_controller::get_multi(&ctx.db_context, ids).context("getting recent_work entities")
}

/// Get all recent_work entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_recent_work(ctx: &AppContext) -> Result<Vec<RecentWorkDto>> {
    recent_work_controller::get_all(&ctx.db_context).context("getting all recent_work entities")
}

/// Update a recent_work entity
pub fn update_recent_work(ctx: &AppContext, dto: &UpdateRecentWorkDto) -> Result<RecentWorkDto> {
    recent_work_controller::update(&ctx.db_context, &ctx.event_hub, dto)
        .context("updating recent_work")
}

/// Update multiple recent_work entities
pub fn update_recent_work_multi(
    ctx: &AppContext,

    dtos: &[UpdateRecentWorkDto],
) -> Result<Vec<RecentWorkDto>> {
    recent_work_controller::update_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating recent_work entities")
}

/// Update a recent_work entity with relationships
pub fn update_recent_work_with_relationships(
    ctx: &AppContext,

    dto: &RecentWorkDto,
) -> Result<RecentWorkDto> {
    recent_work_controller::update_with_relationships(&ctx.db_context, &ctx.event_hub, dto)
        .context("updating recent_work with relationships")
}

/// Update multiple recent_work entities with relationships
pub fn update_recent_work_with_relationships_multi(
    ctx: &AppContext,

    dtos: &[RecentWorkDto],
) -> Result<Vec<RecentWorkDto>> {
    recent_work_controller::update_with_relationships_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating recent_work entities with relationships")
}

/// Remove a recent_work entity by ID
pub fn remove_recent_work(ctx: &AppContext, id: &EntityId) -> Result<()> {
    recent_work_controller::remove(&ctx.db_context, &ctx.event_hub, id)
        .context("removing recent_work")
}

/// Remove multiple recent_work entities by IDs
pub fn remove_recent_work_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<()> {
    recent_work_controller::remove_multi(&ctx.db_context, &ctx.event_hub, ids)
        .context("removing recent_work entities")
}
