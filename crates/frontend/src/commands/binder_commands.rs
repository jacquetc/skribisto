// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from frontend_entity_commands.tera

//! Binder entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::binder::BinderRelationshipField;
use common::types::EntityId;
use direct_access::BinderRelationshipDto;
use direct_access::{BinderDto, CreateBinderDto, UpdateBinderDto, binder_controller};

/// Create a new binder entity (orphan, no parent)
pub fn create_orphan_binder(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateBinderDto,
) -> Result<BinderDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating binder")
}
/// Create a new binder entity as child of owner
pub fn create_binder(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateBinderDto,
    owner_id: EntityId,
    index: i32,
) -> Result<BinderDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating binder")
}
/// Create multiple binder entities (orphan, no parent)
pub fn create_orphan_binder_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateBinderDto],
) -> Result<Vec<BinderDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating binder entities")
}
/// Create multiple binder entities as children of owner
pub fn create_binder_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateBinderDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<BinderDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating binder entities")
}
/// Get a binder entity by ID
pub fn get_binder(ctx: &AppContext, id: &EntityId) -> Result<Option<BinderDto>> {
    binder_controller::get(&ctx.db_context, id).context("getting binder")
}

/// Get multiple binder entities by IDs
pub fn get_binder_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<BinderDto>>> {
    binder_controller::get_multi(&ctx.db_context, ids).context("getting binder entities")
}

/// Get all binder entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_binder(ctx: &AppContext) -> Result<Vec<BinderDto>> {
    binder_controller::get_all(&ctx.db_context).context("getting all binder entities")
}

/// Update a binder entity
pub fn update_binder(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateBinderDto,
) -> Result<BinderDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating binder")
}

/// Update multiple binder entities
pub fn update_binder_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateBinderDto],
) -> Result<Vec<BinderDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating binder entities")
}

/// Update a binder entity with relationships
pub fn update_binder_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &BinderDto,
) -> Result<BinderDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating binder with relationships")
}

/// Update multiple binder entities with relationships
pub fn update_binder_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[BinderDto],
) -> Result<Vec<BinderDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating binder entities with relationships")
}

/// Remove a binder entity by ID
pub fn remove_binder(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing binder")
}

/// Remove multiple binder entities by IDs
pub fn remove_binder_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing binder entities")
}

/// Get a binder relationship
pub fn get_binder_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &BinderRelationshipField,
) -> Result<Vec<EntityId>> {
    binder_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting binder relationship")
}

/// Get relationship IDs for multiple binder entities at once
pub fn get_binder_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &BinderRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    binder_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting binder relationships (many)")
}

/// Get relationship count for a binder entity
pub fn get_binder_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &BinderRelationshipField,
) -> Result<usize> {
    binder_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting binder relationship count")
}

/// Get relationship IDs for a binder entity with pagination
pub fn get_binder_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &BinderRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    binder_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting binder relationship range")
}

/// Set a binder relationship
pub fn set_binder_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &BinderRelationshipDto,
) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::set_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("setting binder relationship")
}

/// Move (reorder) IDs within a binder relationship
pub fn move_binder_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    id: &EntityId,
    field: &BinderRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving binder relationship")
}
