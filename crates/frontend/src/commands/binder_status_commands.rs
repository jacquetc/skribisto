// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.10.0 from frontend_entity_commands.tera

//! BinderStatus entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{
    BinderStatusDto, CreateBinderStatusDto, UpdateBinderStatusDto, binder_status_controller,
};

/// Create a new binder_status entity (orphan, no parent)
pub fn create_orphan_binder_status(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateBinderStatusDto,
) -> Result<BinderStatusDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating binder_status")
}
/// Create a new binder_status entity as child of owner
pub fn create_binder_status(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateBinderStatusDto,
    owner_id: EntityId,
    index: i32,
) -> Result<BinderStatusDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating binder_status")
}
/// Create multiple binder_status entities (orphan, no parent)
pub fn create_orphan_binder_status_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateBinderStatusDto],
) -> Result<Vec<BinderStatusDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating binder_status entities")
}
/// Create multiple binder_status entities as children of owner
pub fn create_binder_status_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateBinderStatusDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<BinderStatusDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating binder_status entities")
}
/// Get a binder_status entity by ID
pub fn get_binder_status(ctx: &AppContext, id: &EntityId) -> Result<Option<BinderStatusDto>> {
    binder_status_controller::get(&ctx.db_context, id).context("getting binder_status")
}

/// Get multiple binder_status entities by IDs
pub fn get_binder_status_multi(
    ctx: &AppContext,
    ids: &[EntityId],
) -> Result<Vec<Option<BinderStatusDto>>> {
    binder_status_controller::get_multi(&ctx.db_context, ids)
        .context("getting binder_status entities")
}

/// Get all binder_status entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_binder_status(ctx: &AppContext) -> Result<Vec<BinderStatusDto>> {
    binder_status_controller::get_all(&ctx.db_context).context("getting all binder_status entities")
}

/// Update a binder_status entity
pub fn update_binder_status(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateBinderStatusDto,
) -> Result<BinderStatusDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating binder_status")
}

/// Update multiple binder_status entities
pub fn update_binder_status_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateBinderStatusDto],
) -> Result<Vec<BinderStatusDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating binder_status entities")
}

/// Update a binder_status entity with relationships
pub fn update_binder_status_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &BinderStatusDto,
) -> Result<BinderStatusDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating binder_status with relationships")
}

/// Update multiple binder_status entities with relationships
pub fn update_binder_status_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[BinderStatusDto],
) -> Result<Vec<BinderStatusDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating binder_status entities with relationships")
}

/// Remove a binder_status entity by ID
pub fn remove_binder_status(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing binder_status")
}

/// Remove multiple binder_status entities by IDs
pub fn remove_binder_status_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_status_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing binder_status entities")
}
