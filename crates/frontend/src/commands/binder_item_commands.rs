// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.7.4 from frontend_entity_commands.tera

//! BinderItem entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::binder_item::BinderItemRelationshipField;
use common::types::EntityId;
use direct_access::BinderItemRelationshipDto;
use direct_access::{
    BinderItemDto, CreateBinderItemDto, UpdateBinderItemDto, binder_item_controller,
};

/// Create a new binder_item entity (orphan, no parent)
pub fn create_orphan_binder_item(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateBinderItemDto,
) -> Result<BinderItemDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating binder_item")
}
/// Create a new binder_item entity as child of owner
pub fn create_binder_item(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateBinderItemDto,
    owner_id: EntityId,
    index: i32,
) -> Result<BinderItemDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating binder_item")
}
/// Create multiple binder_item entities (orphan, no parent)
pub fn create_orphan_binder_item_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateBinderItemDto],
) -> Result<Vec<BinderItemDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating binder_item entities")
}
/// Create multiple binder_item entities as children of owner
pub fn create_binder_item_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateBinderItemDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<BinderItemDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating binder_item entities")
}
/// Get a binder_item entity by ID
pub fn get_binder_item(ctx: &AppContext, id: &EntityId) -> Result<Option<BinderItemDto>> {
    binder_item_controller::get(&ctx.db_context, id).context("getting binder_item")
}

/// Get multiple binder_item entities by IDs
pub fn get_binder_item_multi(
    ctx: &AppContext,
    ids: &[EntityId],
) -> Result<Vec<Option<BinderItemDto>>> {
    binder_item_controller::get_multi(&ctx.db_context, ids).context("getting binder_item entities")
}

/// Get all binder_item entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_binder_item(ctx: &AppContext) -> Result<Vec<BinderItemDto>> {
    binder_item_controller::get_all(&ctx.db_context).context("getting all binder_item entities")
}

/// Update a binder_item entity
pub fn update_binder_item(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateBinderItemDto,
) -> Result<BinderItemDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating binder_item")
}

/// Update multiple binder_item entities
pub fn update_binder_item_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateBinderItemDto],
) -> Result<Vec<BinderItemDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating binder_item entities")
}

/// Update a binder_item entity with relationships
pub fn update_binder_item_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &BinderItemDto,
) -> Result<BinderItemDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating binder_item with relationships")
}

/// Update multiple binder_item entities with relationships
pub fn update_binder_item_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[BinderItemDto],
) -> Result<Vec<BinderItemDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating binder_item entities with relationships")
}

/// Remove a binder_item entity by ID
pub fn remove_binder_item(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing binder_item")
}

/// Remove multiple binder_item entities by IDs
pub fn remove_binder_item_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing binder_item entities")
}

/// Get a binder_item relationship
pub fn get_binder_item_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &BinderItemRelationshipField,
) -> Result<Vec<EntityId>> {
    binder_item_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting binder_item relationship")
}

/// Get relationship IDs for multiple binder_item entities at once
pub fn get_binder_item_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &BinderItemRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    binder_item_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting binder_item relationships (many)")
}

/// Get relationship count for a binder_item entity
pub fn get_binder_item_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &BinderItemRelationshipField,
) -> Result<usize> {
    binder_item_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting binder_item relationship count")
}

/// Get relationship IDs for a binder_item entity with pagination
pub fn get_binder_item_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &BinderItemRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    binder_item_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting binder_item relationship range")
}

/// Set a binder_item relationship
pub fn set_binder_item_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &BinderItemRelationshipDto,
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::set_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("setting binder_item relationship")
}

/// Move (reorder) IDs within a binder_item relationship
pub fn move_binder_item_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    id: &EntityId,
    field: &BinderItemRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    binder_item_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving binder_item relationship")
}
