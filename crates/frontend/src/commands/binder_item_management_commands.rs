// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from frontend_feature_commands.tera

//! BinderItemManagement feature commands

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use binder_item_management::{
    DuplicateDto, DuplicateReturnDto, MergeTwoScenesDto, MoveDto, PromoteDto, SplitSceneDto,
    binder_item_management_controller,
};

pub fn duplicate(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &DuplicateDto,
) -> Result<DuplicateReturnDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_item_management_controller::duplicate(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("duplicate")
}

pub fn move_items(ctx: &AppContext, stack_id: Option<u64>, dto: &MoveDto) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_item_management_controller::move_items(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("move_items")
}

pub fn merge_two_scenes(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &MergeTwoScenesDto,
) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_item_management_controller::merge_two_scenes(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("merge_two_scenes")
}

pub fn split_scene(ctx: &AppContext, stack_id: Option<u64>, dto: &SplitSceneDto) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_item_management_controller::split_scene(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("split_scene")
}

pub fn promote(ctx: &AppContext, stack_id: Option<u64>, dto: &PromoteDto) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    binder_item_management_controller::promote(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("promote")
}
