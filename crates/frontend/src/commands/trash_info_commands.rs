// Generated by Qleany v1.7.4 from frontend_entity_commands.tera

//! TrashInfo entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::trash_info::TrashInfoRelationshipField;
use common::types::EntityId;
use direct_access::TrashInfoRelationshipDto;
use direct_access::{CreateTrashInfoDto, TrashInfoDto, UpdateTrashInfoDto, trash_info_controller};

/// Create a new trash_info entity (orphan, no parent)
pub fn create_orphan_trash_info(
    ctx: &AppContext,

    dto: &CreateTrashInfoDto,
) -> Result<TrashInfoDto> {
    trash_info_controller::create_orphan(&ctx.db_context, &ctx.event_hub, dto)
        .context("creating trash_info")
}
/// Create a new trash_info entity as child of owner
pub fn create_trash_info(
    ctx: &AppContext,

    dto: &CreateTrashInfoDto,
    owner_id: EntityId,
    index: i32,
) -> Result<TrashInfoDto> {
    trash_info_controller::create(&ctx.db_context, &ctx.event_hub, dto, owner_id, index)
        .context("creating trash_info")
}
/// Create multiple trash_info entities (orphan, no parent)
pub fn create_orphan_trash_info_multi(
    ctx: &AppContext,

    dtos: &[CreateTrashInfoDto],
) -> Result<Vec<TrashInfoDto>> {
    trash_info_controller::create_orphan_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("creating trash_info entities")
}
/// Create multiple trash_info entities as children of owner
pub fn create_trash_info_multi(
    ctx: &AppContext,

    dtos: &[CreateTrashInfoDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<TrashInfoDto>> {
    trash_info_controller::create_multi(&ctx.db_context, &ctx.event_hub, dtos, owner_id, index)
        .context("creating trash_info entities")
}
/// Get a trash_info entity by ID
pub fn get_trash_info(ctx: &AppContext, id: &EntityId) -> Result<Option<TrashInfoDto>> {
    trash_info_controller::get(&ctx.db_context, id).context("getting trash_info")
}

/// Get multiple trash_info entities by IDs
pub fn get_trash_info_multi(
    ctx: &AppContext,
    ids: &[EntityId],
) -> Result<Vec<Option<TrashInfoDto>>> {
    trash_info_controller::get_multi(&ctx.db_context, ids).context("getting trash_info entities")
}

/// Get all trash_info entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_trash_info(ctx: &AppContext) -> Result<Vec<TrashInfoDto>> {
    trash_info_controller::get_all(&ctx.db_context).context("getting all trash_info entities")
}

/// Update a trash_info entity
pub fn update_trash_info(ctx: &AppContext, dto: &UpdateTrashInfoDto) -> Result<TrashInfoDto> {
    trash_info_controller::update(&ctx.db_context, &ctx.event_hub, dto)
        .context("updating trash_info")
}

/// Update multiple trash_info entities
pub fn update_trash_info_multi(
    ctx: &AppContext,

    dtos: &[UpdateTrashInfoDto],
) -> Result<Vec<TrashInfoDto>> {
    trash_info_controller::update_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating trash_info entities")
}

/// Update a trash_info entity with relationships
pub fn update_trash_info_with_relationships(
    ctx: &AppContext,

    dto: &TrashInfoDto,
) -> Result<TrashInfoDto> {
    trash_info_controller::update_with_relationships(&ctx.db_context, &ctx.event_hub, dto)
        .context("updating trash_info with relationships")
}

/// Update multiple trash_info entities with relationships
pub fn update_trash_info_with_relationships_multi(
    ctx: &AppContext,

    dtos: &[TrashInfoDto],
) -> Result<Vec<TrashInfoDto>> {
    trash_info_controller::update_with_relationships_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating trash_info entities with relationships")
}

/// Remove a trash_info entity by ID
pub fn remove_trash_info(ctx: &AppContext, id: &EntityId) -> Result<()> {
    trash_info_controller::remove(&ctx.db_context, &ctx.event_hub, id)
        .context("removing trash_info")
}

/// Remove multiple trash_info entities by IDs
pub fn remove_trash_info_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<()> {
    trash_info_controller::remove_multi(&ctx.db_context, &ctx.event_hub, ids)
        .context("removing trash_info entities")
}

/// Get a trash_info relationship
pub fn get_trash_info_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &TrashInfoRelationshipField,
) -> Result<Vec<EntityId>> {
    trash_info_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting trash_info relationship")
}

/// Get relationship IDs for multiple trash_info entities at once
pub fn get_trash_info_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &TrashInfoRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    trash_info_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting trash_info relationships (many)")
}

/// Get relationship count for a trash_info entity
pub fn get_trash_info_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &TrashInfoRelationshipField,
) -> Result<usize> {
    trash_info_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting trash_info relationship count")
}

/// Get relationship IDs for a trash_info entity with pagination
pub fn get_trash_info_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &TrashInfoRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    trash_info_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting trash_info relationship range")
}

/// Set a trash_info relationship
pub fn set_trash_info_relationship(ctx: &AppContext, dto: &TrashInfoRelationshipDto) -> Result<()> {
    trash_info_controller::set_relationship(&ctx.db_context, &ctx.event_hub, dto)
        .context("setting trash_info relationship")
}

/// Move (reorder) IDs within a trash_info relationship
pub fn move_trash_info_relationship(
    ctx: &AppContext,

    id: &EntityId,
    field: &TrashInfoRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    trash_info_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving trash_info relationship")
}
