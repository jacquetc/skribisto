// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from frontend_entity_commands.tera

//! Comment entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::comment::CommentRelationshipField;
use common::types::EntityId;
use direct_access::CommentRelationshipDto;
use direct_access::{CommentDto, CreateCommentDto, UpdateCommentDto, comment_controller};

/// Create a new comment entity (orphan, no parent)
pub fn create_orphan_comment(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateCommentDto,
) -> Result<CommentDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating comment")
}
/// Create a new comment entity as child of owner
pub fn create_comment(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateCommentDto,
    owner_id: EntityId,
    index: i32,
) -> Result<CommentDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating comment")
}
/// Create multiple comment entities (orphan, no parent)
pub fn create_orphan_comment_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateCommentDto],
) -> Result<Vec<CommentDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating comment entities")
}
/// Create multiple comment entities as children of owner
pub fn create_comment_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateCommentDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<CommentDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating comment entities")
}
/// Get a comment entity by ID
pub fn get_comment(ctx: &AppContext, id: &EntityId) -> Result<Option<CommentDto>> {
    comment_controller::get(&ctx.db_context, id).context("getting comment")
}

/// Get multiple comment entities by IDs
pub fn get_comment_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<CommentDto>>> {
    comment_controller::get_multi(&ctx.db_context, ids).context("getting comment entities")
}

/// Get all comment entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_comment(ctx: &AppContext) -> Result<Vec<CommentDto>> {
    comment_controller::get_all(&ctx.db_context).context("getting all comment entities")
}

/// Update a comment entity
pub fn update_comment(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateCommentDto,
) -> Result<CommentDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating comment")
}

/// Update multiple comment entities
pub fn update_comment_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateCommentDto],
) -> Result<Vec<CommentDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating comment entities")
}

/// Update a comment entity with relationships
pub fn update_comment_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CommentDto,
) -> Result<CommentDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating comment with relationships")
}

/// Update multiple comment entities with relationships
pub fn update_comment_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CommentDto],
) -> Result<Vec<CommentDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating comment entities with relationships")
}

/// Remove a comment entity by ID
pub fn remove_comment(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing comment")
}

/// Remove multiple comment entities by IDs
pub fn remove_comment_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing comment entities")
}

/// Get a comment relationship
pub fn get_comment_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &CommentRelationshipField,
) -> Result<Vec<EntityId>> {
    comment_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting comment relationship")
}

/// Get relationship IDs for multiple comment entities at once
pub fn get_comment_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &CommentRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    comment_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting comment relationships (many)")
}

/// Get relationship count for a comment entity
pub fn get_comment_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &CommentRelationshipField,
) -> Result<usize> {
    comment_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting comment relationship count")
}

/// Get relationship IDs for a comment entity with pagination
pub fn get_comment_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &CommentRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    comment_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting comment relationship range")
}

/// Set a comment relationship
pub fn set_comment_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CommentRelationshipDto,
) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::set_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("setting comment relationship")
}

/// Move (reorder) IDs within a comment relationship
pub fn move_comment_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    id: &EntityId,
    field: &CommentRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    comment_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving comment relationship")
}
