// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from frontend_entity_commands.tera

//! Content entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{ContentDto, CreateContentDto, UpdateContentDto, content_controller};

/// Create a new content entity (orphan, no parent)
pub fn create_orphan_content(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateContentDto,
) -> Result<ContentDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating content")
}
/// Create a new content entity as child of owner
pub fn create_content(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateContentDto,
    owner_id: EntityId,
    index: i32,
) -> Result<ContentDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating content")
}
/// Create multiple content entities (orphan, no parent)
pub fn create_orphan_content_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateContentDto],
) -> Result<Vec<ContentDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating content entities")
}
/// Create multiple content entities as children of owner
pub fn create_content_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateContentDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<ContentDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating content entities")
}
/// Get a content entity by ID
pub fn get_content(ctx: &AppContext, id: &EntityId) -> Result<Option<ContentDto>> {
    content_controller::get(&ctx.db_context, id).context("getting content")
}

/// Get multiple content entities by IDs
pub fn get_content_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<ContentDto>>> {
    content_controller::get_multi(&ctx.db_context, ids).context("getting content entities")
}

/// Get all content entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_content(ctx: &AppContext) -> Result<Vec<ContentDto>> {
    content_controller::get_all(&ctx.db_context).context("getting all content entities")
}

/// Update a content entity
pub fn update_content(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateContentDto,
) -> Result<ContentDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating content")
}

/// Update multiple content entities
pub fn update_content_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateContentDto],
) -> Result<Vec<ContentDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating content entities")
}

/// Update a content entity with relationships
pub fn update_content_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &ContentDto,
) -> Result<ContentDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating content with relationships")
}

/// Update multiple content entities with relationships
pub fn update_content_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[ContentDto],
) -> Result<Vec<ContentDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating content entities with relationships")
}

/// Remove a content entity by ID
pub fn remove_content(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing content")
}

/// Remove multiple content entities by IDs
pub fn remove_content_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    content_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing content entities")
}
