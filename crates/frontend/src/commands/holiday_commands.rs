// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.8.0 from frontend_entity_commands.tera

//! Holiday entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{CreateHolidayDto, HolidayDto, UpdateHolidayDto, holiday_controller};

/// Create a new holiday entity (orphan, no parent)
pub fn create_orphan_holiday(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateHolidayDto,
) -> Result<HolidayDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating holiday")
}
/// Create a new holiday entity as child of owner
pub fn create_holiday(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateHolidayDto,
    owner_id: EntityId,
    index: i32,
) -> Result<HolidayDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating holiday")
}
/// Create multiple holiday entities (orphan, no parent)
pub fn create_orphan_holiday_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateHolidayDto],
) -> Result<Vec<HolidayDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating holiday entities")
}
/// Create multiple holiday entities as children of owner
pub fn create_holiday_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateHolidayDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<HolidayDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating holiday entities")
}
/// Get a holiday entity by ID
pub fn get_holiday(ctx: &AppContext, id: &EntityId) -> Result<Option<HolidayDto>> {
    holiday_controller::get(&ctx.db_context, id).context("getting holiday")
}

/// Get multiple holiday entities by IDs
pub fn get_holiday_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<HolidayDto>>> {
    holiday_controller::get_multi(&ctx.db_context, ids).context("getting holiday entities")
}

/// Get all holiday entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_holiday(ctx: &AppContext) -> Result<Vec<HolidayDto>> {
    holiday_controller::get_all(&ctx.db_context).context("getting all holiday entities")
}

/// Update a holiday entity
pub fn update_holiday(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateHolidayDto,
) -> Result<HolidayDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating holiday")
}

/// Update multiple holiday entities
pub fn update_holiday_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateHolidayDto],
) -> Result<Vec<HolidayDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating holiday entities")
}

/// Update a holiday entity with relationships
pub fn update_holiday_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &HolidayDto,
) -> Result<HolidayDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating holiday with relationships")
}

/// Update multiple holiday entities with relationships
pub fn update_holiday_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[HolidayDto],
) -> Result<Vec<HolidayDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating holiday entities with relationships")
}

/// Remove a holiday entity by ID
pub fn remove_holiday(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing holiday")
}

/// Remove multiple holiday entities by IDs
pub fn remove_holiday_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    holiday_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing holiday entities")
}
