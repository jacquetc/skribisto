// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.8.0 from frontend_entity_commands.tera

//! Search entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::search::SearchRelationshipField;
use common::types::EntityId;
use direct_access::SearchRelationshipDto;
use direct_access::{CreateSearchDto, SearchDto, UpdateSearchDto, search_controller};

/// Create a new search entity (orphan, no parent)
pub fn create_orphan_search(ctx: &AppContext, dto: &CreateSearchDto) -> Result<SearchDto> {
    search_controller::create_orphan(&ctx.db_context, &ctx.event_hub, dto)
        .context("creating search")
}
/// Create a new search entity as child of owner
pub fn create_search(
    ctx: &AppContext,

    dto: &CreateSearchDto,
    owner_id: EntityId,
    index: i32,
) -> Result<SearchDto> {
    search_controller::create(&ctx.db_context, &ctx.event_hub, dto, owner_id, index)
        .context("creating search")
}
/// Create multiple search entities (orphan, no parent)
pub fn create_orphan_search_multi(
    ctx: &AppContext,

    dtos: &[CreateSearchDto],
) -> Result<Vec<SearchDto>> {
    search_controller::create_orphan_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("creating search entities")
}
/// Create multiple search entities as children of owner
pub fn create_search_multi(
    ctx: &AppContext,

    dtos: &[CreateSearchDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<SearchDto>> {
    search_controller::create_multi(&ctx.db_context, &ctx.event_hub, dtos, owner_id, index)
        .context("creating search entities")
}
/// Get a search entity by ID
pub fn get_search(ctx: &AppContext, id: &EntityId) -> Result<Option<SearchDto>> {
    search_controller::get(&ctx.db_context, id).context("getting search")
}

/// Get multiple search entities by IDs
pub fn get_search_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<SearchDto>>> {
    search_controller::get_multi(&ctx.db_context, ids).context("getting search entities")
}

/// Get all search entities.
/// Note: iteration order is unspecified (`HashMap`-backed) — not insertion order
/// or `EntityId` order. For ordered collections, use relationship-based retrieval
/// (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_search(ctx: &AppContext) -> Result<Vec<SearchDto>> {
    search_controller::get_all(&ctx.db_context).context("getting all search entities")
}

/// Update a search entity
pub fn update_search(ctx: &AppContext, dto: &UpdateSearchDto) -> Result<SearchDto> {
    search_controller::update(&ctx.db_context, &ctx.event_hub, dto).context("updating search")
}

/// Update multiple search entities
pub fn update_search_multi(ctx: &AppContext, dtos: &[UpdateSearchDto]) -> Result<Vec<SearchDto>> {
    search_controller::update_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating search entities")
}

/// Update a search entity with relationships
pub fn update_search_with_relationships(ctx: &AppContext, dto: &SearchDto) -> Result<SearchDto> {
    search_controller::update_with_relationships(&ctx.db_context, &ctx.event_hub, dto)
        .context("updating search with relationships")
}

/// Update multiple search entities with relationships
pub fn update_search_with_relationships_multi(
    ctx: &AppContext,

    dtos: &[SearchDto],
) -> Result<Vec<SearchDto>> {
    search_controller::update_with_relationships_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating search entities with relationships")
}

/// Remove a search entity by ID
pub fn remove_search(ctx: &AppContext, id: &EntityId) -> Result<()> {
    search_controller::remove(&ctx.db_context, &ctx.event_hub, id).context("removing search")
}

/// Remove multiple search entities by IDs
pub fn remove_search_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<()> {
    search_controller::remove_multi(&ctx.db_context, &ctx.event_hub, ids)
        .context("removing search entities")
}

/// Get a search relationship
pub fn get_search_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &SearchRelationshipField,
) -> Result<Vec<EntityId>> {
    search_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting search relationship")
}

/// Get relationship IDs for multiple search entities at once
pub fn get_search_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &SearchRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    search_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting search relationships (many)")
}

/// Get relationship count for a search entity
pub fn get_search_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &SearchRelationshipField,
) -> Result<usize> {
    search_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting search relationship count")
}

/// Get relationship IDs for a search entity with pagination
pub fn get_search_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &SearchRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    search_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting search relationship range")
}

/// Set a search relationship
pub fn set_search_relationship(ctx: &AppContext, dto: &SearchRelationshipDto) -> Result<()> {
    search_controller::set_relationship(&ctx.db_context, &ctx.event_hub, dto)
        .context("setting search relationship")
}

/// Move (reorder) IDs within a search relationship
pub fn move_search_relationship(
    ctx: &AppContext,

    id: &EntityId,
    field: &SearchRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    search_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving search relationship")
}
