// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.8.0 from frontend_entity_commands.tera

//! Pace entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::pace::PaceRelationshipField;
use common::types::EntityId;
use direct_access::PaceRelationshipDto;
use direct_access::{CreatePaceDto, PaceDto, UpdatePaceDto, pace_controller};

/// Create a new pace entity (orphan, no parent)
pub fn create_orphan_pace(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreatePaceDto,
) -> Result<PaceDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating pace")
}
/// Create a new pace entity as child of owner
pub fn create_pace(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreatePaceDto,
    owner_id: EntityId,
    index: i32,
) -> Result<PaceDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating pace")
}
/// Create multiple pace entities (orphan, no parent)
pub fn create_orphan_pace_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreatePaceDto],
) -> Result<Vec<PaceDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating pace entities")
}
/// Create multiple pace entities as children of owner
pub fn create_pace_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreatePaceDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<PaceDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating pace entities")
}
/// Get a pace entity by ID
pub fn get_pace(ctx: &AppContext, id: &EntityId) -> Result<Option<PaceDto>> {
    pace_controller::get(&ctx.db_context, id).context("getting pace")
}

/// Get multiple pace entities by IDs
pub fn get_pace_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<PaceDto>>> {
    pace_controller::get_multi(&ctx.db_context, ids).context("getting pace entities")
}

/// Get all pace entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_pace(ctx: &AppContext) -> Result<Vec<PaceDto>> {
    pace_controller::get_all(&ctx.db_context).context("getting all pace entities")
}

/// Update a pace entity
pub fn update_pace(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdatePaceDto,
) -> Result<PaceDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating pace")
}

/// Update multiple pace entities
pub fn update_pace_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdatePaceDto],
) -> Result<Vec<PaceDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating pace entities")
}

/// Update a pace entity with relationships
pub fn update_pace_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &PaceDto,
) -> Result<PaceDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating pace with relationships")
}

/// Update multiple pace entities with relationships
pub fn update_pace_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[PaceDto],
) -> Result<Vec<PaceDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating pace entities with relationships")
}

/// Remove a pace entity by ID
pub fn remove_pace(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing pace")
}

/// Remove multiple pace entities by IDs
pub fn remove_pace_multi(ctx: &AppContext, stack_id: Option<u64>, ids: &[EntityId]) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing pace entities")
}

/// Get a pace relationship
pub fn get_pace_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &PaceRelationshipField,
) -> Result<Vec<EntityId>> {
    pace_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting pace relationship")
}

/// Get relationship IDs for multiple pace entities at once
pub fn get_pace_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &PaceRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    pace_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting pace relationships (many)")
}

/// Get relationship count for a pace entity
pub fn get_pace_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &PaceRelationshipField,
) -> Result<usize> {
    pace_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting pace relationship count")
}

/// Get relationship IDs for a pace entity with pagination
pub fn get_pace_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &PaceRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    pace_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting pace relationship range")
}

/// Set a pace relationship
pub fn set_pace_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &PaceRelationshipDto,
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::set_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("setting pace relationship")
}

/// Move (reorder) IDs within a pace relationship
pub fn move_pace_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    id: &EntityId,
    field: &PaceRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    pace_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving pace relationship")
}
