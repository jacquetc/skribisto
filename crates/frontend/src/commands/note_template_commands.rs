// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from frontend_entity_commands.tera

//! NoteTemplate entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{
    CreateNoteTemplateDto, NoteTemplateDto, UpdateNoteTemplateDto, note_template_controller,
};

/// Create a new note_template entity (orphan, no parent)
pub fn create_orphan_note_template(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateNoteTemplateDto,
) -> Result<NoteTemplateDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating note_template")
}
/// Create a new note_template entity as child of owner
pub fn create_note_template(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateNoteTemplateDto,
    owner_id: EntityId,
    index: i32,
) -> Result<NoteTemplateDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating note_template")
}
/// Create multiple note_template entities (orphan, no parent)
pub fn create_orphan_note_template_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateNoteTemplateDto],
) -> Result<Vec<NoteTemplateDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating note_template entities")
}
/// Create multiple note_template entities as children of owner
pub fn create_note_template_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateNoteTemplateDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<NoteTemplateDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating note_template entities")
}
/// Get a note_template entity by ID
pub fn get_note_template(ctx: &AppContext, id: &EntityId) -> Result<Option<NoteTemplateDto>> {
    note_template_controller::get(&ctx.db_context, id).context("getting note_template")
}

/// Get multiple note_template entities by IDs
pub fn get_note_template_multi(
    ctx: &AppContext,
    ids: &[EntityId],
) -> Result<Vec<Option<NoteTemplateDto>>> {
    note_template_controller::get_multi(&ctx.db_context, ids)
        .context("getting note_template entities")
}

/// Get all note_template entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_note_template(ctx: &AppContext) -> Result<Vec<NoteTemplateDto>> {
    note_template_controller::get_all(&ctx.db_context).context("getting all note_template entities")
}

/// Update a note_template entity
pub fn update_note_template(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateNoteTemplateDto,
) -> Result<NoteTemplateDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating note_template")
}

/// Update multiple note_template entities
pub fn update_note_template_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateNoteTemplateDto],
) -> Result<Vec<NoteTemplateDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating note_template entities")
}

/// Update a note_template entity with relationships
pub fn update_note_template_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &NoteTemplateDto,
) -> Result<NoteTemplateDto> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating note_template with relationships")
}

/// Update multiple note_template entities with relationships
pub fn update_note_template_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[NoteTemplateDto],
) -> Result<Vec<NoteTemplateDto>> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating note_template entities with relationships")
}

/// Remove a note_template entity by ID
pub fn remove_note_template(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing note_template")
}

/// Remove multiple note_template entities by IDs
pub fn remove_note_template_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = common::long_operation::lock_or_recover(&ctx.undo_redo_manager);
    note_template_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing note_template entities")
}