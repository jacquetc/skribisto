// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from feature_use_case_uow.tera

use crate::use_cases::import_note_templates_uc::{
    ImportNoteTemplatesUnitOfWorkFactoryTrait, ImportNoteTemplatesUnitOfWorkTrait,
};
use anyhow::{Ok, Result};
use common::database::CommandUnitOfWork;
use common::database::write_guard::WriteTransactionGuard;
use common::database::{db_context::DbContext, transactions::Transaction};
#[allow(unused_imports)]
use common::entities::{NoteTemplate, Work};
use common::event::NoteTemplateManagementEvent::ImportNoteTemplates;
use common::event::{AllEvent, DirectAccessEntity, Event, EventBuffer, EventHub, Origin};
#[allow(unused_imports)]
use common::types;
#[allow(unused_imports)]
use common::types::EntityId;
use std::cell::RefCell;
use std::sync::Arc;

// Unit of work for ImportNoteTemplates

pub struct ImportNoteTemplatesUnitOfWork {
    context: DbContext,
    transaction: Option<Transaction>,
    // RAII: acquired in `begin_transaction`, released via `commit`/`rollback`
    // clearing it — see `common::database::write_guard`'s module doc for the
    // single-write-transaction-per-store invariant this enforces.
    write_guard: Option<WriteTransactionGuard>,
    event_hub: Arc<EventHub>,
    event_buffer: RefCell<EventBuffer>,
}

impl ImportNoteTemplatesUnitOfWork {
    pub fn new(db_context: &DbContext, event_hub: &Arc<EventHub>) -> Self {
        ImportNoteTemplatesUnitOfWork {
            context: db_context.clone(),
            transaction: None,
            write_guard: None,
            event_hub: event_hub.clone(),
            event_buffer: RefCell::new(EventBuffer::new()),
        }
    }
}

impl CommandUnitOfWork for ImportNoteTemplatesUnitOfWork {
    fn begin_transaction(&mut self) -> Result<()> {
        // Acquire BEFORE the write transaction itself — see
        // `common::database::write_guard`'s module doc.
        self.write_guard = Some(WriteTransactionGuard::acquire(
            &self.context,
            "import_note_templates",
        )?);
        self.transaction = Some(Transaction::begin_write_transaction(&self.context)?);
        self.event_buffer.get_mut().begin_buffering();
        Ok(())
    }

    fn commit(&mut self) -> Result<()> {
        self.transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .commit()?;
        self.write_guard = None;
        for event in self.event_buffer.get_mut().flush() {
            self.event_hub.send_event(event);
        }
        Ok(())
    }

    fn rollback(&mut self) -> Result<()> {
        self.transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .rollback()?;
        self.write_guard = None;
        self.event_buffer.get_mut().discard();
        Ok(())
    }

    fn create_savepoint(&self) -> Result<types::Savepoint> {
        self.transaction
            .as_ref()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .create_savepoint()
    }

    fn restore_to_savepoint(&mut self, savepoint: types::Savepoint) -> Result<()> {
        let mut transaction = self
            .transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?;
        transaction.restore_to_savepoint(savepoint)?;

        // Discard buffered events — savepoint restore invalidated them
        self.event_buffer.get_mut().discard();

        // Send Reset immediately (not buffered — UI must refresh now)
        self.event_hub.send_event(Event {
            origin: Origin::DirectAccess(DirectAccessEntity::All(AllEvent::Reset)),
            ids: vec![],
            data: None,
        });

        // Recreate the transaction after restoring to savepoint
        self.transaction = Some(transaction);

        Ok(())
    }
}

// Hand-trimmed to exactly the actions `import_note_templates_uc` uses. The generator's
// scaffold re-renders the full generic Get/GetMulti/Snapshot/Restore set, which would
// silently replace this list and break compilation — the same standing hazard
// `import_tags_uow` documents. Exactly the same macros must be set on the trait in
// ../use_cases/import_note_templates_uc.rs.
#[macros::uow_action(entity = "Work", action = "GetAll")]
#[macros::uow_action(entity = "Work", action = "GetRelationship")]
#[macros::uow_action(entity = "Work", action = "SetRelationship")]
#[macros::uow_action(entity = "Work", action = "Snapshot")]
#[macros::uow_action(entity = "Work", action = "Restore")]
#[macros::uow_action(entity = "NoteTemplate", action = "GetMulti")]
#[macros::uow_action(entity = "NoteTemplate", action = "CreateOrphan")]
impl ImportNoteTemplatesUnitOfWorkTrait for ImportNoteTemplatesUnitOfWork {
    fn publish_import_note_templates_event(&self, ids: Vec<EntityId>, data: Option<String>) {
        self.event_hub.send_event(Event {
            origin: Origin::NoteTemplateManagement(ImportNoteTemplates),
            ids,
            data,
        });
    }
}

pub struct ImportNoteTemplatesUnitOfWorkFactory {
    context: DbContext,
    event_hub: Arc<EventHub>,
}

impl ImportNoteTemplatesUnitOfWorkFactory {
    pub fn new(db_context: &DbContext, event_hub: &Arc<EventHub>) -> Self {
        ImportNoteTemplatesUnitOfWorkFactory {
            context: db_context.clone(),
            event_hub: event_hub.clone(),
        }
    }
}

impl ImportNoteTemplatesUnitOfWorkFactoryTrait for ImportNoteTemplatesUnitOfWorkFactory {
    fn create(&self) -> Box<dyn ImportNoteTemplatesUnitOfWorkTrait> {
        Box::new(ImportNoteTemplatesUnitOfWork::new(
            &self.context,
            &self.event_hub,
        ))
    }
}
