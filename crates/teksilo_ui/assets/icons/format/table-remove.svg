<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round">
  <!-- Remove table — the same header-ruled table as `table-insert.svg`, marked
       with the crossed-out badge `table-col-delete` / `table-row-delete` put on
       whatever goes. A full-length diagonal instead ran through the point where
       both grid dividers already crossed: three strokes through one spot. -->
  <rect x="2.6" y="3" width="10.8" height="10" rx="1.2"/>
  <path d="M2.6 6.2 H13.4"/>
  <path d="M6.4 8.1 L9.6 11.3"/>
  <path d="M9.6 8.1 L6.4 11.3"/>
</svg>
