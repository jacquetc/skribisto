// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.8.0 from entity_dtos.tera

use common::entities::Holiday;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct HolidayDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub label: String,
    pub start_date: chrono::DateTime<chrono::Utc>,
    pub end_date: Option<chrono::DateTime<chrono::Utc>>,
}

impl From<HolidayDto> for Holiday {
    fn from(dto: HolidayDto) -> Self {
        Holiday {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            label: dto.label,
            start_date: dto.start_date,
            end_date: dto.end_date,
        }
    }
}

impl From<&HolidayDto> for Holiday {
    fn from(dto: &HolidayDto) -> Self {
        Holiday {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            label: dto.label.clone(),
            start_date: dto.start_date.clone(),
            end_date: dto.end_date.clone(),
        }
    }
}

impl From<Holiday> for HolidayDto {
    fn from(entity: Holiday) -> Self {
        HolidayDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            label: entity.label,
            start_date: entity.start_date,
            end_date: entity.end_date,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateHolidayDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub label: String,
    pub start_date: chrono::DateTime<chrono::Utc>,
    pub end_date: Option<chrono::DateTime<chrono::Utc>>,
}

impl From<CreateHolidayDto> for Holiday {
    fn from(dto: CreateHolidayDto) -> Self {
        Holiday {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            label: dto.label,
            start_date: dto.start_date,
            end_date: dto.end_date,
        }
    }
}

impl From<&CreateHolidayDto> for Holiday {
    fn from(dto: &CreateHolidayDto) -> Self {
        Holiday {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            label: dto.label.clone(),
            start_date: dto.start_date.clone(),
            end_date: dto.end_date.clone(),
        }
    }
}

impl From<Holiday> for CreateHolidayDto {
    fn from(entity: Holiday) -> Self {
        CreateHolidayDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            label: entity.label,
            start_date: entity.start_date,
            end_date: entity.end_date,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateHolidayDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub label: String,
    pub start_date: chrono::DateTime<chrono::Utc>,
    pub end_date: Option<chrono::DateTime<chrono::Utc>>,
}

impl From<UpdateHolidayDto> for Holiday {
    fn from(dto: UpdateHolidayDto) -> Self {
        Holiday {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            label: dto.label,
            start_date: dto.start_date,
            end_date: dto.end_date,
        }
    }
}

impl From<&UpdateHolidayDto> for Holiday {
    fn from(dto: &UpdateHolidayDto) -> Self {
        Holiday {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            label: dto.label.clone(),
            start_date: dto.start_date.clone(),
            end_date: dto.end_date.clone(),
        }
    }
}

impl From<Holiday> for UpdateHolidayDto {
    fn from(entity: Holiday) -> Self {
        UpdateHolidayDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            label: entity.label,
            start_date: entity.start_date,
            end_date: entity.end_date,
        }
    }
}

impl From<HolidayDto> for UpdateHolidayDto {
    fn from(dto: HolidayDto) -> Self {
        UpdateHolidayDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            label: dto.label,
            start_date: dto.start_date,
            end_date: dto.end_date,
        }
    }
}
