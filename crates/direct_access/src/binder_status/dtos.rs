// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.10.0 from entity_dtos.tera

use common::entities::BinderStatus;
pub use common::entities::StatusCategory;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct BinderStatusDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub uid: uuid::Uuid,
    pub name: String,
    pub category: StatusCategory,
    pub details: String,
}

impl From<BinderStatusDto> for BinderStatus {
    fn from(dto: BinderStatusDto) -> Self {
        BinderStatus {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            uid: dto.uid,
            name: dto.name,
            category: dto.category,
            details: dto.details,
        }
    }
}

impl From<&BinderStatusDto> for BinderStatus {
    fn from(dto: &BinderStatusDto) -> Self {
        BinderStatus {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            uid: dto.uid.clone(),
            name: dto.name.clone(),
            category: dto.category.clone(),
            details: dto.details.clone(),
        }
    }
}

impl From<BinderStatus> for BinderStatusDto {
    fn from(entity: BinderStatus) -> Self {
        BinderStatusDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            uid: entity.uid,
            name: entity.name,
            category: entity.category,
            details: entity.details,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateBinderStatusDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub uid: uuid::Uuid,
    pub name: String,
    pub category: StatusCategory,
    pub details: String,
}

impl From<CreateBinderStatusDto> for BinderStatus {
    fn from(dto: CreateBinderStatusDto) -> Self {
        BinderStatus {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            uid: dto.uid,
            name: dto.name,
            category: dto.category,
            details: dto.details,
        }
    }
}

impl From<&CreateBinderStatusDto> for BinderStatus {
    fn from(dto: &CreateBinderStatusDto) -> Self {
        BinderStatus {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            uid: dto.uid.clone(),
            name: dto.name.clone(),
            category: dto.category.clone(),
            details: dto.details.clone(),
        }
    }
}

impl From<BinderStatus> for CreateBinderStatusDto {
    fn from(entity: BinderStatus) -> Self {
        CreateBinderStatusDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            uid: entity.uid,
            name: entity.name,
            category: entity.category,
            details: entity.details,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateBinderStatusDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub uid: uuid::Uuid,
    pub name: String,
    pub category: StatusCategory,
    pub details: String,
}

impl From<UpdateBinderStatusDto> for BinderStatus {
    fn from(dto: UpdateBinderStatusDto) -> Self {
        BinderStatus {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            uid: dto.uid,
            name: dto.name,
            category: dto.category,
            details: dto.details,
        }
    }
}

impl From<&UpdateBinderStatusDto> for BinderStatus {
    fn from(dto: &UpdateBinderStatusDto) -> Self {
        BinderStatus {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            uid: dto.uid.clone(),
            name: dto.name.clone(),
            category: dto.category.clone(),
            details: dto.details.clone(),
        }
    }
}

impl From<BinderStatus> for UpdateBinderStatusDto {
    fn from(entity: BinderStatus) -> Self {
        UpdateBinderStatusDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            uid: entity.uid,
            name: entity.name,
            category: entity.category,
            details: entity.details,
        }
    }
}

impl From<BinderStatusDto> for UpdateBinderStatusDto {
    fn from(dto: BinderStatusDto) -> Self {
        UpdateBinderStatusDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            uid: dto.uid,
            name: dto.name,
            category: dto.category,
            details: dto.details,
        }
    }
}
