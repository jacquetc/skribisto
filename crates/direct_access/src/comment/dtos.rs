// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from entity_dtos.tera

use common::entities::Comment;
pub use common::entities::CommentAnchorKind;
pub use common::entities::CommentOrphanReason;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CommentDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub content: Option<EntityId>,
    pub kind: CommentAnchorKind,
    pub author_name: String,
    pub body: String,
    pub resolved: bool,
    pub orphaned: bool,
    pub orphan_reason: CommentOrphanReason,
    pub range_start: u64,
    pub range_length: u64,
    pub quote_prefix: String,
    pub quote_exact: String,
    pub quote_exact_truncated: bool,
    pub quote_suffix: String,
    pub block_ordinal_hint: u64,
    pub replies: Vec<EntityId>,
}

impl From<CommentDto> for Comment {
    fn from(dto: CommentDto) -> Self {
        Comment {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            content: dto.content,
            kind: dto.kind,
            author_name: dto.author_name,
            body: dto.body,
            resolved: dto.resolved,
            orphaned: dto.orphaned,
            orphan_reason: dto.orphan_reason,
            range_start: dto.range_start,
            range_length: dto.range_length,
            quote_prefix: dto.quote_prefix,
            quote_exact: dto.quote_exact,
            quote_exact_truncated: dto.quote_exact_truncated,
            quote_suffix: dto.quote_suffix,
            block_ordinal_hint: dto.block_ordinal_hint,
            replies: dto.replies,
        }
    }
}

impl From<&CommentDto> for Comment {
    fn from(dto: &CommentDto) -> Self {
        Comment {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            content: dto.content.clone(),
            kind: dto.kind.clone(),
            author_name: dto.author_name.clone(),
            body: dto.body.clone(),
            resolved: dto.resolved.clone(),
            orphaned: dto.orphaned.clone(),
            orphan_reason: dto.orphan_reason.clone(),
            range_start: dto.range_start.clone(),
            range_length: dto.range_length.clone(),
            quote_prefix: dto.quote_prefix.clone(),
            quote_exact: dto.quote_exact.clone(),
            quote_exact_truncated: dto.quote_exact_truncated.clone(),
            quote_suffix: dto.quote_suffix.clone(),
            block_ordinal_hint: dto.block_ordinal_hint.clone(),
            replies: dto.replies.clone(),
        }
    }
}

impl From<Comment> for CommentDto {
    fn from(entity: Comment) -> Self {
        CommentDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            content: entity.content,
            kind: entity.kind,
            author_name: entity.author_name,
            body: entity.body,
            resolved: entity.resolved,
            orphaned: entity.orphaned,
            orphan_reason: entity.orphan_reason,
            range_start: entity.range_start,
            range_length: entity.range_length,
            quote_prefix: entity.quote_prefix,
            quote_exact: entity.quote_exact,
            quote_exact_truncated: entity.quote_exact_truncated,
            quote_suffix: entity.quote_suffix,
            block_ordinal_hint: entity.block_ordinal_hint,
            replies: entity.replies,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateCommentDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub content: Option<EntityId>,
    pub kind: CommentAnchorKind,
    pub author_name: String,
    pub body: String,
    pub resolved: bool,
    pub orphaned: bool,
    pub orphan_reason: CommentOrphanReason,
    pub range_start: u64,
    pub range_length: u64,
    pub quote_prefix: String,
    pub quote_exact: String,
    pub quote_exact_truncated: bool,
    pub quote_suffix: String,
    pub block_ordinal_hint: u64,
    pub replies: Vec<EntityId>,
}

impl From<CreateCommentDto> for Comment {
    fn from(dto: CreateCommentDto) -> Self {
        Comment {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            content: dto.content,
            kind: dto.kind,
            author_name: dto.author_name,
            body: dto.body,
            resolved: dto.resolved,
            orphaned: dto.orphaned,
            orphan_reason: dto.orphan_reason,
            range_start: dto.range_start,
            range_length: dto.range_length,
            quote_prefix: dto.quote_prefix,
            quote_exact: dto.quote_exact,
            quote_exact_truncated: dto.quote_exact_truncated,
            quote_suffix: dto.quote_suffix,
            block_ordinal_hint: dto.block_ordinal_hint,
            replies: dto.replies,
        }
    }
}

impl From<&CreateCommentDto> for Comment {
    fn from(dto: &CreateCommentDto) -> Self {
        Comment {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            content: dto.content.clone(),
            kind: dto.kind.clone(),
            author_name: dto.author_name.clone(),
            body: dto.body.clone(),
            resolved: dto.resolved.clone(),
            orphaned: dto.orphaned.clone(),
            orphan_reason: dto.orphan_reason.clone(),
            range_start: dto.range_start.clone(),
            range_length: dto.range_length.clone(),
            quote_prefix: dto.quote_prefix.clone(),
            quote_exact: dto.quote_exact.clone(),
            quote_exact_truncated: dto.quote_exact_truncated.clone(),
            quote_suffix: dto.quote_suffix.clone(),
            block_ordinal_hint: dto.block_ordinal_hint.clone(),
            replies: dto.replies.clone(),
        }
    }
}

impl From<Comment> for CreateCommentDto {
    fn from(entity: Comment) -> Self {
        CreateCommentDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            content: entity.content,
            kind: entity.kind,
            author_name: entity.author_name,
            body: entity.body,
            resolved: entity.resolved,
            orphaned: entity.orphaned,
            orphan_reason: entity.orphan_reason,
            range_start: entity.range_start,
            range_length: entity.range_length,
            quote_prefix: entity.quote_prefix,
            quote_exact: entity.quote_exact,
            quote_exact_truncated: entity.quote_exact_truncated,
            quote_suffix: entity.quote_suffix,
            block_ordinal_hint: entity.block_ordinal_hint,
            replies: entity.replies,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateCommentDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub kind: CommentAnchorKind,
    pub author_name: String,
    pub body: String,
    pub resolved: bool,
    pub orphaned: bool,
    pub orphan_reason: CommentOrphanReason,
    pub range_start: u64,
    pub range_length: u64,
    pub quote_prefix: String,
    pub quote_exact: String,
    pub quote_exact_truncated: bool,
    pub quote_suffix: String,
    pub block_ordinal_hint: u64,
}

impl From<UpdateCommentDto> for Comment {
    fn from(dto: UpdateCommentDto) -> Self {
        Comment {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            kind: dto.kind,
            author_name: dto.author_name,
            body: dto.body,
            resolved: dto.resolved,
            orphaned: dto.orphaned,
            orphan_reason: dto.orphan_reason,
            range_start: dto.range_start,
            range_length: dto.range_length,
            quote_prefix: dto.quote_prefix,
            quote_exact: dto.quote_exact,
            quote_exact_truncated: dto.quote_exact_truncated,
            quote_suffix: dto.quote_suffix,
            block_ordinal_hint: dto.block_ordinal_hint,
            content: Default::default(),
            replies: Default::default(),
        }
    }
}

impl From<&UpdateCommentDto> for Comment {
    fn from(dto: &UpdateCommentDto) -> Self {
        Comment {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            kind: dto.kind.clone(),
            author_name: dto.author_name.clone(),
            body: dto.body.clone(),
            resolved: dto.resolved.clone(),
            orphaned: dto.orphaned.clone(),
            orphan_reason: dto.orphan_reason.clone(),
            range_start: dto.range_start.clone(),
            range_length: dto.range_length.clone(),
            quote_prefix: dto.quote_prefix.clone(),
            quote_exact: dto.quote_exact.clone(),
            quote_exact_truncated: dto.quote_exact_truncated.clone(),
            quote_suffix: dto.quote_suffix.clone(),
            block_ordinal_hint: dto.block_ordinal_hint.clone(),
            content: Default::default(),
            replies: Default::default(),
        }
    }
}

impl From<Comment> for UpdateCommentDto {
    fn from(entity: Comment) -> Self {
        UpdateCommentDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            kind: entity.kind,
            author_name: entity.author_name,
            body: entity.body,
            resolved: entity.resolved,
            orphaned: entity.orphaned,
            orphan_reason: entity.orphan_reason,
            range_start: entity.range_start,
            range_length: entity.range_length,
            quote_prefix: entity.quote_prefix,
            quote_exact: entity.quote_exact,
            quote_exact_truncated: entity.quote_exact_truncated,
            quote_suffix: entity.quote_suffix,
            block_ordinal_hint: entity.block_ordinal_hint,
        }
    }
}

impl From<CommentDto> for UpdateCommentDto {
    fn from(dto: CommentDto) -> Self {
        UpdateCommentDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            kind: dto.kind,
            author_name: dto.author_name,
            body: dto.body,
            resolved: dto.resolved,
            orphaned: dto.orphaned,
            orphan_reason: dto.orphan_reason,
            range_start: dto.range_start,
            range_length: dto.range_length,
            quote_prefix: dto.quote_prefix,
            quote_exact: dto.quote_exact,
            quote_exact_truncated: dto.quote_exact_truncated,
            quote_suffix: dto.quote_suffix,
            block_ordinal_hint: dto.block_ordinal_hint,
        }
    }
}
pub use common::direct_access::comment::CommentRelationshipField;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct CommentRelationshipDto {
    pub id: EntityId,
    pub field: CommentRelationshipField,
    pub right_ids: Vec<EntityId>,
}
