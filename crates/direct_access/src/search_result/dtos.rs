// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.10.0 from entity_dtos.tera

pub use common::entities::MatchField;
use common::entities::SearchResult;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct SearchResultDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub binder_item_id: u64,
    pub item_title: String,
    pub match_field: MatchField,
    pub comment_id: u64,
    pub reply_id: u64,
    pub footnote_id: u64,
    pub occurrence_count: u64,
    pub snippet_before: String,
    pub snippet_match: String,
    pub snippet_after: String,
    pub trashed: bool,
}

impl From<SearchResultDto> for SearchResult {
    fn from(dto: SearchResultDto) -> Self {
        SearchResult {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            binder_item_id: dto.binder_item_id,
            item_title: dto.item_title,
            match_field: dto.match_field,
            comment_id: dto.comment_id,
            reply_id: dto.reply_id,
            footnote_id: dto.footnote_id,
            occurrence_count: dto.occurrence_count,
            snippet_before: dto.snippet_before,
            snippet_match: dto.snippet_match,
            snippet_after: dto.snippet_after,
            trashed: dto.trashed,
        }
    }
}

impl From<&SearchResultDto> for SearchResult {
    fn from(dto: &SearchResultDto) -> Self {
        SearchResult {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            binder_item_id: dto.binder_item_id.clone(),
            item_title: dto.item_title.clone(),
            match_field: dto.match_field.clone(),
            comment_id: dto.comment_id.clone(),
            reply_id: dto.reply_id.clone(),
            footnote_id: dto.footnote_id.clone(),
            occurrence_count: dto.occurrence_count.clone(),
            snippet_before: dto.snippet_before.clone(),
            snippet_match: dto.snippet_match.clone(),
            snippet_after: dto.snippet_after.clone(),
            trashed: dto.trashed.clone(),
        }
    }
}

impl From<SearchResult> for SearchResultDto {
    fn from(entity: SearchResult) -> Self {
        SearchResultDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            binder_item_id: entity.binder_item_id,
            item_title: entity.item_title,
            match_field: entity.match_field,
            comment_id: entity.comment_id,
            reply_id: entity.reply_id,
            footnote_id: entity.footnote_id,
            occurrence_count: entity.occurrence_count,
            snippet_before: entity.snippet_before,
            snippet_match: entity.snippet_match,
            snippet_after: entity.snippet_after,
            trashed: entity.trashed,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateSearchResultDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub binder_item_id: u64,
    pub item_title: String,
    pub match_field: MatchField,
    pub comment_id: u64,
    pub reply_id: u64,
    pub footnote_id: u64,
    pub occurrence_count: u64,
    pub snippet_before: String,
    pub snippet_match: String,
    pub snippet_after: String,
    pub trashed: bool,
}

impl From<CreateSearchResultDto> for SearchResult {
    fn from(dto: CreateSearchResultDto) -> Self {
        SearchResult {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            binder_item_id: dto.binder_item_id,
            item_title: dto.item_title,
            match_field: dto.match_field,
            comment_id: dto.comment_id,
            reply_id: dto.reply_id,
            footnote_id: dto.footnote_id,
            occurrence_count: dto.occurrence_count,
            snippet_before: dto.snippet_before,
            snippet_match: dto.snippet_match,
            snippet_after: dto.snippet_after,
            trashed: dto.trashed,
        }
    }
}

impl From<&CreateSearchResultDto> for SearchResult {
    fn from(dto: &CreateSearchResultDto) -> Self {
        SearchResult {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            binder_item_id: dto.binder_item_id.clone(),
            item_title: dto.item_title.clone(),
            match_field: dto.match_field.clone(),
            comment_id: dto.comment_id.clone(),
            reply_id: dto.reply_id.clone(),
            footnote_id: dto.footnote_id.clone(),
            occurrence_count: dto.occurrence_count.clone(),
            snippet_before: dto.snippet_before.clone(),
            snippet_match: dto.snippet_match.clone(),
            snippet_after: dto.snippet_after.clone(),
            trashed: dto.trashed.clone(),
        }
    }
}

impl From<SearchResult> for CreateSearchResultDto {
    fn from(entity: SearchResult) -> Self {
        CreateSearchResultDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            binder_item_id: entity.binder_item_id,
            item_title: entity.item_title,
            match_field: entity.match_field,
            comment_id: entity.comment_id,
            reply_id: entity.reply_id,
            footnote_id: entity.footnote_id,
            occurrence_count: entity.occurrence_count,
            snippet_before: entity.snippet_before,
            snippet_match: entity.snippet_match,
            snippet_after: entity.snippet_after,
            trashed: entity.trashed,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateSearchResultDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub binder_item_id: u64,
    pub item_title: String,
    pub match_field: MatchField,
    pub comment_id: u64,
    pub reply_id: u64,
    pub footnote_id: u64,
    pub occurrence_count: u64,
    pub snippet_before: String,
    pub snippet_match: String,
    pub snippet_after: String,
    pub trashed: bool,
}

impl From<UpdateSearchResultDto> for SearchResult {
    fn from(dto: UpdateSearchResultDto) -> Self {
        SearchResult {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            binder_item_id: dto.binder_item_id,
            item_title: dto.item_title,
            match_field: dto.match_field,
            comment_id: dto.comment_id,
            reply_id: dto.reply_id,
            footnote_id: dto.footnote_id,
            occurrence_count: dto.occurrence_count,
            snippet_before: dto.snippet_before,
            snippet_match: dto.snippet_match,
            snippet_after: dto.snippet_after,
            trashed: dto.trashed,
        }
    }
}

impl From<&UpdateSearchResultDto> for SearchResult {
    fn from(dto: &UpdateSearchResultDto) -> Self {
        SearchResult {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            binder_item_id: dto.binder_item_id.clone(),
            item_title: dto.item_title.clone(),
            match_field: dto.match_field.clone(),
            comment_id: dto.comment_id.clone(),
            reply_id: dto.reply_id.clone(),
            footnote_id: dto.footnote_id.clone(),
            occurrence_count: dto.occurrence_count.clone(),
            snippet_before: dto.snippet_before.clone(),
            snippet_match: dto.snippet_match.clone(),
            snippet_after: dto.snippet_after.clone(),
            trashed: dto.trashed.clone(),
        }
    }
}

impl From<SearchResult> for UpdateSearchResultDto {
    fn from(entity: SearchResult) -> Self {
        UpdateSearchResultDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            binder_item_id: entity.binder_item_id,
            item_title: entity.item_title,
            match_field: entity.match_field,
            comment_id: entity.comment_id,
            reply_id: entity.reply_id,
            footnote_id: entity.footnote_id,
            occurrence_count: entity.occurrence_count,
            snippet_before: entity.snippet_before,
            snippet_match: entity.snippet_match,
            snippet_after: entity.snippet_after,
            trashed: entity.trashed,
        }
    }
}

impl From<SearchResultDto> for UpdateSearchResultDto {
    fn from(dto: SearchResultDto) -> Self {
        UpdateSearchResultDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            binder_item_id: dto.binder_item_id,
            item_title: dto.item_title,
            match_field: dto.match_field,
            comment_id: dto.comment_id,
            reply_id: dto.reply_id,
            footnote_id: dto.footnote_id,
            occurrence_count: dto.occurrence_count,
            snippet_before: dto.snippet_before,
            snippet_match: dto.snippet_match,
            snippet_after: dto.snippet_after,
            trashed: dto.trashed,
        }
    }
}
