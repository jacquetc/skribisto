// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.8.0 from entity_dtos.tera

use common::entities::Search;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct SearchDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub query: String,
    pub case_sensitive: bool,
    pub whole_word: bool,
    pub diacritic_sensitive: bool,
    pub facets: Vec<i64>,
    pub search_body: bool,
    pub search_titles: bool,
    pub search_synopsis: bool,
    pub search_labels: bool,
    pub include_trashed: bool,
    pub truncated: bool,
    pub results: Vec<EntityId>,
}

impl From<SearchDto> for Search {
    fn from(dto: SearchDto) -> Self {
        Search {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            query: dto.query,
            case_sensitive: dto.case_sensitive,
            whole_word: dto.whole_word,
            diacritic_sensitive: dto.diacritic_sensitive,
            facets: dto.facets,
            search_body: dto.search_body,
            search_titles: dto.search_titles,
            search_synopsis: dto.search_synopsis,
            search_labels: dto.search_labels,
            include_trashed: dto.include_trashed,
            truncated: dto.truncated,
            results: dto.results,
        }
    }
}

impl From<&SearchDto> for Search {
    fn from(dto: &SearchDto) -> Self {
        Search {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            query: dto.query.clone(),
            case_sensitive: dto.case_sensitive.clone(),
            whole_word: dto.whole_word.clone(),
            diacritic_sensitive: dto.diacritic_sensitive.clone(),
            facets: dto.facets.clone(),
            search_body: dto.search_body.clone(),
            search_titles: dto.search_titles.clone(),
            search_synopsis: dto.search_synopsis.clone(),
            search_labels: dto.search_labels.clone(),
            include_trashed: dto.include_trashed.clone(),
            truncated: dto.truncated.clone(),
            results: dto.results.clone(),
        }
    }
}

impl From<Search> for SearchDto {
    fn from(entity: Search) -> Self {
        SearchDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            query: entity.query,
            case_sensitive: entity.case_sensitive,
            whole_word: entity.whole_word,
            diacritic_sensitive: entity.diacritic_sensitive,
            facets: entity.facets,
            search_body: entity.search_body,
            search_titles: entity.search_titles,
            search_synopsis: entity.search_synopsis,
            search_labels: entity.search_labels,
            include_trashed: entity.include_trashed,
            truncated: entity.truncated,
            results: entity.results,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateSearchDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub query: String,
    pub case_sensitive: bool,
    pub whole_word: bool,
    pub diacritic_sensitive: bool,
    pub facets: Vec<i64>,
    pub search_body: bool,
    pub search_titles: bool,
    pub search_synopsis: bool,
    pub search_labels: bool,
    pub include_trashed: bool,
    pub truncated: bool,
    pub results: Vec<EntityId>,
}

impl From<CreateSearchDto> for Search {
    fn from(dto: CreateSearchDto) -> Self {
        Search {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            query: dto.query,
            case_sensitive: dto.case_sensitive,
            whole_word: dto.whole_word,
            diacritic_sensitive: dto.diacritic_sensitive,
            facets: dto.facets,
            search_body: dto.search_body,
            search_titles: dto.search_titles,
            search_synopsis: dto.search_synopsis,
            search_labels: dto.search_labels,
            include_trashed: dto.include_trashed,
            truncated: dto.truncated,
            results: dto.results,
        }
    }
}

impl From<&CreateSearchDto> for Search {
    fn from(dto: &CreateSearchDto) -> Self {
        Search {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            query: dto.query.clone(),
            case_sensitive: dto.case_sensitive.clone(),
            whole_word: dto.whole_word.clone(),
            diacritic_sensitive: dto.diacritic_sensitive.clone(),
            facets: dto.facets.clone(),
            search_body: dto.search_body.clone(),
            search_titles: dto.search_titles.clone(),
            search_synopsis: dto.search_synopsis.clone(),
            search_labels: dto.search_labels.clone(),
            include_trashed: dto.include_trashed.clone(),
            truncated: dto.truncated.clone(),
            results: dto.results.clone(),
        }
    }
}

impl From<Search> for CreateSearchDto {
    fn from(entity: Search) -> Self {
        CreateSearchDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            query: entity.query,
            case_sensitive: entity.case_sensitive,
            whole_word: entity.whole_word,
            diacritic_sensitive: entity.diacritic_sensitive,
            facets: entity.facets,
            search_body: entity.search_body,
            search_titles: entity.search_titles,
            search_synopsis: entity.search_synopsis,
            search_labels: entity.search_labels,
            include_trashed: entity.include_trashed,
            truncated: entity.truncated,
            results: entity.results,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateSearchDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub query: String,
    pub case_sensitive: bool,
    pub whole_word: bool,
    pub diacritic_sensitive: bool,
    pub facets: Vec<i64>,
    pub search_body: bool,
    pub search_titles: bool,
    pub search_synopsis: bool,
    pub search_labels: bool,
    pub include_trashed: bool,
    pub truncated: bool,
}

impl From<UpdateSearchDto> for Search {
    fn from(dto: UpdateSearchDto) -> Self {
        Search {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            query: dto.query,
            case_sensitive: dto.case_sensitive,
            whole_word: dto.whole_word,
            diacritic_sensitive: dto.diacritic_sensitive,
            facets: dto.facets,
            search_body: dto.search_body,
            search_titles: dto.search_titles,
            search_synopsis: dto.search_synopsis,
            search_labels: dto.search_labels,
            include_trashed: dto.include_trashed,
            truncated: dto.truncated,
            results: Default::default(),
        }
    }
}

impl From<&UpdateSearchDto> for Search {
    fn from(dto: &UpdateSearchDto) -> Self {
        Search {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            query: dto.query.clone(),
            case_sensitive: dto.case_sensitive.clone(),
            whole_word: dto.whole_word.clone(),
            diacritic_sensitive: dto.diacritic_sensitive.clone(),
            facets: dto.facets.clone(),
            search_body: dto.search_body.clone(),
            search_titles: dto.search_titles.clone(),
            search_synopsis: dto.search_synopsis.clone(),
            search_labels: dto.search_labels.clone(),
            include_trashed: dto.include_trashed.clone(),
            truncated: dto.truncated.clone(),
            results: Default::default(),
        }
    }
}

impl From<Search> for UpdateSearchDto {
    fn from(entity: Search) -> Self {
        UpdateSearchDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            query: entity.query,
            case_sensitive: entity.case_sensitive,
            whole_word: entity.whole_word,
            diacritic_sensitive: entity.diacritic_sensitive,
            facets: entity.facets,
            search_body: entity.search_body,
            search_titles: entity.search_titles,
            search_synopsis: entity.search_synopsis,
            search_labels: entity.search_labels,
            include_trashed: entity.include_trashed,
            truncated: entity.truncated,
        }
    }
}

impl From<SearchDto> for UpdateSearchDto {
    fn from(dto: SearchDto) -> Self {
        UpdateSearchDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            query: dto.query,
            case_sensitive: dto.case_sensitive,
            whole_word: dto.whole_word,
            diacritic_sensitive: dto.diacritic_sensitive,
            facets: dto.facets,
            search_body: dto.search_body,
            search_titles: dto.search_titles,
            search_synopsis: dto.search_synopsis,
            search_labels: dto.search_labels,
            include_trashed: dto.include_trashed,
            truncated: dto.truncated,
        }
    }
}
pub use common::direct_access::search::SearchRelationshipField;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SearchRelationshipDto {
    pub id: EntityId,
    pub field: SearchRelationshipField,
    pub right_ids: Vec<EntityId>,
}
