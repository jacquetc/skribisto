// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.8.0 from entity_dtos.tera

use common::entities::Binder;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct BinderDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub activated: bool,
    pub binder_items: Vec<EntityId>,
}

impl From<BinderDto> for Binder {
    fn from(dto: BinderDto) -> Self {
        Binder {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            activated: dto.activated,
            binder_items: dto.binder_items,
        }
    }
}

impl From<&BinderDto> for Binder {
    fn from(dto: &BinderDto) -> Self {
        Binder {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            activated: dto.activated.clone(),
            binder_items: dto.binder_items.clone(),
        }
    }
}

impl From<Binder> for BinderDto {
    fn from(entity: Binder) -> Self {
        BinderDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            activated: entity.activated,
            binder_items: entity.binder_items,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateBinderDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub activated: bool,
    pub binder_items: Vec<EntityId>,
}

impl From<CreateBinderDto> for Binder {
    fn from(dto: CreateBinderDto) -> Self {
        Binder {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            activated: dto.activated,
            binder_items: dto.binder_items,
        }
    }
}

impl From<&CreateBinderDto> for Binder {
    fn from(dto: &CreateBinderDto) -> Self {
        Binder {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            activated: dto.activated.clone(),
            binder_items: dto.binder_items.clone(),
        }
    }
}

impl From<Binder> for CreateBinderDto {
    fn from(entity: Binder) -> Self {
        CreateBinderDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            activated: entity.activated,
            binder_items: entity.binder_items,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateBinderDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub activated: bool,
}

impl From<UpdateBinderDto> for Binder {
    fn from(dto: UpdateBinderDto) -> Self {
        Binder {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            activated: dto.activated,
            binder_items: Default::default(),
        }
    }
}

impl From<&UpdateBinderDto> for Binder {
    fn from(dto: &UpdateBinderDto) -> Self {
        Binder {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            activated: dto.activated.clone(),
            binder_items: Default::default(),
        }
    }
}

impl From<Binder> for UpdateBinderDto {
    fn from(entity: Binder) -> Self {
        UpdateBinderDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            activated: entity.activated,
        }
    }
}

impl From<BinderDto> for UpdateBinderDto {
    fn from(dto: BinderDto) -> Self {
        UpdateBinderDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            activated: dto.activated,
        }
    }
}
pub use common::direct_access::binder::BinderRelationshipField;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct BinderRelationshipDto {
    pub id: EntityId,
    pub field: BinderRelationshipField,
    pub right_ids: Vec<EntityId>,
}
