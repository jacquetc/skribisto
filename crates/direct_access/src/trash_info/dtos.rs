// Generated by Qleany v1.8.0 from entity_dtos.tera

use common::entities::TrashInfo;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct TrashInfoDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub trashed_at: chrono::DateTime<chrono::Utc>,
    pub origin_binder_id: i64,
    pub trashed_binder: Option<EntityId>,
    pub trashed_binder_item: Option<EntityId>,
}

impl From<TrashInfoDto> for TrashInfo {
    fn from(dto: TrashInfoDto) -> Self {
        TrashInfo {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            trashed_at: dto.trashed_at,
            origin_binder_id: dto.origin_binder_id,
            trashed_binder: dto.trashed_binder,
            trashed_binder_item: dto.trashed_binder_item,
        }
    }
}

impl From<&TrashInfoDto> for TrashInfo {
    fn from(dto: &TrashInfoDto) -> Self {
        TrashInfo {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            trashed_at: dto.trashed_at.clone(),
            origin_binder_id: dto.origin_binder_id.clone(),
            trashed_binder: dto.trashed_binder.clone(),
            trashed_binder_item: dto.trashed_binder_item.clone(),
        }
    }
}

impl From<TrashInfo> for TrashInfoDto {
    fn from(entity: TrashInfo) -> Self {
        TrashInfoDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            trashed_at: entity.trashed_at,
            origin_binder_id: entity.origin_binder_id,
            trashed_binder: entity.trashed_binder,
            trashed_binder_item: entity.trashed_binder_item,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateTrashInfoDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub trashed_at: chrono::DateTime<chrono::Utc>,
    pub origin_binder_id: i64,
    pub trashed_binder: Option<EntityId>,
    pub trashed_binder_item: Option<EntityId>,
}

impl From<CreateTrashInfoDto> for TrashInfo {
    fn from(dto: CreateTrashInfoDto) -> Self {
        TrashInfo {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            trashed_at: dto.trashed_at,
            origin_binder_id: dto.origin_binder_id,
            trashed_binder: dto.trashed_binder,
            trashed_binder_item: dto.trashed_binder_item,
        }
    }
}

impl From<&CreateTrashInfoDto> for TrashInfo {
    fn from(dto: &CreateTrashInfoDto) -> Self {
        TrashInfo {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            trashed_at: dto.trashed_at.clone(),
            origin_binder_id: dto.origin_binder_id.clone(),
            trashed_binder: dto.trashed_binder.clone(),
            trashed_binder_item: dto.trashed_binder_item.clone(),
        }
    }
}

impl From<TrashInfo> for CreateTrashInfoDto {
    fn from(entity: TrashInfo) -> Self {
        CreateTrashInfoDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            trashed_at: entity.trashed_at,
            origin_binder_id: entity.origin_binder_id,
            trashed_binder: entity.trashed_binder,
            trashed_binder_item: entity.trashed_binder_item,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateTrashInfoDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub trashed_at: chrono::DateTime<chrono::Utc>,
    pub origin_binder_id: i64,
}

impl From<UpdateTrashInfoDto> for TrashInfo {
    fn from(dto: UpdateTrashInfoDto) -> Self {
        TrashInfo {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            trashed_at: dto.trashed_at,
            origin_binder_id: dto.origin_binder_id,
            trashed_binder: Default::default(),
            trashed_binder_item: Default::default(),
        }
    }
}

impl From<&UpdateTrashInfoDto> for TrashInfo {
    fn from(dto: &UpdateTrashInfoDto) -> Self {
        TrashInfo {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            trashed_at: dto.trashed_at.clone(),
            origin_binder_id: dto.origin_binder_id.clone(),
            trashed_binder: Default::default(),
            trashed_binder_item: Default::default(),
        }
    }
}

impl From<TrashInfo> for UpdateTrashInfoDto {
    fn from(entity: TrashInfo) -> Self {
        UpdateTrashInfoDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            trashed_at: entity.trashed_at,
            origin_binder_id: entity.origin_binder_id,
        }
    }
}

impl From<TrashInfoDto> for UpdateTrashInfoDto {
    fn from(dto: TrashInfoDto) -> Self {
        UpdateTrashInfoDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            trashed_at: dto.trashed_at,
            origin_binder_id: dto.origin_binder_id,
        }
    }
}
pub use common::direct_access::trash_info::TrashInfoRelationshipField;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct TrashInfoRelationshipDto {
    pub id: EntityId,
    pub field: TrashInfoRelationshipField,
    pub right_ids: Vec<EntityId>,
}
