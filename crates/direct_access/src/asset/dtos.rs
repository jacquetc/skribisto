// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.10.0 from entity_dtos.tera

use common::entities::Asset;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct AssetDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub content_hash: String,
    pub file_name: String,
    pub mime_type: String,
    pub width: u64,
    pub height: u64,
    pub byte_size: u64,
    pub alt: String,
    pub is_cover: bool,
}

impl From<AssetDto> for Asset {
    fn from(dto: AssetDto) -> Self {
        Asset {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            content_hash: dto.content_hash,
            file_name: dto.file_name,
            mime_type: dto.mime_type,
            width: dto.width,
            height: dto.height,
            byte_size: dto.byte_size,
            alt: dto.alt,
            is_cover: dto.is_cover,
        }
    }
}

impl From<&AssetDto> for Asset {
    fn from(dto: &AssetDto) -> Self {
        Asset {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            content_hash: dto.content_hash.clone(),
            file_name: dto.file_name.clone(),
            mime_type: dto.mime_type.clone(),
            width: dto.width.clone(),
            height: dto.height.clone(),
            byte_size: dto.byte_size.clone(),
            alt: dto.alt.clone(),
            is_cover: dto.is_cover.clone(),
        }
    }
}

impl From<Asset> for AssetDto {
    fn from(entity: Asset) -> Self {
        AssetDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            content_hash: entity.content_hash,
            file_name: entity.file_name,
            mime_type: entity.mime_type,
            width: entity.width,
            height: entity.height,
            byte_size: entity.byte_size,
            alt: entity.alt,
            is_cover: entity.is_cover,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateAssetDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub content_hash: String,
    pub file_name: String,
    pub mime_type: String,
    pub width: u64,
    pub height: u64,
    pub byte_size: u64,
    pub alt: String,
    pub is_cover: bool,
}

impl From<CreateAssetDto> for Asset {
    fn from(dto: CreateAssetDto) -> Self {
        Asset {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            content_hash: dto.content_hash,
            file_name: dto.file_name,
            mime_type: dto.mime_type,
            width: dto.width,
            height: dto.height,
            byte_size: dto.byte_size,
            alt: dto.alt,
            is_cover: dto.is_cover,
        }
    }
}

impl From<&CreateAssetDto> for Asset {
    fn from(dto: &CreateAssetDto) -> Self {
        Asset {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            content_hash: dto.content_hash.clone(),
            file_name: dto.file_name.clone(),
            mime_type: dto.mime_type.clone(),
            width: dto.width.clone(),
            height: dto.height.clone(),
            byte_size: dto.byte_size.clone(),
            alt: dto.alt.clone(),
            is_cover: dto.is_cover.clone(),
        }
    }
}

impl From<Asset> for CreateAssetDto {
    fn from(entity: Asset) -> Self {
        CreateAssetDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            content_hash: entity.content_hash,
            file_name: entity.file_name,
            mime_type: entity.mime_type,
            width: entity.width,
            height: entity.height,
            byte_size: entity.byte_size,
            alt: entity.alt,
            is_cover: entity.is_cover,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateAssetDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub content_hash: String,
    pub file_name: String,
    pub mime_type: String,
    pub width: u64,
    pub height: u64,
    pub byte_size: u64,
    pub alt: String,
    pub is_cover: bool,
}

impl From<UpdateAssetDto> for Asset {
    fn from(dto: UpdateAssetDto) -> Self {
        Asset {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            content_hash: dto.content_hash,
            file_name: dto.file_name,
            mime_type: dto.mime_type,
            width: dto.width,
            height: dto.height,
            byte_size: dto.byte_size,
            alt: dto.alt,
            is_cover: dto.is_cover,
        }
    }
}

impl From<&UpdateAssetDto> for Asset {
    fn from(dto: &UpdateAssetDto) -> Self {
        Asset {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            content_hash: dto.content_hash.clone(),
            file_name: dto.file_name.clone(),
            mime_type: dto.mime_type.clone(),
            width: dto.width.clone(),
            height: dto.height.clone(),
            byte_size: dto.byte_size.clone(),
            alt: dto.alt.clone(),
            is_cover: dto.is_cover.clone(),
        }
    }
}

impl From<Asset> for UpdateAssetDto {
    fn from(entity: Asset) -> Self {
        UpdateAssetDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            content_hash: entity.content_hash,
            file_name: entity.file_name,
            mime_type: entity.mime_type,
            width: entity.width,
            height: entity.height,
            byte_size: entity.byte_size,
            alt: entity.alt,
            is_cover: entity.is_cover,
        }
    }
}

impl From<AssetDto> for UpdateAssetDto {
    fn from(dto: AssetDto) -> Self {
        UpdateAssetDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            content_hash: dto.content_hash,
            file_name: dto.file_name,
            mime_type: dto.mime_type,
            width: dto.width,
            height: dto.height,
            byte_size: dto.byte_size,
            alt: dto.alt,
            is_cover: dto.is_cover,
        }
    }
}
