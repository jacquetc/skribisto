// Generated by Qleany v1.8.0 from entity_dtos.tera

use common::entities::ProgressSnapshot;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct ProgressSnapshotDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub day: chrono::DateTime<chrono::Utc>,
    pub total_word_count: i64,
    pub total_char_count: Option<i64>,
    pub book_item_ids: Vec<u64>,
    pub book_word_counts: Vec<i64>,
}

impl From<ProgressSnapshotDto> for ProgressSnapshot {
    fn from(dto: ProgressSnapshotDto) -> Self {
        ProgressSnapshot {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            day: dto.day,
            total_word_count: dto.total_word_count,
            total_char_count: dto.total_char_count,
            book_item_ids: dto.book_item_ids,
            book_word_counts: dto.book_word_counts,
        }
    }
}

impl From<&ProgressSnapshotDto> for ProgressSnapshot {
    fn from(dto: &ProgressSnapshotDto) -> Self {
        ProgressSnapshot {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            day: dto.day.clone(),
            total_word_count: dto.total_word_count.clone(),
            total_char_count: dto.total_char_count.clone(),
            book_item_ids: dto.book_item_ids.clone(),
            book_word_counts: dto.book_word_counts.clone(),
        }
    }
}

impl From<ProgressSnapshot> for ProgressSnapshotDto {
    fn from(entity: ProgressSnapshot) -> Self {
        ProgressSnapshotDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            day: entity.day,
            total_word_count: entity.total_word_count,
            total_char_count: entity.total_char_count,
            book_item_ids: entity.book_item_ids,
            book_word_counts: entity.book_word_counts,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateProgressSnapshotDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub day: chrono::DateTime<chrono::Utc>,
    pub total_word_count: i64,
    pub total_char_count: Option<i64>,
    pub book_item_ids: Vec<u64>,
    pub book_word_counts: Vec<i64>,
}

impl From<CreateProgressSnapshotDto> for ProgressSnapshot {
    fn from(dto: CreateProgressSnapshotDto) -> Self {
        ProgressSnapshot {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            day: dto.day,
            total_word_count: dto.total_word_count,
            total_char_count: dto.total_char_count,
            book_item_ids: dto.book_item_ids,
            book_word_counts: dto.book_word_counts,
        }
    }
}

impl From<&CreateProgressSnapshotDto> for ProgressSnapshot {
    fn from(dto: &CreateProgressSnapshotDto) -> Self {
        ProgressSnapshot {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            day: dto.day.clone(),
            total_word_count: dto.total_word_count.clone(),
            total_char_count: dto.total_char_count.clone(),
            book_item_ids: dto.book_item_ids.clone(),
            book_word_counts: dto.book_word_counts.clone(),
        }
    }
}

impl From<ProgressSnapshot> for CreateProgressSnapshotDto {
    fn from(entity: ProgressSnapshot) -> Self {
        CreateProgressSnapshotDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            day: entity.day,
            total_word_count: entity.total_word_count,
            total_char_count: entity.total_char_count,
            book_item_ids: entity.book_item_ids,
            book_word_counts: entity.book_word_counts,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateProgressSnapshotDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub day: chrono::DateTime<chrono::Utc>,
    pub total_word_count: i64,
    pub total_char_count: Option<i64>,
    pub book_item_ids: Vec<u64>,
    pub book_word_counts: Vec<i64>,
}

impl From<UpdateProgressSnapshotDto> for ProgressSnapshot {
    fn from(dto: UpdateProgressSnapshotDto) -> Self {
        ProgressSnapshot {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            day: dto.day,
            total_word_count: dto.total_word_count,
            total_char_count: dto.total_char_count,
            book_item_ids: dto.book_item_ids,
            book_word_counts: dto.book_word_counts,
        }
    }
}

impl From<&UpdateProgressSnapshotDto> for ProgressSnapshot {
    fn from(dto: &UpdateProgressSnapshotDto) -> Self {
        ProgressSnapshot {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            day: dto.day.clone(),
            total_word_count: dto.total_word_count.clone(),
            total_char_count: dto.total_char_count.clone(),
            book_item_ids: dto.book_item_ids.clone(),
            book_word_counts: dto.book_word_counts.clone(),
        }
    }
}

impl From<ProgressSnapshot> for UpdateProgressSnapshotDto {
    fn from(entity: ProgressSnapshot) -> Self {
        UpdateProgressSnapshotDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            day: entity.day,
            total_word_count: entity.total_word_count,
            total_char_count: entity.total_char_count,
            book_item_ids: entity.book_item_ids,
            book_word_counts: entity.book_word_counts,
        }
    }
}

impl From<ProgressSnapshotDto> for UpdateProgressSnapshotDto {
    fn from(dto: ProgressSnapshotDto) -> Self {
        UpdateProgressSnapshotDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            day: dto.day,
            total_word_count: dto.total_word_count,
            total_char_count: dto.total_char_count,
            book_item_ids: dto.book_item_ids,
            book_word_counts: dto.book_word_counts,
        }
    }
}
