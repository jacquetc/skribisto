// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.8.0 from entity_dtos.tera

use common::entities::BinderTag;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct BinderTagDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub color: String,
    pub details: String,
    pub discoverable: bool,
}

impl From<BinderTagDto> for BinderTag {
    fn from(dto: BinderTagDto) -> Self {
        BinderTag {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            color: dto.color,
            details: dto.details,
            discoverable: dto.discoverable,
        }
    }
}

impl From<&BinderTagDto> for BinderTag {
    fn from(dto: &BinderTagDto) -> Self {
        BinderTag {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            color: dto.color.clone(),
            details: dto.details.clone(),
            discoverable: dto.discoverable.clone(),
        }
    }
}

impl From<BinderTag> for BinderTagDto {
    fn from(entity: BinderTag) -> Self {
        BinderTagDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            color: entity.color,
            details: entity.details,
            discoverable: entity.discoverable,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateBinderTagDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub color: String,
    pub details: String,
    pub discoverable: bool,
}

impl From<CreateBinderTagDto> for BinderTag {
    fn from(dto: CreateBinderTagDto) -> Self {
        BinderTag {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            color: dto.color,
            details: dto.details,
            discoverable: dto.discoverable,
        }
    }
}

impl From<&CreateBinderTagDto> for BinderTag {
    fn from(dto: &CreateBinderTagDto) -> Self {
        BinderTag {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            color: dto.color.clone(),
            details: dto.details.clone(),
            discoverable: dto.discoverable.clone(),
        }
    }
}

impl From<BinderTag> for CreateBinderTagDto {
    fn from(entity: BinderTag) -> Self {
        CreateBinderTagDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            color: entity.color,
            details: entity.details,
            discoverable: entity.discoverable,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateBinderTagDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub color: String,
    pub details: String,
    pub discoverable: bool,
}

impl From<UpdateBinderTagDto> for BinderTag {
    fn from(dto: UpdateBinderTagDto) -> Self {
        BinderTag {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            color: dto.color,
            details: dto.details,
            discoverable: dto.discoverable,
        }
    }
}

impl From<&UpdateBinderTagDto> for BinderTag {
    fn from(dto: &UpdateBinderTagDto) -> Self {
        BinderTag {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            color: dto.color.clone(),
            details: dto.details.clone(),
            discoverable: dto.discoverable.clone(),
        }
    }
}

impl From<BinderTag> for UpdateBinderTagDto {
    fn from(entity: BinderTag) -> Self {
        UpdateBinderTagDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            color: entity.color,
            details: entity.details,
            discoverable: entity.discoverable,
        }
    }
}

impl From<BinderTagDto> for UpdateBinderTagDto {
    fn from(dto: BinderTagDto) -> Self {
        UpdateBinderTagDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            color: dto.color,
            details: dto.details,
            discoverable: dto.discoverable,
        }
    }
}
