// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet
// Generated by Qleany v1.10.0 from entity_dtos.tera

use common::entities::Milestone;
pub use common::entities::MilestoneKind;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct MilestoneDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub label: String,
    pub target_item: Option<EntityId>,
    pub target_date: chrono::DateTime<chrono::Utc>,
    pub target_word_count: Option<i64>,
    pub kind: MilestoneKind,
}

impl From<MilestoneDto> for Milestone {
    fn from(dto: MilestoneDto) -> Self {
        Milestone {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            label: dto.label,
            target_item: dto.target_item,
            target_date: dto.target_date,
            target_word_count: dto.target_word_count,
            kind: dto.kind,
        }
    }
}

impl From<&MilestoneDto> for Milestone {
    fn from(dto: &MilestoneDto) -> Self {
        Milestone {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            label: dto.label.clone(),
            target_item: dto.target_item.clone(),
            target_date: dto.target_date.clone(),
            target_word_count: dto.target_word_count.clone(),
            kind: dto.kind.clone(),
        }
    }
}

impl From<Milestone> for MilestoneDto {
    fn from(entity: Milestone) -> Self {
        MilestoneDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            label: entity.label,
            target_item: entity.target_item,
            target_date: entity.target_date,
            target_word_count: entity.target_word_count,
            kind: entity.kind,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateMilestoneDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub label: String,
    pub target_item: Option<EntityId>,
    pub target_date: chrono::DateTime<chrono::Utc>,
    pub target_word_count: Option<i64>,
    pub kind: MilestoneKind,
}

impl From<CreateMilestoneDto> for Milestone {
    fn from(dto: CreateMilestoneDto) -> Self {
        Milestone {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            label: dto.label,
            target_item: dto.target_item,
            target_date: dto.target_date,
            target_word_count: dto.target_word_count,
            kind: dto.kind,
        }
    }
}

impl From<&CreateMilestoneDto> for Milestone {
    fn from(dto: &CreateMilestoneDto) -> Self {
        Milestone {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            label: dto.label.clone(),
            target_item: dto.target_item.clone(),
            target_date: dto.target_date.clone(),
            target_word_count: dto.target_word_count.clone(),
            kind: dto.kind.clone(),
        }
    }
}

impl From<Milestone> for CreateMilestoneDto {
    fn from(entity: Milestone) -> Self {
        CreateMilestoneDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            label: entity.label,
            target_item: entity.target_item,
            target_date: entity.target_date,
            target_word_count: entity.target_word_count,
            kind: entity.kind,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateMilestoneDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub label: String,
    pub target_date: chrono::DateTime<chrono::Utc>,
    pub target_word_count: Option<i64>,
    pub kind: MilestoneKind,
}

impl From<UpdateMilestoneDto> for Milestone {
    fn from(dto: UpdateMilestoneDto) -> Self {
        Milestone {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            label: dto.label,
            target_date: dto.target_date,
            target_word_count: dto.target_word_count,
            kind: dto.kind,
            target_item: Default::default(),
        }
    }
}

impl From<&UpdateMilestoneDto> for Milestone {
    fn from(dto: &UpdateMilestoneDto) -> Self {
        Milestone {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            label: dto.label.clone(),
            target_date: dto.target_date.clone(),
            target_word_count: dto.target_word_count.clone(),
            kind: dto.kind.clone(),
            target_item: Default::default(),
        }
    }
}

impl From<Milestone> for UpdateMilestoneDto {
    fn from(entity: Milestone) -> Self {
        UpdateMilestoneDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            label: entity.label,
            target_date: entity.target_date,
            target_word_count: entity.target_word_count,
            kind: entity.kind,
        }
    }
}

impl From<MilestoneDto> for UpdateMilestoneDto {
    fn from(dto: MilestoneDto) -> Self {
        UpdateMilestoneDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            label: dto.label,
            target_date: dto.target_date,
            target_word_count: dto.target_word_count,
            kind: dto.kind,
        }
    }
}
pub use common::direct_access::milestone::MilestoneRelationshipField;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct MilestoneRelationshipDto {
    pub id: EntityId,
    pub field: MilestoneRelationshipField,
    pub right_ids: Vec<EntityId>,
}
