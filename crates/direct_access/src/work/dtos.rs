// Generated by Qleany v1.7.4 from entity_dtos.tera

use common::entities::Work;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct WorkDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub title: String,
    pub author_name: String,
    pub dict_language: String,
    pub binders: Vec<EntityId>,
    pub tags: Vec<EntityId>,
    pub dict_words: Vec<EntityId>,
}

impl From<WorkDto> for Work {
    fn from(dto: WorkDto) -> Self {
        Work {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            author_name: dto.author_name,
            dict_language: dto.dict_language,
            binders: dto.binders,
            tags: dto.tags,
            dict_words: dto.dict_words,
        }
    }
}

impl From<&WorkDto> for Work {
    fn from(dto: &WorkDto) -> Self {
        Work {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            title: dto.title.clone(),
            author_name: dto.author_name.clone(),
            dict_language: dto.dict_language.clone(),
            binders: dto.binders.clone(),
            tags: dto.tags.clone(),
            dict_words: dto.dict_words.clone(),
        }
    }
}

impl From<Work> for WorkDto {
    fn from(entity: Work) -> Self {
        WorkDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            title: entity.title,
            author_name: entity.author_name,
            dict_language: entity.dict_language,
            binders: entity.binders,
            tags: entity.tags,
            dict_words: entity.dict_words,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateWorkDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub title: String,
    pub author_name: String,
    pub dict_language: String,
    pub binders: Vec<EntityId>,
    pub tags: Vec<EntityId>,
    pub dict_words: Vec<EntityId>,
}

impl From<CreateWorkDto> for Work {
    fn from(dto: CreateWorkDto) -> Self {
        Work {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            author_name: dto.author_name,
            dict_language: dto.dict_language,
            binders: dto.binders,
            tags: dto.tags,
            dict_words: dto.dict_words,
        }
    }
}

impl From<&CreateWorkDto> for Work {
    fn from(dto: &CreateWorkDto) -> Self {
        Work {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            title: dto.title.clone(),
            author_name: dto.author_name.clone(),
            dict_language: dto.dict_language.clone(),
            binders: dto.binders.clone(),
            tags: dto.tags.clone(),
            dict_words: dto.dict_words.clone(),
        }
    }
}

impl From<Work> for CreateWorkDto {
    fn from(entity: Work) -> Self {
        CreateWorkDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            title: entity.title,
            author_name: entity.author_name,
            dict_language: entity.dict_language,
            binders: entity.binders,
            tags: entity.tags,
            dict_words: entity.dict_words,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateWorkDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub title: String,
    pub author_name: String,
    pub dict_language: String,
}

impl From<UpdateWorkDto> for Work {
    fn from(dto: UpdateWorkDto) -> Self {
        Work {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            author_name: dto.author_name,
            dict_language: dto.dict_language,
            binders: Default::default(),
            tags: Default::default(),
            dict_words: Default::default(),
        }
    }
}

impl From<&UpdateWorkDto> for Work {
    fn from(dto: &UpdateWorkDto) -> Self {
        Work {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            title: dto.title.clone(),
            author_name: dto.author_name.clone(),
            dict_language: dto.dict_language.clone(),
            binders: Default::default(),
            tags: Default::default(),
            dict_words: Default::default(),
        }
    }
}

impl From<Work> for UpdateWorkDto {
    fn from(entity: Work) -> Self {
        UpdateWorkDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            title: entity.title,
            author_name: entity.author_name,
            dict_language: entity.dict_language,
        }
    }
}

impl From<WorkDto> for UpdateWorkDto {
    fn from(dto: WorkDto) -> Self {
        UpdateWorkDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            author_name: dto.author_name,
            dict_language: dto.dict_language,
        }
    }
}
pub use common::direct_access::work::WorkRelationshipField;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct WorkRelationshipDto {
    pub id: EntityId,
    pub field: WorkRelationshipField,
    pub right_ids: Vec<EntityId>,
}
