// Generated by Qleany v1.8.0 from entity_dtos.tera

use common::entities::RecentWork;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct RecentWorkDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub title: String,
    pub last_opened_at: chrono::DateTime<chrono::Utc>,
    pub absolute_path: String,
}

impl From<RecentWorkDto> for RecentWork {
    fn from(dto: RecentWorkDto) -> Self {
        RecentWork {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            last_opened_at: dto.last_opened_at,
            absolute_path: dto.absolute_path,
        }
    }
}

impl From<&RecentWorkDto> for RecentWork {
    fn from(dto: &RecentWorkDto) -> Self {
        RecentWork {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            title: dto.title.clone(),
            last_opened_at: dto.last_opened_at.clone(),
            absolute_path: dto.absolute_path.clone(),
        }
    }
}

impl From<RecentWork> for RecentWorkDto {
    fn from(entity: RecentWork) -> Self {
        RecentWorkDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            title: entity.title,
            last_opened_at: entity.last_opened_at,
            absolute_path: entity.absolute_path,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateRecentWorkDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub title: String,
    pub last_opened_at: chrono::DateTime<chrono::Utc>,
    pub absolute_path: String,
}

impl From<CreateRecentWorkDto> for RecentWork {
    fn from(dto: CreateRecentWorkDto) -> Self {
        RecentWork {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            last_opened_at: dto.last_opened_at,
            absolute_path: dto.absolute_path,
        }
    }
}

impl From<&CreateRecentWorkDto> for RecentWork {
    fn from(dto: &CreateRecentWorkDto) -> Self {
        RecentWork {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            title: dto.title.clone(),
            last_opened_at: dto.last_opened_at.clone(),
            absolute_path: dto.absolute_path.clone(),
        }
    }
}

impl From<RecentWork> for CreateRecentWorkDto {
    fn from(entity: RecentWork) -> Self {
        CreateRecentWorkDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            title: entity.title,
            last_opened_at: entity.last_opened_at,
            absolute_path: entity.absolute_path,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateRecentWorkDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub title: String,
    pub last_opened_at: chrono::DateTime<chrono::Utc>,
    pub absolute_path: String,
}

impl From<UpdateRecentWorkDto> for RecentWork {
    fn from(dto: UpdateRecentWorkDto) -> Self {
        RecentWork {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            last_opened_at: dto.last_opened_at,
            absolute_path: dto.absolute_path,
        }
    }
}

impl From<&UpdateRecentWorkDto> for RecentWork {
    fn from(dto: &UpdateRecentWorkDto) -> Self {
        RecentWork {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            title: dto.title.clone(),
            last_opened_at: dto.last_opened_at.clone(),
            absolute_path: dto.absolute_path.clone(),
        }
    }
}

impl From<RecentWork> for UpdateRecentWorkDto {
    fn from(entity: RecentWork) -> Self {
        UpdateRecentWorkDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            title: entity.title,
            last_opened_at: entity.last_opened_at,
            absolute_path: entity.absolute_path,
        }
    }
}

impl From<RecentWorkDto> for UpdateRecentWorkDto {
    fn from(dto: RecentWorkDto) -> Self {
        UpdateRecentWorkDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            last_opened_at: dto.last_opened_at,
            absolute_path: dto.absolute_path,
        }
    }
}
