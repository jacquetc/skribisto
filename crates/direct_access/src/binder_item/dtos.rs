// Generated by Qleany v1.7.4 from entity_dtos.tera

use common::entities::BinderItem;
pub use common::entities::BinderItemRole;
pub use common::entities::BinderItemSubRole;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct BinderItemDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub title: String,
    pub sub_title: String,
    pub role: BinderItemRole,
    pub sub_role: BinderItemSubRole,
    pub label: String,
    pub activated: bool,
    pub is_favorite: bool,
    pub is_printable: bool,
    pub indent: i64,
    pub word_count_goal: i64,
    pub char_count_goal: i64,
    pub dict_language: String,
    pub contents: Vec<EntityId>,
    pub references: Vec<EntityId>,
    pub tags: Vec<EntityId>,
}

impl From<BinderItemDto> for BinderItem {
    fn from(dto: BinderItemDto) -> Self {
        BinderItem {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            sub_title: dto.sub_title,
            role: dto.role,
            sub_role: dto.sub_role,
            label: dto.label,
            activated: dto.activated,
            is_favorite: dto.is_favorite,
            is_printable: dto.is_printable,
            indent: dto.indent,
            word_count_goal: dto.word_count_goal,
            char_count_goal: dto.char_count_goal,
            dict_language: dto.dict_language,
            contents: dto.contents,
            references: dto.references,
            tags: dto.tags,
        }
    }
}

impl From<&BinderItemDto> for BinderItem {
    fn from(dto: &BinderItemDto) -> Self {
        BinderItem {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            title: dto.title.clone(),
            sub_title: dto.sub_title.clone(),
            role: dto.role.clone(),
            sub_role: dto.sub_role.clone(),
            label: dto.label.clone(),
            activated: dto.activated.clone(),
            is_favorite: dto.is_favorite.clone(),
            is_printable: dto.is_printable.clone(),
            indent: dto.indent.clone(),
            word_count_goal: dto.word_count_goal.clone(),
            char_count_goal: dto.char_count_goal.clone(),
            dict_language: dto.dict_language.clone(),
            contents: dto.contents.clone(),
            references: dto.references.clone(),
            tags: dto.tags.clone(),
        }
    }
}

impl From<BinderItem> for BinderItemDto {
    fn from(entity: BinderItem) -> Self {
        BinderItemDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            title: entity.title,
            sub_title: entity.sub_title,
            role: entity.role,
            sub_role: entity.sub_role,
            label: entity.label,
            activated: entity.activated,
            is_favorite: entity.is_favorite,
            is_printable: entity.is_printable,
            indent: entity.indent,
            word_count_goal: entity.word_count_goal,
            char_count_goal: entity.char_count_goal,
            dict_language: entity.dict_language,
            contents: entity.contents,
            references: entity.references,
            tags: entity.tags,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateBinderItemDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub title: String,
    pub sub_title: String,
    pub role: BinderItemRole,
    pub sub_role: BinderItemSubRole,
    pub label: String,
    pub activated: bool,
    pub is_favorite: bool,
    pub is_printable: bool,
    pub indent: i64,
    pub word_count_goal: i64,
    pub char_count_goal: i64,
    pub dict_language: String,
    pub contents: Vec<EntityId>,
    pub references: Vec<EntityId>,
    pub tags: Vec<EntityId>,
}

impl From<CreateBinderItemDto> for BinderItem {
    fn from(dto: CreateBinderItemDto) -> Self {
        BinderItem {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            sub_title: dto.sub_title,
            role: dto.role,
            sub_role: dto.sub_role,
            label: dto.label,
            activated: dto.activated,
            is_favorite: dto.is_favorite,
            is_printable: dto.is_printable,
            indent: dto.indent,
            word_count_goal: dto.word_count_goal,
            char_count_goal: dto.char_count_goal,
            dict_language: dto.dict_language,
            contents: dto.contents,
            references: dto.references,
            tags: dto.tags,
        }
    }
}

impl From<&CreateBinderItemDto> for BinderItem {
    fn from(dto: &CreateBinderItemDto) -> Self {
        BinderItem {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            title: dto.title.clone(),
            sub_title: dto.sub_title.clone(),
            role: dto.role.clone(),
            sub_role: dto.sub_role.clone(),
            label: dto.label.clone(),
            activated: dto.activated.clone(),
            is_favorite: dto.is_favorite.clone(),
            is_printable: dto.is_printable.clone(),
            indent: dto.indent.clone(),
            word_count_goal: dto.word_count_goal.clone(),
            char_count_goal: dto.char_count_goal.clone(),
            dict_language: dto.dict_language.clone(),
            contents: dto.contents.clone(),
            references: dto.references.clone(),
            tags: dto.tags.clone(),
        }
    }
}

impl From<BinderItem> for CreateBinderItemDto {
    fn from(entity: BinderItem) -> Self {
        CreateBinderItemDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            title: entity.title,
            sub_title: entity.sub_title,
            role: entity.role,
            sub_role: entity.sub_role,
            label: entity.label,
            activated: entity.activated,
            is_favorite: entity.is_favorite,
            is_printable: entity.is_printable,
            indent: entity.indent,
            word_count_goal: entity.word_count_goal,
            char_count_goal: entity.char_count_goal,
            dict_language: entity.dict_language,
            contents: entity.contents,
            references: entity.references,
            tags: entity.tags,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateBinderItemDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub title: String,
    pub sub_title: String,
    pub role: BinderItemRole,
    pub sub_role: BinderItemSubRole,
    pub label: String,
    pub activated: bool,
    pub is_favorite: bool,
    pub is_printable: bool,
    pub indent: i64,
    pub word_count_goal: i64,
    pub char_count_goal: i64,
    pub dict_language: String,
}

impl From<UpdateBinderItemDto> for BinderItem {
    fn from(dto: UpdateBinderItemDto) -> Self {
        BinderItem {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            sub_title: dto.sub_title,
            role: dto.role,
            sub_role: dto.sub_role,
            label: dto.label,
            activated: dto.activated,
            is_favorite: dto.is_favorite,
            is_printable: dto.is_printable,
            indent: dto.indent,
            word_count_goal: dto.word_count_goal,
            char_count_goal: dto.char_count_goal,
            dict_language: dto.dict_language,
            contents: Default::default(),
            references: Default::default(),
            tags: Default::default(),
        }
    }
}

impl From<&UpdateBinderItemDto> for BinderItem {
    fn from(dto: &UpdateBinderItemDto) -> Self {
        BinderItem {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            title: dto.title.clone(),
            sub_title: dto.sub_title.clone(),
            role: dto.role.clone(),
            sub_role: dto.sub_role.clone(),
            label: dto.label.clone(),
            activated: dto.activated.clone(),
            is_favorite: dto.is_favorite.clone(),
            is_printable: dto.is_printable.clone(),
            indent: dto.indent.clone(),
            word_count_goal: dto.word_count_goal.clone(),
            char_count_goal: dto.char_count_goal.clone(),
            dict_language: dto.dict_language.clone(),
            contents: Default::default(),
            references: Default::default(),
            tags: Default::default(),
        }
    }
}

impl From<BinderItem> for UpdateBinderItemDto {
    fn from(entity: BinderItem) -> Self {
        UpdateBinderItemDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            title: entity.title,
            sub_title: entity.sub_title,
            role: entity.role,
            sub_role: entity.sub_role,
            label: entity.label,
            activated: entity.activated,
            is_favorite: entity.is_favorite,
            is_printable: entity.is_printable,
            indent: entity.indent,
            word_count_goal: entity.word_count_goal,
            char_count_goal: entity.char_count_goal,
            dict_language: entity.dict_language,
        }
    }
}

impl From<BinderItemDto> for UpdateBinderItemDto {
    fn from(dto: BinderItemDto) -> Self {
        UpdateBinderItemDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            title: dto.title,
            sub_title: dto.sub_title,
            role: dto.role,
            sub_role: dto.sub_role,
            label: dto.label,
            activated: dto.activated,
            is_favorite: dto.is_favorite,
            is_printable: dto.is_printable,
            indent: dto.indent,
            word_count_goal: dto.word_count_goal,
            char_count_goal: dto.char_count_goal,
            dict_language: dto.dict_language,
        }
    }
}
pub use common::direct_access::binder_item::BinderItemRelationshipField;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct BinderItemRelationshipDto {
    pub id: EntityId,
    pub field: BinderItemRelationshipField,
    pub right_ids: Vec<EntityId>,
}
