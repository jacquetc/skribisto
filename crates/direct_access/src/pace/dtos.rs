// Generated by Qleany v1.8.0 from entity_dtos.tera

use common::entities::Pace;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct PaceDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub book_item: Option<EntityId>,
    pub start_date: chrono::DateTime<chrono::Utc>,
    pub end_date: chrono::DateTime<chrono::Utc>,
    pub weekday_mask: i64,
    pub active: bool,
    pub holidays: Vec<EntityId>,
    pub milestones: Vec<EntityId>,
}

impl From<PaceDto> for Pace {
    fn from(dto: PaceDto) -> Self {
        Pace {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            book_item: dto.book_item,
            start_date: dto.start_date,
            end_date: dto.end_date,
            weekday_mask: dto.weekday_mask,
            active: dto.active,
            holidays: dto.holidays,
            milestones: dto.milestones,
        }
    }
}

impl From<&PaceDto> for Pace {
    fn from(dto: &PaceDto) -> Self {
        Pace {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            book_item: dto.book_item.clone(),
            start_date: dto.start_date.clone(),
            end_date: dto.end_date.clone(),
            weekday_mask: dto.weekday_mask.clone(),
            active: dto.active.clone(),
            holidays: dto.holidays.clone(),
            milestones: dto.milestones.clone(),
        }
    }
}

impl From<Pace> for PaceDto {
    fn from(entity: Pace) -> Self {
        PaceDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            book_item: entity.book_item,
            start_date: entity.start_date,
            end_date: entity.end_date,
            weekday_mask: entity.weekday_mask,
            active: entity.active,
            holidays: entity.holidays,
            milestones: entity.milestones,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreatePaceDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub book_item: Option<EntityId>,
    pub start_date: chrono::DateTime<chrono::Utc>,
    pub end_date: chrono::DateTime<chrono::Utc>,
    pub weekday_mask: i64,
    pub active: bool,
    pub holidays: Vec<EntityId>,
    pub milestones: Vec<EntityId>,
}

impl From<CreatePaceDto> for Pace {
    fn from(dto: CreatePaceDto) -> Self {
        Pace {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            book_item: dto.book_item,
            start_date: dto.start_date,
            end_date: dto.end_date,
            weekday_mask: dto.weekday_mask,
            active: dto.active,
            holidays: dto.holidays,
            milestones: dto.milestones,
        }
    }
}

impl From<&CreatePaceDto> for Pace {
    fn from(dto: &CreatePaceDto) -> Self {
        Pace {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            book_item: dto.book_item.clone(),
            start_date: dto.start_date.clone(),
            end_date: dto.end_date.clone(),
            weekday_mask: dto.weekday_mask.clone(),
            active: dto.active.clone(),
            holidays: dto.holidays.clone(),
            milestones: dto.milestones.clone(),
        }
    }
}

impl From<Pace> for CreatePaceDto {
    fn from(entity: Pace) -> Self {
        CreatePaceDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            book_item: entity.book_item,
            start_date: entity.start_date,
            end_date: entity.end_date,
            weekday_mask: entity.weekday_mask,
            active: entity.active,
            holidays: entity.holidays,
            milestones: entity.milestones,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdatePaceDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub start_date: chrono::DateTime<chrono::Utc>,
    pub end_date: chrono::DateTime<chrono::Utc>,
    pub weekday_mask: i64,
    pub active: bool,
}

impl From<UpdatePaceDto> for Pace {
    fn from(dto: UpdatePaceDto) -> Self {
        Pace {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            start_date: dto.start_date,
            end_date: dto.end_date,
            weekday_mask: dto.weekday_mask,
            active: dto.active,
            book_item: Default::default(),
            holidays: Default::default(),
            milestones: Default::default(),
        }
    }
}

impl From<&UpdatePaceDto> for Pace {
    fn from(dto: &UpdatePaceDto) -> Self {
        Pace {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            start_date: dto.start_date.clone(),
            end_date: dto.end_date.clone(),
            weekday_mask: dto.weekday_mask.clone(),
            active: dto.active.clone(),
            book_item: Default::default(),
            holidays: Default::default(),
            milestones: Default::default(),
        }
    }
}

impl From<Pace> for UpdatePaceDto {
    fn from(entity: Pace) -> Self {
        UpdatePaceDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            start_date: entity.start_date,
            end_date: entity.end_date,
            weekday_mask: entity.weekday_mask,
            active: entity.active,
        }
    }
}

impl From<PaceDto> for UpdatePaceDto {
    fn from(dto: PaceDto) -> Self {
        UpdatePaceDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            start_date: dto.start_date,
            end_date: dto.end_date,
            weekday_mask: dto.weekday_mask,
            active: dto.active,
        }
    }
}
pub use common::direct_access::pace::PaceRelationshipField;

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct PaceRelationshipDto {
    pub id: EntityId,
    pub field: PaceRelationshipField,
    pub right_ids: Vec<EntityId>,
}
