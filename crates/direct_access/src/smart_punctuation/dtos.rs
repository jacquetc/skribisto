// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.9.0 from entity_dtos.tera

pub use common::entities::QuoteStyle;
use common::entities::SmartPunctuation;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct SmartPunctuationDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub override_app_default: bool,
    pub dashes: bool,
    pub ellipsis: bool,
    pub quotes: bool,
    pub quote_style: QuoteStyle,
    pub pre_punctuation_spacing: bool,
    pub dialogue_marker: bool,
}

impl From<SmartPunctuationDto> for SmartPunctuation {
    fn from(dto: SmartPunctuationDto) -> Self {
        SmartPunctuation {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            override_app_default: dto.override_app_default,
            dashes: dto.dashes,
            ellipsis: dto.ellipsis,
            quotes: dto.quotes,
            quote_style: dto.quote_style,
            pre_punctuation_spacing: dto.pre_punctuation_spacing,
            dialogue_marker: dto.dialogue_marker,
        }
    }
}

impl From<&SmartPunctuationDto> for SmartPunctuation {
    fn from(dto: &SmartPunctuationDto) -> Self {
        SmartPunctuation {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            override_app_default: dto.override_app_default.clone(),
            dashes: dto.dashes.clone(),
            ellipsis: dto.ellipsis.clone(),
            quotes: dto.quotes.clone(),
            quote_style: dto.quote_style.clone(),
            pre_punctuation_spacing: dto.pre_punctuation_spacing.clone(),
            dialogue_marker: dto.dialogue_marker.clone(),
        }
    }
}

impl From<SmartPunctuation> for SmartPunctuationDto {
    fn from(entity: SmartPunctuation) -> Self {
        SmartPunctuationDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            override_app_default: entity.override_app_default,
            dashes: entity.dashes,
            ellipsis: entity.ellipsis,
            quotes: entity.quotes,
            quote_style: entity.quote_style,
            pre_punctuation_spacing: entity.pre_punctuation_spacing,
            dialogue_marker: entity.dialogue_marker,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CreateSmartPunctuationDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub override_app_default: bool,
    pub dashes: bool,
    pub ellipsis: bool,
    pub quotes: bool,
    pub quote_style: QuoteStyle,
    pub pre_punctuation_spacing: bool,
    pub dialogue_marker: bool,
}

impl From<CreateSmartPunctuationDto> for SmartPunctuation {
    fn from(dto: CreateSmartPunctuationDto) -> Self {
        SmartPunctuation {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            override_app_default: dto.override_app_default,
            dashes: dto.dashes,
            ellipsis: dto.ellipsis,
            quotes: dto.quotes,
            quote_style: dto.quote_style,
            pre_punctuation_spacing: dto.pre_punctuation_spacing,
            dialogue_marker: dto.dialogue_marker,
        }
    }
}

impl From<&CreateSmartPunctuationDto> for SmartPunctuation {
    fn from(dto: &CreateSmartPunctuationDto) -> Self {
        SmartPunctuation {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            override_app_default: dto.override_app_default.clone(),
            dashes: dto.dashes.clone(),
            ellipsis: dto.ellipsis.clone(),
            quotes: dto.quotes.clone(),
            quote_style: dto.quote_style.clone(),
            pre_punctuation_spacing: dto.pre_punctuation_spacing.clone(),
            dialogue_marker: dto.dialogue_marker.clone(),
        }
    }
}

impl From<SmartPunctuation> for CreateSmartPunctuationDto {
    fn from(entity: SmartPunctuation) -> Self {
        CreateSmartPunctuationDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            override_app_default: entity.override_app_default,
            dashes: entity.dashes,
            ellipsis: entity.ellipsis,
            quotes: entity.quotes,
            quote_style: entity.quote_style,
            pre_punctuation_spacing: entity.pre_punctuation_spacing,
            dialogue_marker: entity.dialogue_marker,
        }
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct UpdateSmartPunctuationDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub override_app_default: bool,
    pub dashes: bool,
    pub ellipsis: bool,
    pub quotes: bool,
    pub quote_style: QuoteStyle,
    pub pre_punctuation_spacing: bool,
    pub dialogue_marker: bool,
}

impl From<UpdateSmartPunctuationDto> for SmartPunctuation {
    fn from(dto: UpdateSmartPunctuationDto) -> Self {
        SmartPunctuation {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            override_app_default: dto.override_app_default,
            dashes: dto.dashes,
            ellipsis: dto.ellipsis,
            quotes: dto.quotes,
            quote_style: dto.quote_style,
            pre_punctuation_spacing: dto.pre_punctuation_spacing,
            dialogue_marker: dto.dialogue_marker,
        }
    }
}

impl From<&UpdateSmartPunctuationDto> for SmartPunctuation {
    fn from(dto: &UpdateSmartPunctuationDto) -> Self {
        SmartPunctuation {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            override_app_default: dto.override_app_default.clone(),
            dashes: dto.dashes.clone(),
            ellipsis: dto.ellipsis.clone(),
            quotes: dto.quotes.clone(),
            quote_style: dto.quote_style.clone(),
            pre_punctuation_spacing: dto.pre_punctuation_spacing.clone(),
            dialogue_marker: dto.dialogue_marker.clone(),
        }
    }
}

impl From<SmartPunctuation> for UpdateSmartPunctuationDto {
    fn from(entity: SmartPunctuation) -> Self {
        UpdateSmartPunctuationDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            override_app_default: entity.override_app_default,
            dashes: entity.dashes,
            ellipsis: entity.ellipsis,
            quotes: entity.quotes,
            quote_style: entity.quote_style,
            pre_punctuation_spacing: entity.pre_punctuation_spacing,
            dialogue_marker: entity.dialogue_marker,
        }
    }
}

impl From<SmartPunctuationDto> for UpdateSmartPunctuationDto {
    fn from(dto: SmartPunctuationDto) -> Self {
        UpdateSmartPunctuationDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            override_app_default: dto.override_app_default,
            dashes: dto.dashes,
            ellipsis: dto.ellipsis,
            quotes: dto.quotes,
            quote_style: dto.quote_style,
            pre_punctuation_spacing: dto.pre_punctuation_spacing,
            dialogue_marker: dto.dialogue_marker,
        }
    }
}
