# SPDX-License-Identifier: GPL-3.0-only
# SPDX-FileCopyrightText: 2026 Cyril Jacquet

# Hand-written (NOT generated by Qleany). Pure ordered-list / indent-tree
# primitives over a Binder's `binder_items` relationship vec, shared by
# binder_item_management::move_items and trash_management::restore_items_to.
[package]
name = "skribisto-binder-ordering"
description = "Pure ordered-list / indent-tree primitives for Skribisto's binder model"
authors.workspace = true
version.workspace = true
edition.workspace = true
license.workspace = true
readme = "../../README.md"
publish = false

[lib]
name = "binder_ordering"

[dependencies]
common = { workspace = true }
anyhow = { workspace = true }

[lints]
workspace = true
