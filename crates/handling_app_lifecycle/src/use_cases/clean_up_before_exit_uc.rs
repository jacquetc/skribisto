// Generated by Qleany v1.7.4 from feature_use_case.tera
use anyhow::Result;
use common::database::CommandUnitOfWork;
use common::entities::Root;
use common::types::EntityId;

pub trait CleanUpBeforeExitUnitOfWorkFactoryTrait: Send + Sync {
    fn create(&self) -> Box<dyn CleanUpBeforeExitUnitOfWorkTrait>;
}

//TODO: adapt entities and actions to real use :
// Create, CreateMulti, Get, GetMulti, Update (scalar-only), UpdateMulti (scalar-only),
// UpdateWithRelationships, UpdateWithRelationshipsMulti,
// Remove, RemoveMulti, GetRelationship, GetRelationshipsFromRightIds,
// SetRelationship, SetRelationshipMulti
//
// You have here a read-write unit of work trait.
//
// RO means Read Only.
// Do not mix read-only and write actions in the same unit of work.
//
// Exactly the same macros must be set in the use case uow trait file in ../units_of_work/clean_up_before_exit_uow.rs
//
#[macros::uow_action(entity = "Root", action = "Get")]
#[macros::uow_action(entity = "Root", action = "GetMulti")]
#[macros::uow_action(entity = "Root", action = "Snapshot")]
#[macros::uow_action(entity = "Root", action = "Restore")]
pub trait CleanUpBeforeExitUnitOfWorkTrait: CommandUnitOfWork {
    fn publish_clean_up_before_exit_event(&self, ids: Vec<EntityId>, data: Option<String>);
}

pub struct CleanUpBeforeExitUseCase {
    uow_factory: Box<dyn CleanUpBeforeExitUnitOfWorkFactoryTrait>,
}

impl CleanUpBeforeExitUseCase {
    pub fn new(uow_factory: Box<dyn CleanUpBeforeExitUnitOfWorkFactoryTrait>) -> Self {
        CleanUpBeforeExitUseCase { uow_factory }
    }

    #[allow(unreachable_code)]
    pub fn execute(&mut self) -> Result<()> {
        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;

        //TODO: CleanUpBeforeExitUseCase to be implemented
        unimplemented!("CleanUpBeforeExitUseCase unimplemented");
        uow.commit()?;

        uow.publish_clean_up_before_exit_event(vec![], None);
        Ok(())
    }
}
