// SPDX-License-Identifier: GPL-3.0-only
// SPDX-FileCopyrightText: 2026 Cyril Jacquet

// Generated by Qleany v1.8.0 from feature_dtos.tera

// NOTE: the generator's import inference wrongly emitted
// `use common::entities::{MentionEntity, MentionHit};` here — those two enums are not
// entities, they are defined further down in THIS file (from the use case's own
// `enum_values`), so importing them creates a name collision with their own
// declarations. Hand-corrected back to the real dependency, `EntityId`, used inside
// `MentionHit::Found` / `MentionEntity::Discoverable` below.
use common::types::EntityId;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct ScanMentionsDto {
    pub work_id: u64,
}
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct MentionScanResultDto {
    pub row: MentionHit,
    pub hits: MentionHits,
    pub entity: MentionEntity,
    pub table: MentionTable,
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum MentionHit {
    #[default]
    Empty,
    Found {
        owner_id: EntityId,
        target_id: EntityId,
        title: String,
        /// Every distinct name of the target that matched in this document, in the order
        /// they were first met: its title, its aliases, or several of them. A scene that
        /// writes "Elizabeth" twice and "Lizzy" once carries both, because which names a
        /// writer actually reaches for in a given scene is the interesting part, and a
        /// single name would have to pick one and silently drop the rest.
        matched_names: Vec<String>,
        is_title_match: bool,
        hit_count: i64,
        is_confirmed: bool,
        is_point_of_view: bool,
        evidence: String,
    },
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum MentionHits {
    #[default]
    Empty,
    Found(Vec<MentionHit>),
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum MentionEntity {
    #[default]
    Empty,
    Discoverable {
        id: EntityId,
        title: String,
        aliases: Vec<String>,
    },
}

#[derive(Serialize, Deserialize, Default, Clone, Debug, PartialEq, Eq)]
pub enum MentionTable {
    #[default]
    Empty,
    Entities(Vec<MentionEntity>),
}
