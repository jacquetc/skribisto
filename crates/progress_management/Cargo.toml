# SPDX-License-Identifier: GPL-3.0-only
# SPDX-FileCopyrightText: 2026 Cyril Jacquet

# Generated by Qleany v1.8.0 from feature_cargo.tera
[package]
name = "skribisto-progress-management"
description = "progress_management feature for Skribisto"
authors.workspace = true
documentation.workspace = true
keywords.workspace = true
categories.workspace = true
version.workspace = true
edition.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
readme = "../../README.md"
publish = true

[lib]
name = "progress_management"

[dependencies]
anyhow = { workspace = true }
common = { workspace = true }
macros = { workspace = true }
serde = "1.0"
serde_json = "1.0"
chrono = { workspace = true }
uuid = { workspace = true }
# The shared frozen tree reader + scope spine, the compile item-metas, and the
# counting policy/cache — count_words counts exactly what export would emit.
skrib_format = { workspace = true }
skribisto_model = { workspace = true }
skribisto_compiler = { workspace = true }

[dev-dependencies]
# Test setup only: build a real store (new_work) and read snapshots back.
work_management = { workspace = true }
direct_access = { workspace = true }
tempfile = "3"

[lints]
workspace = true



