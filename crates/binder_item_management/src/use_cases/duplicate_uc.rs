// Generated by Qleany v1.7.4 from feature_use_case.tera
use crate::DuplicateDto;
use crate::DuplicateReturnDto;
use anyhow::{Result, anyhow};
use common::database::CommandUnitOfWork;
use common::entities::{Binder, BinderItem, Content};
use common::types::EntityId;

pub trait DuplicateUnitOfWorkFactoryTrait: Send + Sync {
    fn create(&self) -> Box<dyn DuplicateUnitOfWorkTrait>;
}

//TODO: adapt entities and actions to real use :
// Create, CreateMulti, Get, GetMulti, Update (scalar-only), UpdateMulti (scalar-only),
// UpdateWithRelationships, UpdateWithRelationshipsMulti,
// Remove, RemoveMulti, GetRelationship, GetRelationshipsFromRightIds,
// SetRelationship, SetRelationshipMulti
//
// You have here a read-write unit of work trait.
//
// RO means Read Only.
// Do not mix read-only and write actions in the same unit of work.
//
// Exactly the same macros must be set in the use case uow trait file in ../units_of_work/duplicate_uow.rs
//
#[macros::uow_action(entity = "Binder", action = "Get")]
#[macros::uow_action(entity = "Binder", action = "GetMulti")]
#[macros::uow_action(entity = "Binder", action = "Snapshot")]
#[macros::uow_action(entity = "Binder", action = "Restore")]
#[macros::uow_action(entity = "BinderItem", action = "Get")]
#[macros::uow_action(entity = "BinderItem", action = "GetMulti")]
#[macros::uow_action(entity = "BinderItem", action = "Snapshot")]
#[macros::uow_action(entity = "BinderItem", action = "Restore")]
#[macros::uow_action(entity = "Content", action = "Get")]
#[macros::uow_action(entity = "Content", action = "GetMulti")]
#[macros::uow_action(entity = "Content", action = "Snapshot")]
#[macros::uow_action(entity = "Content", action = "Restore")]
pub trait DuplicateUnitOfWorkTrait: CommandUnitOfWork {
    fn publish_duplicate_event(&self, ids: Vec<EntityId>, data: Option<String>);
}

pub struct DuplicateUseCase {
    uow_factory: Box<dyn DuplicateUnitOfWorkFactoryTrait>,
}

impl DuplicateUseCase {
    pub fn new(uow_factory: Box<dyn DuplicateUnitOfWorkFactoryTrait>) -> Self {
        DuplicateUseCase { uow_factory }
    }

    pub fn execute(&mut self, dto: &DuplicateDto) -> Result<DuplicateReturnDto> {
        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;

        //TODO: DuplicateUseCase to be implemented
        unimplemented!("DuplicateUseCase unimplemented");
        uow.commit()?;

        uow.publish_duplicate_event(vec![], None);
        //Ok(DuplicateReturnDto {
        //
        //})
        // placeholder to allow compilation
        Err(anyhow!("Not implemented"))
    }
}
use common::undo_redo::UndoRedoCommand;
use std::any::Any;
impl UndoRedoCommand for DuplicateUseCase {
    fn undo(&mut self) -> Result<()> {
        //TODO: DuplicateUseCase  undo to be implemented
        unimplemented!("DuplicateUseCase undo unimplemented");

        Ok(())
    }

    fn redo(&mut self) -> Result<()> {
        //TODO: DuplicateUseCase  redo to be implemented
        unimplemented!("DuplicateUseCase redo unimplemented");

        Ok(())
    }
    fn as_any(&self) -> &dyn Any {
        self
    }
}
