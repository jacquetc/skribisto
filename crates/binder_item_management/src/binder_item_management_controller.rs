// Generated by Qleany v1.7.4 from feature_controller.tera

use crate::DuplicateDto;
use crate::DuplicateReturnDto;
use crate::MoveDto;
use crate::units_of_work::duplicate_uow::DuplicateUnitOfWorkFactory;
use crate::units_of_work::move_items_uow::MoveItemsUnitOfWorkFactory;
use crate::use_cases::duplicate_uc::DuplicateUseCase;
use crate::use_cases::move_items_uc::MoveItemsUseCase;
use anyhow::Result;

use common::undo_redo::UndoRedoManager;
use common::{database::db_context::DbContext, event::EventHub};
use std::sync::Arc;

pub fn duplicate(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    dto: &DuplicateDto,
) -> Result<DuplicateReturnDto> {
    let uow_context = DuplicateUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = DuplicateUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(return_dto)
}

pub fn move_items(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    dto: &MoveDto,
) -> Result<()> {
    let uow_context = MoveItemsUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = MoveItemsUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(return_dto)
}
